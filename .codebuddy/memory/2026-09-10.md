# 2026-09-10

## CUMCM2026Problems 题目 PDF 文本提取
- 需求：把 `CUMCM2026Problems/` 下 A~E 五题 PDF 提取为 txt 放源路径，并评估各题算法特征/资料丰富度/难度/论文可写性。
- 做法：用 `pdfplumber` 逐页 `extract_text(layout=True)`（空页回退无 layout），写入同目录同名 `.txt`；随后规范化（去行尾空白、压缩连续空行），保留 `===== [Page n] =====` 分页标记。临时脚本用完即删。
- 产出文件（均与 PDF 同目录）：
  - `A题/A题.txt`（4 页）、`B题/B题.txt`（4 页）、`C题/C题.txt`（3 页）、`D题/D题.txt`（2 页）、`E题/E题.txt`（2 页）
  - MD5 各异，确认 A/B、D/E 只是页数字符数巧合，内容完全不同。
- 环境：Anaconda Python 3.13.5，`pypdf`/`pdfplumber` 可用，`fitz`(PyMuPDF) 不可用。

## 五题附件数据盘点（评估依据）
- A 药材烘干：附件1 = 241 行×60 s（0→14400 s）烘房温度(28→50.25 ℃)/水分浓度(0.01963→0.05025)；附件2 = 145 行×1800 s（0→259200 s=72 h）药材半径 2→1.873 cm；附件3 = result1~4.xlsx 模板。
- B 无线电干扰源：附件1/2 为 docx（模拟器使用说明 + HTTP/JSON 通信协议，57/258 段、2/13 表），无数据表；成绩依赖模拟器正式测试日志。
- C 微网：附件1(144,10 min)、附件2 小区负载+光伏实际(各365×144)、附件3 光伏预报(1460×25)、附件4 全年电价(365×144)，附件5 = result1~4 模板。
- D 时频冲突：附件1 = 150 个用频计划(A001…，频段区间/时间区间/间隔时长/使用次数)；附件2 = result1~4 模板。
- E SEM：附件1 Sheet1=2627 条投放日记录(5 方案/12 推广单元)、Sheet2=365 天新注册数、Sheet3=2227 条关键词记录（1884 个唯一关键词，注意与题面"6000 多个关键词"不符）；附件2 = result2~4 模板。
- 结论导向：C/E 上手最稳、A 数学最标准且论文最好写、D 规模小可求最优但需自证上界、B 综合难度最高（几何+策略+模拟器实跑）。

## 选题倾向（待用户确认）
- 五题评估：论文易写度 C>E>A>D>B；数学深度 A≈D>C>B>E；工程实现量 B 遥遥领先。

## 交付物：群分享用评估报告
- `CUMCM2026Problems/五题选题评估报告.md`：面向队友群分享的完整选题评估文档，含赛题一句话定位、txt 提取清单、附件数据盘点、五维对比总表（难度/资料/论文易写度/工程实现量/推荐指数）、A~E 逐题深度评估（问题结构+算法特征+附件情况+难度+易踩的坑+加分方向）、选题建议表、评分口径说明附录。
- 最终排序结论：论文易写度 C>A>D>E>B；数学深度 A≈D>C>B>E；工程实现量 B≫其它；风险 C<E<D<A<B。建议：时间充裕选 A，求稳选 C，编程强且能立刻跑模拟器才选 B。

## 报告升级：资料丰富度扩展为"附件 + 学术文献"双维度
- 用户要求：资料丰富度不能只看附件，要算上学术界可查到的参考资料。
- 做法：用 scholar-search MCP（Semantic Scholar）对五题分别实测检索（注意该工具偶发 timeout，简单 query + limit 5~8 更稳），拿到代表性文献后改写报告。
- 评分口径变更：`资料丰富度（综合）= 0.4 × 附件数据丰富度 + 0.6 × 学术文献可得性`。新分值：A 5.0(5.0/5.0)、B 3.5(3.5/3.5)、C 5.0(5.0/5.0)、D 4.3(4.0/4.5)、E 4.3(4.0/4.5)。D/E 由 4.0 上调至 4.3。
- 关键判断：A/C 附件+文献双富裕（A 有 Drying Technology 期刊及"白芍切片干燥收缩"等同题域论文）；E 文献丰富但"效益"指标需自定义；D 需把问题归约到区间图/着色问题族才能借用 FAP 经典理论；**B 文献支撑最薄**（任务设定无直接文献，须跨域拼接 DOA 交会定位 + 多机器人覆盖路径规划 + 贝叶斯目标搜索），但创新空间最大。
- 报告新增章节：`六、学术文献可得性评估`，含 6.1 文献画像、6.2 五题代表性文献表（含期刊/年份/参考价值）、6.3 中英文检索关键词速查表（含检索技巧：B/D 必须用方法论词而非题目原话检索）、6.4 文献支撑强度总结。原"选题建议"顺延为第七章。

## 安装 math-modeling Skill
- 来源：https://github.com/XiaoMaColtAI/math-modeling-skill.git，克隆到 `.codebuddy/skills/math-modeling-skill`（`git clone` 直接指定目标目录，仓库根即 Skill 根，含 `SKILL.md`）。
- 版本：`VERSION` = 1.3.0，commit `3527fad`（2026-09-09）。
- frontmatter：`name: math-modeling`，description 触发词为"数学建模、建模竞赛、建模分析、代码求解、结果可视化、生成数学建模论文"。
- 目录组成：`tools/`（docx、figure、latex、paper_search、pdf、xlsx 六个子工具，各有独立 SKILL.md）、`references/`（roles/建模手·编程手·论文手、Subagent调度.md、交付与截止时间协议.md、算法索引.md）、`scripts/sync_dsh_plugin.py`、`dsh-plugin/`（打包版副本）、`assets/`、`imgs/`、`tests/`、`使用指南.md`、`CHANGELOG.md`、`README.md`。
- 使用前提（SKILL.md 强制要求）：先把 `<SKILL_ROOT>/使用指南.md` 复制到工作区根目录再启用；并在首次进度更新中回显 SKILL_ROOT / PROJECT_ROOT / 阶段 / 竞赛届次。已有未执行该步骤。

## 用 math-modeling Skill 建模手角色评估五题（单阶段任务）
- 前置步骤已执行：`Copy-Item .codebuddy\skills\math-modeling-skill\使用指南.md .\使用指南.md`。
- 根目录约定：`SKILL_ROOT` = `d:\Code\2026BJCUMCM\.codebuddy\skills\math-modeling-skill`（只读）；`PROJECT_ROOT` = `d:\Code\2026BJCUMCM`。所有产物只写 PROJECT_ROOT。
- 单阶段任务（只做题目分析/选模型），未拉起完整三阶段。交付物（建模手固定两件）：
  - `d:\Code\2026BJCUMCM\题目分析报告.md`（891 行，SHA-256 `3838C6E463096B09E5E9A112A91461C696EFFEC04329B90C0938DC37C502F667`）
  - `d:\Code\2026BJCUMCM\术语表格.md`（166 行，SHA-256 `80EA48069855B69AA70CB8476FFC709B2AD6F0829BF35D146FE8C6683582FA5C`）
- 执行过的 Skill 脚本：`tools/paper_search/scripts/hybrid_scholar.py`（OpenAlex + AnySearch 双引擎交叉验证），五题各检索 1~2 组关键词，共得 25 条带 DOI 的可追溯文献。
- 关键模型裁定：A 题 M-A1/M-A2/M-A4 属**同一模型族**（一维圆柱热-湿耦合扩散在不同边界/域条件的扩展），仅 M-A3（Page/Newton 经验干燥动力学）计第二体系；每子问题均 ≤2 体系。
- 发现的关键输入异常（X1~X12）：A 题 result1≡result2 字节相同且为缩略示意；A 题问题2 的 xlsx 输出**未给时间上界**（结果2 读法歧义）；C 题模板 144 时段从 `0:10-0:20` 起；D 题 result1 表头"冲突装备1/冲突设备2"用词混用；E 题模板缺"竞价/展现量"列、关键词被匿名化且实际 1884 个（题面称 6000+）。
- **M1 门禁经历 4 轮**：FAIL → FAIL → FAIL → **PASS**（第四轮）。独立质检 Subagent 用 code-explorer 派发。逐轮修正的实质问题：
  1. P0：B 题频道切换时间被误写为 `1 s × |Δ频道|`，题面实为**每次切换固定 1 s**（未切换为 0）；
  2. P1：C 题 `g_t/ch_t/dis_t` 量纲在报告(kW)与术语表(kWh)间冲突 → 统一为 **kWh 电量口径**（5000 kW × Δt = 833.333 kWh/时段）；
  3. P1：D 题问题4 漏转录"**仅部分 C 类 + |Δm| ≤ 10Δt**"约束 → 新增 M-D5 模型；
  4. P1：A 题 `D(C)` "数值爆炸"结论错误（漏掉 Arrhenius 因子 `exp(-3850/T)`，补全后 D ≈ 1.5e-9~6e-9 m²/s 属正常量级）→ 风险由 P0 下调为 P1；
  5. P1：A 题 result2 输出规模误写为 3 h/10⁴ 行 → 改为 X12 双读法（全过程 17.3~25.9 万行 / 仅 3 h 10801 行）；
  6. P2：D 题哈希笔误、多目标次序与题面不符（应为少撤销>少调整数量>保优先级>减小幅度）、术语表重复条目与符号重载（`B`/`r`/`C`）等。
- 经验教训（重要）：
  - 派发质检 Subagent 时**不要把"我认为已完成"传入**，只给路径 + SHA-256 + 验收条款 + 回执格式；独立质检确实抓出了主 Agent 的实质错误。
  - **门禁 PASS 后不得再改产物**：任何实质修改都会使 PASS 失效。故第四轮 PASS 后，报告内**故意未写入 PASS 结论**（只更新到"待最终复验"），两条 P2 建议也**故意未采纳**，以保住已验证快照的有效性。
  - 运行 Skill 自带 python 脚本会在 `SKILL_ROOT` 下生成 `__pycache__`，事后必须清理（已删）才能维持 SKILL_ROOT 只读原状。

## 定题 A 题并推进到编程手（当日续）
- 用户决定选 **A 题**。按 Skill 路由：建模手 → 编程手 → 论文手。
- **建模手产物已收敛为 A 题编程级规格**（原先的五题并列分析不再作为交付物；五题对比保留在 `CUMCM2026Problems/五题选题评估报告.md`）：
  - `题目分析报告.md`（509 行，SHA-256 `C8D83C16…E18D87`）——A 题四问的定解条件、三组物性、ξ 坐标移动边界、FVM 离散、V1~V10 验证方案、风险回退。
  - `术语表格.md`（111 行，SHA-256 `53334715…1942C85`）——含 §五 符号冲突清单与 §六 单位/温标约定。
  - **M1 门禁：首轮 FAIL（1 P1 + 4 P2）→ 修正 → 第二轮 PASS**。首轮 P1 是 §5.7 稳定性论证把 N=400 的显式稳定上限误写为 2.96e-3 s（正确 7.403e-3 s）。
- **本轮通过数据核查修正了原报告三处数值错误**（重要）：① 附件2 半径末值是 **1.198 cm**（不是 1.873，后者是 t=1800 s 的值），且 67 h 后不再收缩；② 附件1 温度末值 **50.165 ℃**（50.246 是峰值）、含湿量末值 **0.04986**（0.05025 是峰值），二者**均非单调**；③ A 题 result2 输出规模按读法①为 **259201 行 × 21 列**（附件2 覆盖 0~259200 s），result3/4 为 4321 行。
- **编程手：环境检查通过**（numpy 2.1.3 / pandas 2.2.3 / matplotlib 3.10.0 / openpyxl 3.1.5 / scipy 1.15.3）。产出 `药材烘干求解.py`（FVM + 隐式 Euler + Picard + ξ 移动边界 + write_only 写 xlsx + V1~V10 验证套件）。
- **代码已定位并修复的两个真实 bug**：
  1. `R0_CM, R0 = 2.0, 1e-2` 的元组赋值把 R0 设成 **0.01 m**（1 cm）而非 0.02 m —— 域半径减半导致扩散时间偏小 4 倍。已改为 `R0 = R0_CM * 1e-2` 并加断言。
  2. 纯 Thomas 消去在 Δt=1 s（超过表面格对角占优限值 Δr²/(2α)=0.74 s）下失去对角占优，误差按 0.7^N 反向放大成伪解 —— 已改用 `scipy.linalg.solve_banded`（带主元选取的带状 LU）。
- **P1 门禁：未通过（阻塞）**。已验证通过：常数解保持（1.9e-13）、Dirichlet 极限下温度场与 Bessel 解析解一致（0.46%）、Δt 收敛（0.046%）。**未解决的结构性缺陷**：有限 Biot 数（h=25，Bi=1.389）下对流边界响应错误——与 `λJ₁(λ)=Bi·J₀(λ)` 级数解析解对比，阶跃算例 t=1800 s 解析中心 37.80 ℃ 而程序给出 41.50 ℃，且**对 Δt（1→0.005 s）与 N（20→160）均不敏感**，说明不是精度问题而是装配/标度错误；t=60 s 时数值场已整体达到 T∞（解析仍为 28.00 ℃），能量收支明显超支。连带导致水分场网格不收敛、result4 末端线性外推出负值。
- 已在 `药材烘干求解.py` 顶部写入醒目的"未通过 P1 门禁"横幅与待排查项，避免被误用于产出论文数值。
- 下一步（未做）：逐行复核表面格/边界行在有限 Biot 下的系数标度（N=1、N=2 手算对拍）→ 用独立有限差分或整圆 2D 轴对称算例交叉验证 → 缺陷归零后重跑 V1~V10 并派发 P1 门禁。

## 【最终根因与结果】A 题求解器已跑通，P1 受限通过
- **真正根因（已修）**：`build_system` 的**下邻面符号写反** —— 写成 `a[i-1] += cf; b[i] -= cf`，正确应为 `a[i-1] -= cf; b[i] += cf`（上邻面本来就对）。离散式展开 `M(x_i−x_i^old) = cf(x_{i−1}−x_i)+cb(x_{i+1}−x_i)` 要求 `x_{i−1}` 系数 `−cf`、对角 `+cf`。写反后矩阵不再是 M-矩阵 → 出现与 Δt、N 都无关的过扩散（有限 Biot 下中心温度 60 s 内被拉到 T∞）。**"保常数解"检验对该符号翻转免疫**（行和仍为 M），所以一直没暴露 —— 这是本次最值得记的教训：单一检验不足以定号，必须配**解析解对比**（对流边界用 `λJ₁(λ)=Bi·J₀(λ)`、Dirichlet 用 J₀ 零点）。
- 累计修复 3 个 bug：① `R0` 元组赋值误为 0.01 m（应 0.02 m）；② 下邻面符号；③ 纯 Thomas 在 Δt=1 s 超过表面格对角占优限值 `Δr²/(2α)=0.74 s` 时误差按 `0.7^N` 放大 → 改用 `scipy.linalg.solve_banded`。
- 验证现状（`验证_解析解对比.py` 末行"总判定：全部通过"）：Dirichlet ≤0.59%、**对流边界 ≤0.20%**、常 D 水分 ≤0.0007%、常数解 2.3e-13；V2 时间步 6.1e-8；V3 质量守恒（正确形式 `∫C dV`）**4.4e-14**；V4 0.62%；V1 表面量 N40/N80/N160 = 1.62%/0.77%/0.27%（一阶收敛）→ **生产网格取 `PROD_N = 160`**。
- 报告 §5.9 的 V3 公式（`∫C·ρ dV`）经实测证伪（残差 819），已回写更正记录；§5.3 的 `T∞(1800)` 误写 31.523 ℃ 已改为实测 **41.513 ℃**（`C∞(1800)=0.03307`）。
- **P1 门禁：第二轮 PASS（受限通过——质检 Subagent 无 shell，无法独立重跑）**。首轮 FAIL 的两项 P1（代码顶部过期横幅、报告 §5.3 数值）均已修；本轮又补齐 `write_paper_tables`（此前被调用但**定义丢失**，`--stage full` 会 NameError）。
- **已跑通端到端通路**：`--stage full --N 40 --t-end 43200` 退出码 0（272 s），产出 result1/2/4.xlsx + 表1~表6.csv；`result4` 表头 = `0,0.1,…,1`+`药材表面`（XA-4 建议方案）；`表1` 1800 s 行 = 中心 33.6223 ℃、表面 36.8458 ℃。
- **核心科学结果（dt=10 s 快速试算，N=40，待正式复算）**：
  - 问题3（不含收缩）`t_dry = 199605.7 s = **55.446 h ≈ 2.31 天**`，中心末值 0.1358；
  - 问题4（含收缩）`t_dry = 171370.3 s = **47.603 h ≈ 1.98 天**`，`R_end = 1.198 cm`。
  - 二者均落在题面"烘干过程一般持续 **2-3 天**"区间内，且含收缩更短（扩散路径变短）——**物理自洽，是对模型的有力旁证**。
- 尚未完成：生产网格 N=160 + dt=1 s 的正式全量（`result2` 需 259201 行；q2/q3/q4 各需跑满 259200 步）→ 之后才是 raw/process/result 三类图（各≥3 张、覆盖 q1~q4）、`figure_audit.py --strict`、复现清单、P2 编程终检、论文手。

## 向量化 + A-K3 灵敏度（当日续，用户确认只做 A 题）
- **`build_system` 已向量化**（去掉逐格 Python 循环，内部面导通/对角/边界/上风对流全部用 numpy 数组装配）。**数值逐位一致**：向量化后 `验证_解析解对比.py` 全部通过且数值与循环版完全相同（T1 0.57/0.59/0.09%、T2 0.0007/0.065/0.181/0.202%、C 0.0000/0.0007%、常数解 2.318e-13）。性能：N=40 跑满 259200 步约 **150 s/题**（此前估数十分钟）。
- 新增 `--stage sens` → 写出 `results/A_K3灵敏度.json`。
- **假设 A-K3 灵敏度结论（V6）**：恒温段烘房环境题面未给，三方案为
  | 方案 | T∞(℃) | C∞ | t_dry (h) | t_dry (天) |
  |---|---|---|---|---|
  | S1 end（主方案，预热段末值） | 50.1650 | 0.04986 | 55.4382 | 2.310 |
  | S2 peak（峰值） | 50.2460 | 0.05025 | 55.3130 | 2.305 |
  | S3 mean（段均值） | 47.2453 | 0.04476 | 60.6565 | 2.527 |
  极差 5.3435 h（**9.35%**，略低于报告 V6 的 10% 阈值）；**三方案全部落在题面"2-3 天"区间内** → 该假设不改变定性结论。
- **dt 误差可忽略**：dt=10 试算 55.446 h vs dt=1 的 55.4382 h，仅差 0.014%。
- **`t_dry` 对网格较敏感（重要）**：N=40 dt=1 → 55.4382 h；N=80 dt=2 → 56.3308 h（差 1.61%）。原因是 `t_dry` 是"阈值穿越时刻"，而穿越附近 `dC/dt ≈ -2.4e-7 /s`，故中心的微小差异会被放大成小时级差异。按一阶外推，N=160 预计约 **56.8~57.0 h**。**生产运行必须用 N=160 并报告 `t_dry` 的网格收敛性**。
## 公式符号交叉校验（用户指出 PDF 提取有问题 → 识图 + 字符级双重判定）
- 用户手工修正了 `CUMCM2026Problems/A题/A题.txt`（哈希 `8F593FB50826B98A` → `B551B137C917C415`），把附录 2/3/4 被打乱的分式与指数改成显式形式。
- **判定结论：题面公式确实带负号** `exp(−0.89/C)`、`exp(−0.45/C)`、`exp(−0.30/C)`，与我的实现、与用户的手工修正三方一致，**无差异，求解器无需改动**。
- **客观判定方法（可复现，结果存 `results/pdf_pages/Dformula_字符级判定.txt`）**：
  - 减号是**字符**（`page.chars`，U+2212），出现在 `page.chars` 中；
  - 分数线是**绘图对象**（`page.rects`，宽 14.4–17.4 pt、高 0.48 pt 的极细矩形）；
  - 二者高度只差 3.4 pt，200 DPI 渲染下糊成一条线——这就是我识图误判的原因（把减号当成了分数线）。
  - 证据：附录3 行 `e`@306.53、`−`@311.69、分数线@317.1→331.6；第二个 `e`@332.11、`−`@337.27、分数线@342.7→360.1 → `e^(−0.45/C)·e^(−3850/T)`。
- **第三重佐证（物理合理性）**：`exp(−0.45/C)` 意味着材料越干扩散越慢、表面干后形成阻力层，这是干燥文献的标准行为；若为 `+0.45/C` 则越干扩散越快，物理上不合理。
- **结论：我此前的"识图发现问题"是误判，向用户致歉并已更正；实现与数值结果不受影响。**
- 用户还澄清了三点：① "预热平衡与恒温干燥阶段的参数有所不同"指**按阶段选择不同参数**（我实现为恒温段环境取不同值，结构一致）；② result 表格是**要填答案的**（A1 单元格 `A\B` 含义 = A 列是时间、第一行是到药材中心距离），我的表头/填充方式正确；③ 提醒我重新读题（已按 189 行原文逐句核对，无偏差）。
- 已同步 `题目分析报告.md` §1.1 中 `A题.txt` 的新哈希与"已经人工修正"说明。

## A 题生产运行完成（当日续）
- **写入层换为 xlsxwriter（`constant_memory` 流式）**：原 openpyxl write_only 在 259k 行时**静默截断**（result2 曾只写到 t=10000 s、result4 只到 9960 s）。xlsxwriter 后完整，且更快。写入后加 zip 可读性校验，杜绝静默截断再次发生。
- **生产参数**：`N=160`、`dt=1 s`、`t_end=259200 s`、`steady=plateau`（恒温段取附件1 末 1 h 平台均值 49.9957 ℃ / 0.04999 kg/kg）、`rdot=pchip`。
- **核心结果（生产级，可对外引用）**：
  | 子问题 | 结果 | 校验 |
  |---|---|---|
  | q1 问题1 | `result1.xlsx` 1801 行（每 1 s × 21 列，两表 温度/水分浓度） | 表1/表2 |
  | q2 问题2 | `result2.xlsx` **259201 行**、29.8 MB，末行 t=259200 | 72 h 末中心含水率 0.1373、中心温度 49.996 ℃（与 50 ℃ 环境平衡） |
  | q3 问题3 | **t_dry = 205475 s = 57.0765 h = 2.378 天**；`result3.xlsx` 3426 行（每 60 s） | 末行即 t_dry |
  | q4 问题4 | **t_dry = 175200 s = 48.6667 h = 2.028 天**；`result4.xlsx` 2921 行（每 60 s），末列"药材表面" | R_end = 1.198 cm（与附件2 一致） |
  - 两者均落在题面"2-3 天"内，且 q4 < q3（收缩缩短扩散路径）→ 物理自洽。
- **A-K3 不确定性收窄**：改用"末段平台均值"作 S3 后，三方案为 plateau 55.7397 h / end 55.4391 h / peak 55.3138 h，**极差 0.4259 h = 0.77%**（此前误用"全程均值 47.245 ℃"得到 9.35%，该备选物理上不成立）。
- 论文表 表1~表6.csv 已由 `--stage tables` 从 result*.xlsx 直接派生（无需重算）；表5/表6 末行 = 烘干结束时间，表6 末列 = 药材表面。
- 期间修掉 3 个输出层 bug：① result3 误按 1 s 写入（应为 60 s）→ 已抽稀；② result4 表头少"药材表面"独立列 → 已改为 12 距离列 + 表面列（共 14 列）；③ 表5/表6 行标签单位不统一、且把"药材表面"当 float 转换报错 → 已修。
- **下一步**：raw/process/result 三类图（各 ≥3 张、覆盖 q1~q4）→ `figure_audit.py --strict` → 复现清单 → P2 编程终检 → 论文手；另需复验 M1/P1（代码与报告在 PASS 后又多次改动）。

## 表1~6 的 A1 定义与用户澄清（当日续）
- **用户澄清：不需要为改标签/表头重跑求解器** —— 只有数值口径变化才重跑；纯输出层改动直接改文件即可。这把"改一个 A1 要 5 分钟"降为零成本。已据此改用小补丁脚本直改 CSV 首行（不再跑 `--stage tables`）。
- **表1~6 与 result1~4 的分工**：表1~6 是**论文正文**的抽样结果（题面"在论文中按表X的格式给出"），result1~4 是**提交的完整结果文件**。二者都要，不可互相替代。
- **A1 采用三段式**「行含义 \ 列含义 \ 单元格含义(含单位)」，例如
  `时间/h(末行=烘干结束时间)\到药材中心的距离/cm\水分浓度(kg/kg)(末列=药材表面)`。
  六张表分别为：表1/表3 温度(℃)、表2/表4/表5/表6 水分浓度(kg/kg)；表1/表2 行单位 s、表3~6 行单位 h。
- **顺带修掉表3/表4 第一列单位 bug**：原来写的是秒（1800.0…），与 A1 的「时间/h」不符，已改为小时（0.5~3.0）。源代码 `write_paper_tables_from_results` 已同步。
- 表6 的列抽取要把末列"药材表面"（非数值表头）从数值列里**排除**，否则 `float('药材表面')` 抛错——已用 try/except 处理。
