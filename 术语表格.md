# 术语表格　CUMCM 2026 A 题：药材的烘干问题

> 本表格由 math-modeling Skill 的**建模手**角色产出，为**建模草稿**，提交前须人工核对。
> 用途：全文术语、符号、单位统一。**"首次出现位置"用于论文写作时回溯定义**。
> 编码规则：`A-K*` 为关键假设、`A-R*` 为可放宽假设（见 `题目分析报告.md` §三）；`XA-*` 为输入异常/待确认项（见该报告 §1.4）；`q1~q4` 对应问题1~4。

## 一、核心物理量与符号

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现位置 | 禁止混用的近义词 |
|---|---|---|---|---|---|---|
| 药材干基含水率 | moisture content (dry basis) | `C` | **药材内部**水分质量与干物质质量之比；初始 2.55，烘干判据 0.15 | kg/kg | A 题问题1 正文 | **"水分浓度"**（题面用词，但附件1 同名量含义不同，见下行）、含水率、湿度、含水比例 |
| 烘房空气含湿量 | drying-chamber air moisture content | `C_∞`（附件中记作"水分浓度"） | **烘房空气**中的水分浓度；实测 0.01963 → 0.04986（峰值 0.05025 @ 2.72 h） | kg/kg | A 附件1 列名 | **严禁与药材含水率 `C` 混用**（量级差约 2 个数量级，见 XA-6） |
| 药材温度 | material temperature | `T` | 药材内部温度场 | **计算用 K**（输出用 ℃） | A 题问题1 | 与烘房温度 `T_∞` 混用 |
| 烘房温度 | drying-chamber temperature | `T_∞`（附件中记作"温度"） | 烘房环境温度；实测 28.000 → 50.165 ℃（峰值 50.246 @ 2.95 h） | ℃（计算前 `+273.15`） | A 附件1 列名 | 与药材温度 `T` 混用；注意**末值≠峰值**（XA-3） |
| 到药材中心的距离 | radial distance from cylinder axis | `r` | 沿径向距圆柱轴心的距离；输出列取 0~2.0（q4 末列为"药材表面"） | **计算用 m**（输出用 cm） | A 题问题1 表1 | **与半径 `R(t)` 混用**（`r` 是坐标，`R` 是域边界） |
| 药材半径（表面位置） | material radius / surface position | `R(t)` | 圆柱外表面到轴心的距离；实测 2.000 → **1.198** cm（67 h 后恒定，XA-5） | cm（计算用 m） | A 附件2 | 与 `r` 混用 |
| 初始温度 | initial temperature | `T_0` | 烘干开始时的药材温度，28 | ℃ | A 题问题1 | — |
| 初始含水率 | initial moisture content | `C_0` | 烘干开始时的药材干基含水率，2.55 | kg/kg | A 题问题1 | 与 `C_∞(0) = 0.01963` 混用 |
| 预热平衡阶段 | preheating / equilibrium stage | — | 烘房升温至趋近恒温的过程；本报告取 `[0, 14400] s` | s | A 题问题1、问题2 | 与"恒温干燥阶段"混用（阶段划分见假设 A-K3） |
| 恒温干燥阶段 | constant-temperature drying stage | — | 预热平衡之后的干燥过程；本报告取 `(14400, 259200] s` | s | A 题问题2 | 同上 |
| 烘干（结束）时间 | drying time / end time | `t_dry` | 各处含水率均 **< 0.15** kg/kg（题面原文"低于"）的最早时刻，即 `min{ t : max_r C(r,t) < 0.15 }` | s（论文表5/表6 用 h） | A 题问题3、问题4 | "烘干时长""干燥周期"（须统一为"烘干时间"）；**不得写作 `≤ 0.15`** |
| 移动边界 | moving boundary | `R(t)` | 因收缩而随时间变化的表面位置 | m | A 题问题4 | "自由边界""Stefan 问题"（机理不同，勿类比） |
| 无量纲径向坐标 | dimensionless radial coordinate | `ξ` | `ξ = r / R(t) ∈ [0,1]`，把移动域映到固定域 | — | A 题问题4 建模 | **与 `r` 混用**（`r` 为物理坐标） |
| 表面收缩速率 | surface shrinkage rate | `Ṙ(t)` | `dR/dt`；实测呈阶梯状（92/144 个区间为 0），需平滑处理 | m/s | A 题问题4 建模 | "收缩率"（后者常指体积/尺寸比，勿混） |

## 二、物性与传递系数

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现位置 | 禁止混用的近义词 |
|---|---|---|---|---|---|---|
| 密度 | density | `ρ(C)` | 药材密度；问题1 常数 820，问题2/3 `650+128C`，问题4 `760+90C` | kg/m³ | A 附录2/3/4 | — |
| 比热容 | specific heat capacity | `c_p(C)` | 定压比热；问题1 常数 2600，问题2/3 `1450+2736C/(C+1)`，问题4 `1850+2150C/(C+1)` | J/(kg·K) | A 附录2/3/4 | "热容"（后者含质量/体积因子） |
| 热传导系数 | thermal conductivity | `k(C)` | 导热系数；问题1 常数 0.36，问题2/3 `0.21+0.38C/(C+1)`，问题4 `0.12+0.20C/(C+1)` | W/(m·K) | A 附录2/3/4 | **"热扩散率"** `α = k/(ρc_p)`（单位 m²/s，勿混） |
| 水分扩散系数 | moisture diffusion coefficient | `D(C,T)` | 水分的有效扩散系数；问题1 `7e-9·exp(−0.89/C)`，问题2/3 `2.4e-3·exp(−0.45/C)·exp(−3850/T)`，问题4 `4.2e-4·exp(−0.30/C)·exp(−3850/T)` | m²/s | A 附录2/3/4 | **与传质系数 `h_m` 混用**（前者材料内部、后者表面）；**`T` 必须用 K** |
| 对流换热系数 | convective heat transfer coefficient | `h` | 表面与烘房空气的换热系数，25 | W/(m²·K) | A 附录2 | **与 `h_m` 混用**（换热 vs 传质） |
| 对流传质系数 | convective mass transfer coefficient | `h_m` | 表面与烘房空气的传质系数，`8×10⁻⁷` | m/s | A 附录2 | 与 `h`、`D` 混用 |
| 热扩散率 | thermal diffusivity | `α` | `α = k/(ρ·c_p)`；问题1 约 `1.69e-7`（用于显式格式稳定性判据） | m²/s | A 题数值方案（§5.7） | 与 `D` 混用（热 vs 湿） |
| 传热毕渥数 | Biot number (heat) | `Bi = h·R/k` | 表征内部导热与表面换热的相对强度 | — | A 题分析（可选） | 与传质毕渥数混用 |
| 传质毕渥数 | Biot number (mass) | `Bi_m = h_m·R/D` | 表征内部扩散与表面传质的相对强度 | — | A 题分析（可选） | 与 `Bi` 混用 |

## 三、数值与计算术语

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现位置 | 禁止混用的近义词 |
|---|---|---|---|---|---|---|
| 空间网格数 | number of radial nodes | `N` | 径向节点数；基准 40，收敛验证取 20/40/80/160 | — | A 题数值方案 | 与"单元格数""输出列数"混用 |
| 空间步长 | radial step | `Δr` | `Δr = R/N`；基准 `5e-4 m`（0.05 cm） | m | A 题数值方案 | 与"输出距离间隔 0.1 cm"混用（二者不同：0.05 vs 0.1 cm） |
| 时间步长 | time step | `Δt` | 计算步长，取 1 | s | A 题数值方案 | 与"输出间隔"混用（q3/q4 输出每 60 s，计算仍每 1 s） |
| 节点中心半径 | cell-center radius | `r_i` | `r_i = (i−1/2)·Δr`（有限体积格心） | m | A 题数值方案 | 与 `r`（任意物理坐标）混用 |
| 有限体积法 | finite volume method | FVM | 以控制体守恒形式离散，保证离散守恒 | — | A 题数值方案 | 与"有限差分法"混用（本报告用 FVM） |
| 隐式欧拉 | implicit (backward) Euler | — | 一阶时间离散，无条件稳定 | — | A 题数值方案 | 与"显式欧拉"混用（显式不满足稳定条件，见 §5.7） |
| 三对角矩阵 / Thomas 算法 | tridiagonal system / Thomas algorithm | — | 隐式格式产生的线性系统及其 `O(N)` 解法 | — | A 题数值方案 | "追赶法"（同义，须统一） |
| Picard 迭代 | Picard iteration | `m` | 每时间步内处理非线性物性的外层不动点迭代 | — | A 题数值方案 | 与"Newton 迭代"混用（后者需 Jacobian） |
| 收敛判据 | convergence tolerance | `tol` | Picard 迭代的绝对收敛阈值 `1e-10`（℃、kg/kg） | — | A 题数值方案 | 与网格/时间步收敛阈值（相对 0.5%）混用 |
| 网格无关性 | grid independence | V1 | 加密网格后结果不再显著变化 | — | A 题验证方案 §5.9 | "网格收敛性"（同义） |
| 守恒审计 | conservation audit | V3 | 对水分总量与焓做收支核对 | — | A 题验证方案 | 与"质量平衡"混用（前者含数值残差核查） |
| 事件定位 | event location | — | 求 `C(0,t) − 0.15` 首次变号的时刻 | — | A 题问题3 算法 | 与"根求解"混用（同义，论文可择一） |

## 四、输出与结果文件术语

| 中文术语 | 英文术语 | 符号 | 定义 | 单位 | 首次出现位置 | 禁止混用的近义词 |
|---|---|---|---|---|---|---|
| 结果文件 | result file | `result1~4.xlsx` | 官方模板要求的结果文件 | — | A 附录1 | 与论文"表1~表6"混用（前者是 xlsx，后者是论文表格） |
| 工作表 | worksheet | `温度` / `水分浓度` / `Sheet1` | result1/2 用 `温度`+`水分浓度`；result3/4 用 `Sheet1` | — | A 附录1、附件3 | **不得自行改名** |
| 固定距离列 | fixed radial columns | `0, 0.1, …, 2.0` | 结果表的距离列，共 **21 列**（每 0.1 cm） | cm | A 题问题1 | 与"节点中心 `r_i`"混用（列位置需插值得到） |
| 药材表面列 | surface column | `药材表面` | `result4.xlsx` 末列，填入该时刻 `r = R(t)` 处的结果 | cm | A 附件3 `result4` 表头 | **与固定列 `2.0` 混用**（表面位置随时间变化，见 XA-4） |
| 保留四位小数 | round to 4 decimals | — | 题面要求"所有结果保留四位小数" | — | A 题问题1 末句 | 不得只对部分表四舍五入 |
| 完整结果 | complete results | — | 指按题面给定步长（1 s 或 60 s）与 0.1 cm 间隔的全网格输出 | — | A 题问题1/2/3/4 | 与"论文表格结果"混用（后者是抽样） |

## 五、符号冲突与易混清单（A 题内部）

| 冲突对 | 说明 | 处理约定 |
|---|---|---|
| `C` vs `C_∞` | 药材含水率 vs 烘房含湿量 | 药材一律写 `C`，烘房一律写 `C_∞`（**附件1 的列名"水分浓度"指后者**） |
| `T` vs `T_∞` | 药材温度 vs 烘房温度 | 药材写 `T`，烘房写 `T_∞`（附件1 的列名"温度"指后者） |
| `r` vs `R(t)` | 径向坐标 vs 表面位置 | `r` 为坐标（可变取值），`R(t)` 为域边界 |
| `r` vs `ξ` | 物理坐标 vs 无量纲坐标 | q4 中先写 `ξ = r/R(t)`，此后公式明确标注自变量 |
| `k` vs `α` | 热传导系数 vs 热扩散率 | `k` 单位 W/(m·K)，`α` 单位 m²/s，不得互相替代 |
| `D` vs `h_m` | 内部扩散系数 vs 表面传质系数 | `D` m²/s（材料内部），`h_m` m/s（表面） |
| `Δr` vs 输出间隔 0.1 cm | 计算步长 0.05 cm vs 输出间隔 0.1 cm | **两者不同**：输出列由节点值插值得到，论文中须说明 |
| 附件1 的"温度" vs 药材温度 | 列名相同但含义不同 | 引用附件1 时一律写 `T_∞(t)`，并在图注中标注"烘房温度" |
| `t_dry`（s） vs 表5/表6（h） | 内部计算用 s，论文表用 h | 换算关系 `h = s/3600`，论文中给出换算说明 |

## 六、单位与温标约定（统一口径）

| 量 | 附件/题面单位 | 计算单位 | 输出单位 | 换算与注意事项 |
|---|---|---|---|---|
| 长度 | cm | m | cm | `×1e-2`；**半径 2.0 cm = 0.02 m** |
| 温度 | ℃ | **K** | ℃ | `K = ℃ + 273.15`；**`D` 的 `exp(−3850/T)` 必须用 K** |
| 时间 | s（论文表用 h） | s | s（论文表 h） | `h = s/3600` |
| 含水率 / 含湿量 | kg/kg | kg/kg | kg/kg | 无量纲，保留 4 位小数时注意 `0.1500` 的写法 |
| `D` | m²/s | m²/s | — | 量级应为 `1e-11 ~ 6e-9`，出现其他量级即为错误 |
| `ρ` | kg/m³ | 同 | — | **650~990 量级**（问题1 为常数 820；问题2/3 `650+128C`，`C∈[0,2.55]` → 650~976；问题4 `760+90C` → 760~990） |
| `c_p` | J/(kg·K) | 同 | — | 1400~4700 量级 |
| `k` | W/(m·K) | 同 | — | 0.1~0.6 量级 |
| `h` | W/(m²·K) | 同 | — | 25（问题2~4 沿用，见报告 §5.2 待确认项） |
| `h_m` | m/s | 同 | — | `8e-7`（同上） |

## 七、假设与待确认项索引

| 编号 | 一句话内容 | 详见 |
|---|---|---|
| A-K1 | 一维径向均质圆柱，端面效应可忽略 | `题目分析报告.md` §三 |
| A-K2 | Fick 扩散 + 对流传质边界，`D` 依赖 `C`（及 `T`） | 同上 |
| A-K3 | 预热段 `[0,14400] s`；恒温段取预热段末值恒定（`T_∞=50.165 ℃`、`C_∞=0.04986`） | 同上（含 S1/S2/S3 灵敏度） |
| A-R1 ~ A-R6 | 环境均匀、初始均匀、忽略相变与辐射、`R(t)` 直接给定、按比例收缩、线性驱动 | 同上 |
| XA-1 | `result1≡result2` 且均为缩略示意，需自建完整网格 | §1.4 |
| XA-2 | `result1/2` 有 `温度`+`水分浓度` 两表；`result3/4` 只有 `Sheet1` | §1.4 |
| XA-3 | 附件1 温度与含湿量**非单调**，末值 ≠ 峰值 | §1.4、§2.1 |
| XA-4 | `result4` 末列为"药材表面"，列定义**待确认** | §1.4、§5.6 |
| XA-5 | 附件2 半径阶梯收缩，67 h 后恒为 1.198 cm | §1.4、§2.2 |
| XA-6 | 药材含水率 2.55 与附件1 含湿量 0.02~0.05 含义不同 | §1.4、§二 |
| XA-7 | `result2` 输出时间上界未明（读法① 259201 行 / 读法② 10801 行） | §1.4、§2.4 |
