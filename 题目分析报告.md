# 题目分析报告　CUMCM 2026 A 题：药材的烘干问题

> 本报告由 math-modeling Skill 的**建模手**角色产出，仅为**建模草稿**，不得原样作为参赛作品提交。
> 所有模型选择、公式、参数与假设在提交前必须由参赛队伍人工核对、改写。
> **本版说明**：题目已定为 **A 题**，故本报告由"五题并列选题分析"收敛为 **A 题编程级规格**（可直接作为编程手输入）。选题阶段的五题对比结论保留在 `CUMCM2026Problems/五题选题评估报告.md`，不再在本报告中重复。

## 〇、任务与核验状态

| 项目 | 内容 |
|---|---|
| 目标竞赛 / 届次 | 全国大学生数学建模竞赛（CUMCM）2026 年（高教社杯） |
| 已定题目 | **A 题　药材的烘干问题** |
| 工作阶段 | 建模手（本报告）→ 编程手（待启动）→ 论文手 |
| `SKILL_ROOT` | `d:\Code\2026BJCUMCM\.codebuddy\skills\math-modeling-skill`（只读） |
| `PROJECT_ROOT` | `d:\Code\2026BJCUMCM` |
| 输入目录 | `d:\Code\2026BJCUMCM\CUMCM2026Problems\A题`（只读） |
| 建模手固定产物 | 本文件 + `术语表格.md`（仅此两件） |
| 选题决策记录 | 选择 A 题依据加权评分（可实现性 5 / 证据完备 5 / 验证强度 5 / 论文可写 4 / 时间风险 3 → 加权 4.25，五题第 2），且 A 题为唯一需自建数值格式、机理最清晰的题 |

**待核验的官方规则（本报告不据此下结论）**

- [ ] CUMCM 2026 当届论文格式规范（`format2026.doc` 未解析；当届规则须以官方最新发布为准）
- [ ] 摘要字数、正文页数上限、附录与支撑材料要求
- [ ] `result1~4.xlsx` 是否必须严格套用附件3 模板的文件名与**工作表名**
- [ ] 结果文件是否有单元格数/文件大小上限（见 §2.4 输出规模核算）
- [ ] 问题4 的 `result4.xlsx` 列定义（模板末列为"药材表面"，见 §1.4 XA-4）

---

## 一、输入清单与快照

### 1.1 赛题原文

| 文件 | 页 | SHA-256(16) |
|---|---|---|
| `A题/A题.pdf` | 4 | `052D8014BFF5727C` |
| `A题/A题.txt`（文本提取件，**已经人工修正**：把附录 2/3/4 中被 PDF 提取打乱的分式与指数改写为显式形式，如 `D=2.4×10⁻³·e^(−0.45/C)·e^(−3850/T)`。仅用于检索与引用，不参与计算；**权威来源是 `A题.pdf` 原件**） | — | `B551B137C917C415` |

### 1.2 数据附件

| 文件 | 实测结构 | SHA-256(16) |
|---|---|---|
| `A题/附件/附件1.xlsx` | `Sheet1`，241 行 × 3 列（`时间`/`温度`/`水分浓度`），Δt = 60 s，t ∈ [0, 14400] s | `7EF32870ABEEF420` |
| `A题/附件/附件2.xlsx` | `Sheet1`，145 行 × 2 列（`时间`/`半径`），Δt = 1800 s，t ∈ [0, 259200] s | `5563ACBFA4B4AFB1` |

### 1.3 结果模板（`A题/附件/附件3/`）

| 文件 | 工作表 | 模板规模（仅为示意图，须自行扩展） | SHA-256(16) |
|---|---|---|---|
| `result1.xlsx` | `温度`、`水分浓度` | 5 行 × 6 列 | `23B261B295C1B787` |
| `result2.xlsx` | `温度`、`水分浓度` | 5 行 × 6 列 | `23B261B295C1B787` |
| `result3.xlsx` | `Sheet1` | 5 行 × 6 列 | `07E4793D620A7F89` |
| `result4.xlsx` | `Sheet1` | 5 行 × 6 列 | `86E9300FFA3D30C4` |

模板表头实测：

| 文件 | 表头（列顺序） |
|---|---|
| `result1/2`（两张工作表均同） | `时间\到药材中心的距离`、`0`、`0.1`、`0.2`、`…`、`2` |
| `result3` | `时间\到药材中心的距离`、`0`、`0.1`、`0.2`、`…`、`2` |
| `result4` | `时间\到药材中心的距离`、`0`、`0.1`、`0.2`、`…`、**`药材表面`** |

### 1.4 输入异常与待确认项

| 编号 | 现象（实测证据） | 影响 | 处理 |
|---|---|---|---|
| XA-1 | `result1.xlsx` 与 `result2.xlsx` **字节完全相同**（哈希均 `23B261B295C1B787`），且两者均为 5 行 × 6 列的**缩略示意**（表头 `0 / 0.1 / 0.2 / … / 2`） | 两问输出时间跨度差别极大（1800 s vs 259200 s），却共用同一模板，易误把模板当完整网格 | 模板**只是示意**：须自行生成完整网格（距离 0~2.0 cm 每 0.1 cm 共 **21 列**）；时间行数按 §5.8 逐问确定 |
| XA-2 | `result1/2` 有 `温度` + `水分浓度` 两张工作表；`result3/4` 只有一张 `Sheet1` | 工作表名与数量影响判分 | 严格照模板：问题1/2 输出两表（温度、水分浓度），问题3/4 只输出水分浓度 |
| XA-3 | 附件1 的 `温度` 与 `水分浓度` **均非单调**（温度 95/240 处下降，含湿量 80/240 处下降），且在 14400 s 末仍未平稳（末值 50.165 ℃ / 0.04986 kg/kg） | 直接线性插值可行；但"预热平衡阶段何时结束"无法从数据直接判定 | 线性插值用于边界条件；阶段划分列为假设 A-K3，并做灵敏度（见 §5.4） |
| XA-4 | `result4.xlsx` 表头末列为 `药材表面`，而 `result3.xlsx` 末列为 `2` | 问题4 因药材收缩，表面位置随时间变化（2.0 → 1.198 cm），无法用固定 r 作为末列标签 | **待确认项**。本报告建议：结果4 的固定 r 列仍取 0~1.1 cm（覆盖 R(t) 的最小值 1.198 cm 以内的 0.1 倍数），并在**末列 `药材表面`** 依次填入各时刻 **r = R(t) 处**的结果；同时**必须**在论文中说明该列语义。若官方另有规定以官方为准 |
| XA-5 | 附件2 的半径在 t ≈ 22.5~67 h 间呈**阶梯状下降**（92 个 1800 s 区间增量为 0），67 h 后恒为 1.198 cm | 移动边界速度 Ṙ(t) 大部分时段为 0、少数时点跳变 | 对 R(t) 用**分段线性插值**；Ṙ 在阶梯段取 0、在跳变段取阶跃斜率；并对 R(t) 做单调平滑拟合作为对照（见 §5.9 验证） |
| XA-6 | 题面"水量浓度（即干基含水率）为 2.55 kg/kg"指**药材含水率**；附件1 的"水分浓度"取值 0.01963~0.05025 kg/kg，是**烘房空气含湿量** | 二者物理含义不同、量级差约 2 个数量级 | 全文严格区分：药材记 `C`、烘房记 `C_∞`；**严禁混用**（见 `术语表格.md` §一与 §六） |
| XA-7 | 问题2 的 xlsx 输出**未给时间上界**（题面仅"将每隔 1 s、到药材中心距离每隔 0.1 cm 的完整结果保存到 result2.xlsx"），而问题1 明写"**1800 s 内**" | result2 的行数存在两种读法 | **待确认项**。附件2 覆盖 0~259200 s（72 h）且题面称"烘干过程一般持续 2-3 天"，故本报告按 **读法①：全程 0~259200 s @1 s = 259201 行**（行数 < 1,048,576，可写入 xlsx）；读法②（仅 3 h = 10801 行）作为回退。须与官方规则/编程手确认后写入模型合同 |

---

## 二、数据理解与预处理计划

### 2.1 附件1（烘房环境，边界条件来源）

| 统计量 | 温度 `T_∞`（℃） | 含湿量 `C_∞`（kg/kg） |
|---|---|---|
| 首值（t = 0） | 28.0000 | 0.01963 |
| 末值（t = 14400 s = 4 h） | **50.1650** | **0.04986** |
| 最大值 | 50.2460 @ t = 10620 s（2.95 h） | 0.05025 @ t = 9780 s（2.72 h） |
| 最小值 | 28.0000 @ t = 0 | 0.01963 @ t = 0 |
| 单调性 | **非单调**（95/240 处下降，最大单步 +0.797、最小单步 −0.450） | **非单调**（80/240 处下降） |
| 采样 | 241 点，Δt = 60 s，无缺失、无平坦段 | 同左 |

> **重要更正**：早先版本把末值误记为 50.25 ℃ / 0.05025 kg/kg —— 那是**峰值**而非末值。本表为实测值，后续编程与论文一律以本表为准。

### 2.2 附件2（药材半径，移动边界来源）

| 统计量 | 半径 `R(t)`（cm） |
|---|---|
| 首值（t = 0） | 2.0000 |
| 末值（t = 259200 s = 72 h） | **1.1980** |
| 单调性 | **单调不增**（`max ΔR = 0`，`min ΔR = −0.127` cm） |
| 零增量区间 | **92 / 144** 个区间增量为 0（阶梯状）；首次零增量出现在第 44→45 个区间，最后一次非零变化截至 t = 241200 s（67 h） |
| 收缩比 | `R(72h)/R(0) = 0.599`，对应截面积比 `0.359` |
| 抽样（每 6 h） | 0 h: 2.000，6 h: 1.374，12 h: 1.248，18 h: 1.214，24 h: 1.204，30 h: 1.201，36 h: 1.200，…，67 h 后: 1.198 |
| 采样 | 145 点，Δt = 1800 s，无缺失 |

> **重要更正**：早先版本把末值误记为 1.873 cm —— 那是 **t = 1800 s（第 2 个采样点）** 的值。**正确末值为 1.198 cm**，且 67 h 后不再收缩。该更正直接影响问题4 的移动边界范围与 `result4.xlsx` 的列定义（XA-4）。

### 2.3 量纲与单位核对（机理题必做）

| 物理量 | 题目给的单位 | 计算采用 | 换算 |
|---|---|---|---|
| 长度 `r, R` | cm | **m** | ×1e-2 |
| 温度 `T, T_∞` | ℃ | **K** | `+273.15`（`D` 的 Arrhenius 因子必须用 K） |
| 时间 `t` | s | s | — |
| 含水率 `C, C_∞` | kg/kg | kg/kg（无量纲） | — |
| `ρ` | kg/m³ | 同 | — |
| `c_p` | J/(kg·K) | 同 | — |
| `k` | W/(m·K) | 同 | — |
| `D` | m²/s | 同 | — |
| `h` | W/(m²·K) | 同 | — |
| `h_m` | m/s | 同 | — |

**已核验的量级合理性**（用于确认公式转录无误）：

- 问题1：`D = 7e-9·exp(−0.89/C)` → `C = 2.55` 时 `D = 7e-9×exp(−0.34902) ≈ 4.937e-9` m²/s；`C = 0.15` 时 `D = 7e-9×exp(−5.93333) ≈ 1.855e-11` m²/s。（取整后分别记 `4.94e-9` 与 `1.85e-11`）
- 问题2/3：`D = 2.4e-3·exp(−0.45/C)·exp(−3850/T)` → `(C, T) = (2.55, 301.15 K)` 时 `D ≈ 5.6e-9` m²/s；`(0.15, 350 K)` 时 `D ≈ 2.0e-9` m²/s。
- 问题4：`D = 4.2e-4·exp(−0.30/C)·exp(−3850/T)`（同一温度因子）。
- 结论：`D` 落在 `1e-11 ~ 6e-9 m²/s` 区间，与食品/药材干燥的有效水分扩散系数典型量级一致，**公式转录无误、无需额外上界约束**。**注意：若漏乘 `exp(−3850/T)`（或把 T 用 ℃ 代入）将得到荒谬数值，编程时必须自检。**

### 2.4 输出规模核算（写入 xlsx 前必须确认）

| 文件 | 工作表 | 数据行数 | 数据列数 | 单元格数 |
|---|---|---|---|---|
| `result1.xlsx` | 温度 + 水分浓度 | 1801（0~1800 s @1 s） | 21（0~2.0 cm @0.1 cm） | 2 × 37821 ≈ 7.6 万 |
| `result2.xlsx` | 温度 + 水分浓度 | **259201**（0~259200 s @1 s，读法①）或 10801（读法②） | 21 | 2 × 5.44e6 ≈ **1089 万**（读法①） |
| `result3.xlsx` | Sheet1 | ≤ 4321（0~259200 s @60 s） | 21 | ≤ 9.1 万 |
| `result4.xlsx` | Sheet1 | ≤ 4321（@60 s） | 21（含末列"药材表面"，见 XA-4） | ≤ 9.1 万 |

**结论**：`result2` 在读法①下需写入约 **1089 万个单元格**，必须用 `openpyxl` 的 **write_only 模式**（`Workbook(write_only=True)`）或先写 CSV 再转换，**不得**用 `pandas.to_excel`（内存与耗时不可接受）。若官方有文件大小上限或写入失败，回退读法②并与建模手确认（对应 §七 风险表）。

### 2.5 预处理计划

1. **边界插值**：`T_∞(t)`、`C_∞(t)` 由附件1 线性插值（`np.interp`），定义域 `[0, 14400] s`；超出范围时按 §5.4 的分段规则取值。
2. **半径插值**：`R(t)` 由附件2 线性插值（`np.interp`），定义域 `[0, 259200] s`；并另做**单调平滑版本**（如 PCHIP 或单调回归）作为对照。
3. **经验公式封装**：把三组 `(ρ, c_p, k, D)` 封装为接受 `(C, T_K)` 返回系数的函数，**统一以 K 入参**，避免温标混用。
4. **不插值、不外推**：任何超出附件定义域的取值必须显式声明规则，不静默外推。

---

## 三、假设分级

> 计数口径（Skill 规定"关键假设通常 ≤ 3 条，全部可放宽假设合计 ≤ 7 条"，按**单题**计数）：本题 **关键 3 条 + 可放宽 6 条**。

### 关键假设（放宽会使模型失效）

| 编号 | 假设 | 依据 | 若放宽会怎样 |
|---|---|---|---|
| **A-K1** | 药材为各向同性均质圆柱，传热传质仅沿**径向一维**进行（轴向与周向可忽略）；长度 25 cm 远大于半径 2 cm，端面效应可忽略 | 题面"形状大致呈圆柱形"，输出表只给"到药材中心距离"这一维坐标 | 一维方程与全部输出列的语义同时失效，须改 2D 轴对称模型 |
| **A-K2** | 水分迁移为 Fick 扩散，`D` 仅依赖 `C`（问题1）或 `C` 与 `T`（问题2~4，由附录3/4 给定）；表面为对流传质边界 | 题面附录2/3/4 明确给出 `D` 与 `h_m` | 需引入毛细/对流项，超出题给信息 |
| **A-K3** | 阶段划分：**预热平衡阶段 = [0, 14400] s**（附件1 覆盖区间），**恒温干燥阶段 = (14400, 259200] s**；恒温阶段烘房环境取预热段**末值恒定**，即 `T_∞ = 50.165 ℃`、`C_∞ = 0.04986 kg/kg` | 题面"烘干过程一般持续 2-3 天，预热平衡与恒温干燥阶段的参数有所不同"，但**未给恒温阶段的烘房数值**；附件1 只覆盖"烘干初期"（附录1 原文）至 14400 s | 恒温段边界条件改变会系统性改变干燥速率与总时长；须做灵敏度（取峰值、取段均值、取末值三方案对比，见 §5.9） |

### 可放宽假设

| 编号 | 假设 | 依据 | 放宽后的复验方式 |
|---|---|---|---|
| A-R1 | 烘房环境均匀，温湿度只随时间变化、与空间位置无关 | 附件1 仅给单一温湿度序列 | 不可进一步放宽（无空间数据），论文中声明 |
| A-R2 | 初始温度与含水率在截面上均匀 | 题面只给单一初值 28 ℃ / 2.55 kg/kg | 改用非均匀初值（如表面略干）做对照 |
| A-R3 | 忽略相变潜热与热辐射 | 全时段温度 28~50 ℃，无相变 | 加入辐射项对照 |
| A-R4 | 药材半径变化**直接由附件2 给出**，不由含水率反算 | 题面"请根据附件2…确定烘干时长"，附录4 未给半径-含水率关系式 | 用 `R(C)` 单调关系反算并与附件2 对比 |
| A-R5 | 收缩过程中材料沿半径**按比例**收缩（同一 ξ 面上的物质点始终对应） | 移动边界用 `ξ = r/R(t)` 坐标变换的必要前提 | 改为"外层先固结、内层后收缩"的非均匀映射做对照 |
| A-R6 | 烘房含湿量与药材表面含水率之间的平衡关系为线性（即 `C_surface` 与 `C_∞` 线性驱动通量） | 题面给的是对流传质系数 `h_m` 线性驱动形式 | 改用非线性等温线（如 GAB）对照 |

---

## 四、模型合同（前置合同）

| 子问题 | 任务定义 | 数据 | 结论类型 | 模型（≤2 独立体系） | 实现 | 验证 | 风险 |
|---|---|---|---|---|---|---|---|
| **q1**（问题1） | 建立并求解预热平衡阶段 `T(r,t)`、`C(r,t)`，t ∈ [0,1800] s | 附件1（插值边界）、附录2 常数物性 | 双场时空分布（表1/表2 + `result1.xlsx`） | **M-A**（一维圆柱热-湿耦合扩散模型族·基础版，常物性） | Python/NumPy + 带主元选取的带状 LU（`scipy.linalg.solve_banded`，见 §5.9 更正记录③；无主元的 Thomas 不可用） | 网格无关性、时间步收敛、守恒审计、常系数解析解对比 | 非线性 `D(C)` 迭代收敛；模板网格需自建 |
| **q2**（问题2） | 建立并求解**整个烘干过程**（0~259200 s）的 `T`、`C` 场 | 附件1、附录3 变物性 | 双场时空分布（表3/表4 + `result2.xlsx`） | **M-A 扩展版**（变物性 + 分阶段边界）＋ **M-B** Page/Newton 经验干燥动力学（并行对比） | 同上 | 与 M-B 的干燥曲线对比、能量守恒、阶段边界灵敏度 | 输出 1089 万单元格（XA-7）；恒温段边界为假设 A-K3 |
| **q3**（问题3） | 求"各处 `C < 0.15 kg/kg`"所需时间（h） | 复用 q2 | 单一时间 + 过程表（表5 + `result3.xlsx`） | 复用 **M-A 扩展版**，外层事件定位（粗扫描 + 二分细化） | 同上 | 时间步长收敛性、判据位置的明确性 | 判据取**中心点**达标（中心最难干燥） |
| **q4**（问题4） | 含尺寸变化的烘干时长 | 附件2 半径、附录4 物性 | 单一时间 + 过程表（表6 + `result4.xlsx`） | **M-A 移动边界版**（`ξ = r/R(t)` 坐标变换，同一模型族）＋ 固定域等效迭代（对照） | 同上 | 两种处理的偏差、`R(t)` 插值敏感性 | 移动边界数值稳定性；`result4` 列定义（XA-4） |

**模型计数复核**：q1/q2/q4 的 M-A 基础版、扩展版、移动边界版共享**同一控制机理（热-湿耦合扩散）与同一状态变量组 `(T, C)`**，仅物性、边界条件与求解域不同，按 Skill 规则计为**一个模型族**；M-B（经验干燥动力学）为独立第二体系。故 q2 为 2 个体系、q1/q3/q4 均 ≤ 2，**全部合规**。

---

## 五、模型与数学定义（编程级）

### 5.1 控制方程

一维圆柱（`r ∈ [0, R]`）：

```
ρ(C)·c_p(C)·∂T/∂t = (1/r)·∂/∂r[ k(C)·r·∂T/∂r ]          (A-1)
∂C/∂t             = (1/r)·∂/∂r[ D(C,T)·r·∂C/∂r ]         (A-2)
```

边界条件：

```
r = R:   -k·∂T/∂r = h·( T - T_∞(t) )        （第三类，对流换热）
         -D·∂C/∂r = h_m·( C - C_∞(t) )      （第三类，对流传质）
r = 0:   ∂T/∂r = 0,  ∂C/∂r = 0              （对称条件）
```

初始条件：`T(r,0) = T_0 = 28 ℃ = 301.15 K`，`C(r,0) = C_0 = 2.55 kg/kg`，`0 ≤ r ≤ R(0) = 2 cm`。

> **几何说明**：题面给出药材**长度 25 cm**，但控制方程与输出均为径向一维，故长度仅用于说明"端面效应可忽略"（假设 A-K1），**不进入计算**。

### 5.2 三组物性与参数

| 量 | 问题1（附录2） | 问题2、3（附录3） | 问题4（附录4） |
|---|---|---|---|
| `ρ`（kg/m³） | 820（常数） | `650 + 128·C` | `760 + 90·C` |
| `c_p`（J/(kg·K)） | 2600（常数） | `1450 + 2736·C/(C+1)` | `1850 + 2150·C/(C+1)` |
| `k`（W/(m·K)） | 0.36（常数） | `0.21 + 0.38·C/(C+1)` | `0.12 + 0.20·C/(C+1)` |
| `D`（m²/s） | `7e-9·exp(−0.89/C)` | `2.4e-3·exp(−0.45/C)·exp(−3850/T)` | `4.2e-4·exp(−0.30/C)·exp(−3850/T)` |
| `h`（W/(m²·K)） | 25 | 25（题面未另给，沿用附录2） | 25（同上） |
| `h_m`（m/s） | `8e-7` | `8e-7`（同上） | `8e-7`（同上） |

> **待确认**：题面仅给出问题1 的 `h` 与 `h_m`（附录2），问题2~4 未重复给出。本报告按"沿用附录2 的 `h = 25 W/(m²·K)`、`h_m = 8e-7 m/s`"处理，并列入灵敏度分析（`h` 与 `h_m` 各 ±50%）。

> **温度单位**：`D` 中的 `exp(−3850/T)` 要求 `T` 以 **K** 代入；`T_∞` 由附件1 读入为 ℃，进入公式前必须 `+273.15`。

### 5.3 q1（问题1）定解条件与输出

- 时域：`t ∈ [0, 1800] s`，时间步 `Δt = 1 s`（与输出粒度一致）。
- 边界：`T_∞(t)`、`C_∞(t)` 由附件1 在 `[0, 1800] s` 内线性插值。**实测端点值**（附件1 第 1 行与第 31 行）：`T_∞(0) = 28.000 ℃` → `T_∞(1800) = 41.513 ℃`；`C_∞(0) = 0.01963` → `C_∞(1800) = 0.03307 kg/kg`。
  > **更正记录（2026-09-10，由 P1 质检发现）**：本行前一版本把 `T_∞(1800)` 误写为 `31.523 ℃`、`C_∞(1800)` 误写为 `0.02396`，与附件1 实测不符（`[0,1800] s` 内有 24 个采样点的温度已超过 31.523 ℃，该值不可能是区间末值）。**以本行实测值为准。**
- 物性：附录2 常数。
- 输出：
  - 论文**表1**（温度）与**表2**（水分浓度）：`t = 100, 300, 600, 900, 1200, 1500, 1800 s` × `r = 0, 0.5, 1, 1.5, 2 cm`，保留 4 位小数。
  - `result1.xlsx`：工作表 `温度` 与 `水分浓度`；A 列时间 `0, 1, …, 1800`（1801 行）；第 1 行距离 `0, 0.1, …, 2.0`（21 列）；4 位小数。

### 5.4 q2（问题2）全流程与阶段边界

- 时域：`t ∈ [0, 259200] s`（72 h，由附件2 的覆盖范围确定，与"2-3 天"一致），`Δt = 1 s`。
- 物性：附录3 变物性（`ρ, c_p, k` 随 `C`；`D` 随 `C` 与 `T`）。
- 分段边界条件（对应假设 A-K3）：

```
预热平衡阶段  t ∈ [0, 14400] s :   T_∞(t), C_∞(t)  ←  附件1 线性插值
恒温干燥阶段  t ∈ (14400, 259200] s :  T_∞ = 50.165 ℃  ,  C_∞ = 0.04986 kg/kg  （取预热段末值，恒定）
```

- **灵敏度方案**（须在论文中给出对比）：恒温段环境取
  - S1（主方案，末值）：`T_∞ = 50.165 ℃`、`C_∞ = 0.04986 kg/kg`
  - S2（峰值）：`T_∞ = 50.246 ℃`、`C_∞ = 0.05025 kg/kg`
  - S3（段均值）：`T_∞ = \bar{T}_∞`、`C_∞ = \bar{C}_∞`（对附件1 全体 241 点求均值）
  比较三方案下的中心点干燥曲线与问题3 的烘干时长差异。
- 输出：
  - 论文**表3**（温度）与**表4**（水分浓度）：`t = 0.5, 1.0, …, 3.0 h` × `r = 0, 0.5, 1, 1.5, 2 cm`。
  - `result2.xlsx`：同 `result1` 的表结构；A 列时间 `0, 1, …, 259200`（259201 行，读法①，见 XA-7）；21 列。

### 5.5 q3（问题3）终止时间

判据（题面）："药材**各处**的水分浓度应低于 0.15 kg/kg" → 取

```
t_dry = min { t : max_{r ∈ [0, R]} C(r, t) < 0.15 }        (A-3)
```

> **严格不等号的实现说明**：题面原文为"药材各处的水分浓度应**低于** 0.15 kg/kg"，故判据取**严格小于**。数值实现时用 `C(0,t) − 0.15 < 0` 作事件判定；因输出保留 4 位小数（`0.1500`），若结果恰落在 0.15 的 1e-4 邻域内，须在论文中显式说明所采用的判定（如 `C ≤ 0.15 − 1e-6`），避免"等于 0.15"的歧义。

由于中心最难干燥（对称中心无通量），`max_r C(r,t) = C(0,t)`，故等价于中心点含水率降至 0.15 的时刻。**必须同时在论文中给出中心点与表面点两条曲线**，以证明判据取中心点是保守且正确的选择。

- 求解时域：`[0, 259200] s`（72 h），若 72 h 内未达标，则报告"72 h 内未达标"并说明（不得外推附件2 的半径定义域）。
- 事件定位算法：先以 `Δt = 1 s` 沿时间步进，每步检查 `C(0,t) − 0.15` 的符号；首次变号后在该步内用**线性插值**给出精确时刻（精度优于 1 s，对小时量级的结论绰绰有余）；若需更严，在该步内做二分（`tol = 1e-3 s`）。
- 单位换算：`t_dry` 由秒换算为**小时**（题面要求"单位：h"），保留 4 位小数。
- 输出：
  - 论文**表5**：`t = 6, 12, 18, … h` × `r = 0, 0.5, 1, 1.5, 2 cm`，末行为"烘干结束时间"。
  - `result3.xlsx`：工作表 `Sheet1`；A 列时间 `0, 60, …, t_dry`（步长 60 s）；第 1 行距离 `0, 0.1, …, 2.0`（21 列）。

### 5.6 q4（问题4）移动边界

物性用附录4。域 `r ∈ [0, R(t)]`，`R(t)` 由附件2 线性插值（`ξ` 网格上求解）。

无量纲坐标变换 `ξ = r / R(t) ∈ [0, 1]`（假设 A-R5）：

```
∂C/∂t |_r = ∂C/∂t |_ξ − (ξ·Ṙ(t)/R(t))·∂C/∂ξ                     (A-4)
(1/r)·∂/∂r[ D·r·∂C/∂r ] = (1/R²)·(1/ξ)·∂/∂ξ[ D·ξ·∂C/∂ξ ]        (A-5)
```

得到固定域 `[0,1]` 上的方程 + 一阶对流项 `−(ξ·Ṙ/R)·∂C/∂ξ`。温度方程（A-1）作同样变换。

边界与初始条件：

```
ξ = 1:  −(D/R)·∂C/∂ξ = h_m·( C − C_∞(t) )  ；   −(k/R)·∂T/∂ξ = h·( T − T_∞(t) )
ξ = 0:  ∂C/∂ξ = 0  ；  ∂T/∂ξ = 0
t = 0:  C = 2.55, T = 28 ℃
```

`Ṙ(t)` 的处理（对应 XA-5）：`R(t)` 自附件2 线性插值后，`Ṙ` 在 92 个阶梯段内为 0、在跳变段为阶跃斜率。为避免阶跃引起数值振荡，采用以下之一并报告差异：
- 方案 a：对 `R(t)` 做**单调 PCHIP 平滑**后求导（推荐，主方案）；
- 方案 b：直接用分段常数 `Ṙ`（在阶梯段取 0）；
- 方案 c：**固定域等效迭代**——不引入 `ξ` 变换，而在每个时间步用当前 `R(t)` 重划物理网格并做插值迁移（对照模型）。

终止时间同 q3 的判据（A-3），但 `max_r` 的取值域为 `[0, R(t)]`。

输出：
- 论文**表6**：`t = 6, 12, 18, … h` × `r = 0, 0.5, 1, …` + 末列"药材表面"；末行为"烘干结束时间"。
- `result4.xlsx`：工作表 `Sheet1`；A 列时间步长 **60 s**；列定义**待确认**（XA-4），本报告建议为 `0, 0.1, …, 1.1` cm 固定列 + 末列 `药材表面`（填入该时刻 `r = R(t)` 处的值）。

### 5.7 数值方案

**（1）空间离散**——有限体积法（保证守恒）

- 节点：`r_i = (i − 1/2)·Δr`，`i = 1..N`，`Δr = R/N`；取 `N = 40`（`Δr = 5e-4 m = 0.05 cm`）为基准，`N = 20/40/80/160` 做收敛验证。输出所需的 `0, 0.1, …, 2.0 cm` 位置由节点值**线性插值**（必要时用节点中心到该点的两点插值）。
- 内节点（`i = 2..N−1`）：

```
dT_i/dt = [ k_{i+1/2}·r_{i+1/2}·(T_{i+1}−T_i)/Δr − k_{i−1/2}·r_{i−1/2}·(T_i−T_{i−1})/Δr ]
          / ( ρ_i·c_p,i·r_i·Δr )
```

其中 `r_{i±1/2} = r_i ± Δr/2`，`k_{i±1/2} = (k_i + k_{i±1})/2`。
- **中心节点 `i = 1`（`r → 0`）**：由对称性 `∂T/∂r|_0 = 0`，用 L'Hôpital 极限得

```
(1/r)·∂/∂r[ k·r·∂T/∂r ] |_{r=0}  =  2·k·∂²T/∂r² |_0
```

（因 `∂T/∂r = 0` 故含 `∂k/∂r` 的项亦为 0）。用镜像虚拟点 `T_0 = T_2` 离散：

```
dT_1/dt = 4·k_1·(T_2 − T_1) / ( ρ_1·c_p,1·Δr² )
```

含水率方程同理（`C_0 = C_2`，系数用 `D_1`）。
- **外边界 `i = N`（`r = R`）**：半格能量/质量平衡（厚度 `Δr/2`）

```
ρ_N·c_p,N·(R + r_{N−1/2})·(Δr/2)·dT_N/dt
   = 2·k_{N−1/2}·r_{N−1/2}·(T_{N−1} − T_N)/Δr  +  2·h·R·(T_∞ − T_N)
ρ_N·(R + r_{N−1/2})·(Δr/2)·dC_N/dt
   = 2·D_{N−1/2}·r_{N−1/2}·(C_{N−1} − C_N)/Δr  +  2·h_m·R·(C_∞ − C_N)
```

**（2）时间离散**——隐式 Euler（无条件稳定，且 `Δt = 1 s` 必须隐式）

> **稳定性论证（必须写入论文，用于回应"为何不用显式格式"）**：热扩散率 `α = k/(ρ·c_p) = 0.36/(820×2600) ≈ 1.6886e-7 m²/s`。显式格式稳定条件 `Δt ≤ Δr²/(2α)`（`2α ≈ 3.3772e-7 m²/s`），代入两个网格尺度：
> | 网格 | `Δr` | `Δr²` | 显式稳定上限 `Δr²/(2α)` |
> |---|---|---|---|
> | `N = 40`（基准，`Δr = R/40`） | `5e-4 m` | `2.5e-7 m²` | **0.7402 s ≈ 0.74 s** |
> | `N = 400`（极细网格验证） | `5e-5 m` | `2.5e-9 m²` | **7.403e-3 s ≈ 7.4e-3 s** |
>
> 而输出要求 `Δt = 1 s`，比基准网格的稳定上限还大约 1.35 倍、比极细网格大约 135 倍，**显式格式在任何可用网格下都不满足稳定条件**，故必须用隐式格式。
>
> **【更正记录】** 本表前一版本把 `N = 400` 的稳定上限误写为 `2.96e-3 s`（正确值 `7.40e-3 s`）。该错误由质检 Subagent 独立复算发现（依据同一判据 `Δt ≤ Δr²/(2α)` 在 `Δr = 5e-4` 处得 0.74 s 可交叉验证）。**定量数值以本表为准**；结论方向（必须隐式）不受影响，但该表将写入论文，故必须按更正后的数值引用。
> 可选升级：Crank–Nicolson（二阶时间精度），但须注意非线性项的二阶处理成本；本报告以隐式 Euler 为主方案，CN 作对照。

**（3）非线性耦合**——外层 Picard 迭代

每一时间步内：

```
第 m 次迭代：
  1) 用 (T^m, C^m) 计算 ρ, c_p, k, D
  2) 解隐式线性系统得 T^{m+1}（三对角，**带主元选取的带状 LU**；见 §5.9 更正记录③）
  3) 解隐式线性系统得 C^{m+1}
  4) 收敛判据： max( max|T^{m+1}−T^m| , max|C^{m+1}−C^m| ) < 1e-10   （绝对量，℃ 与 kg/kg）
     同时记录迭代次数；上限 50 次，超限则对该步做欠松弛（ω = 0.5）并告警
```

**（4）移动边界（q4）**：在 `ξ` 网格上按同样格式离散，一阶对流项 `−(ξ·Ṙ/R)·∂C/∂ξ` 用**上风（upwind）**差分；`Ṙ` 按 §5.6 的方案 a/b 处理。

**（5）实现约束**：所有时间步只保存需要的输出行（`result1` 每 1 s、`result3/4` 每 60 s、`result2` 每 1 s），避免 259200 步 × 全节点的历史全存。

### 5.8 算法步骤与输入输出

```
输入：附件1.xlsx（T_∞(t), C_∞(t)）、附件2.xlsx（R(t)）、附录2/3/4 的三组参数
输出：result1.xlsx、result2.xlsx、result3.xlsx、result4.xlsx
      + 论文表1~表6 所需的定点值 CSV（供论文手直接引用）

算法（q1/q2 通用）：
  1. 读入附件1/2，构造 np.interp 插值器；单位换算（℃→K、cm→m）
  2. 初始化 r 网格、T、C 数组（T = 301.15 K，C = 2.55）
  3. for t_step in 1..N_t:                     # Δt = 1 s
       冻结边界 T_∞(t)、C_∞(t)
       for m in 1..50:                        # Picard
          由 (T, C) 算 ρ, c_p, k, D（注意 D 的温度因子用 K）
          组装三对角系数（内节点 + 中心节点 + 边界半格）
          带状 LU 解 T 系统 → T_new
          带状 LU 解 C 系统 → C_new
          if max|Δ| < 1e-10: break
       更新 T, C；按输出粒度记录
  4. 写出 xlsx（write_only 模式）与定点值 CSV
  5. 运行验证套件（§5.9），输出误差报告

算法（q3）：复用 q2 主循环，在每步检查 C[0] − 0.15 的符号，首次变号后线性插值定位 t_dry，单位换算为 h
算法（q4）：在 ξ 网格上复用主循环，每步由 R(t) 更新变换项与边界系数，映射回物理坐标输出
```

**停止条件**：q1 到 1800 s；q2 到 259200 s；q3/q4 到 `t_dry`（或到达 259200 s 上限并报告）。

### 5.9 验证方案与阈值

| 编号 | 检验 | 方法 | 通过标准（依据） |
|---|---|---|---|
| V1 | 网格无关性 | `N = 20/40/80/160` 对比末时刻中心含水率与中心温度 | 相对变化 < 0.5%（基准解 `N=160`；工程精度经验值） |
| V2 | 时间步收敛 | `Δt = 1 s` vs `0.5 s` | 同上 < 0.5% |
| V3 | 守恒性 | 对水分做收支审计。**（已按编程手反馈更正，见下方更正记录）** 正确形式为 `Δ(2π∫C·r dr) = −∫h_m(C_s−C_∞)·2πR dt`，即守恒量是 `∫C dV`，**不含 ρ**；温度做焓收支 | 相对残差 < 1%（离散格式理论量级） |
| V4 | 常系数解析解 | 令 `ρ, c_p, k, D` 为常数、`h → ∞`（等温第一类边界），与 Bessel 级数解析解对比 | 最大相对误差 < 1%（差分截断误差量级） |
| V5 | 与经验模型对比（M-B） | q2 的中心点干燥曲线 vs Page 模型 `MR = exp(−k·t^n)` 拟合 | `R² > 0.98`（干燥动力学文献常用门槛） |
| V6 | 阶段边界灵敏度（A-K3） | S1/S2/S3 三方案对比 q3 的 `t_dry` | 报告三方案差异；若差异 > 10%，须在论文中作为主要不确定性讨论 |
| V7 | 半径插值灵敏度（A-R4/XA-5） | `R(t)` 分段线性 vs 单调 PCHIP vs 固定域方案 c | q4 烘干时长差异 < 5% 视为稳健 |
| V8 | 物性公式量级自检 | 按 §2.3 的四个工况点复算 `D` | 与手算值相对误差 < 1e-6（防温标/转录错误） |
| V9 | 边界系数灵敏度 | `h`、`h_m` 各 ±50% | 报告 `t_dry` 的响应幅度 |
| V10 | 判据位置论证 | 同时给出中心点与表面点的含水率曲线 | 证明 `max_r C = C(0,t)`（中心最晚达标） |

**§5.9 更正记录（由编程手实测反馈，2026-09-10）**

1. **V3 的守恒量写错**：原式 `Δ(∫C·ρ·2πrL dr) = −∫h_m(C_s−C_∞)·2πRL dt` 中的 `ρ` 是多余的。报告 §5.1 的 `(A-2)` 为 `∂C/∂t = (1/r)∂/∂r[D r ∂C/∂r]`，**方程内不含 ρ**，故被守恒的是 `∫C dV`；`ρ` 只出现在温度方程 `(A-1)`。
   **实测取证**（`药材烘干求解.py --stage verify`，q1 算例、1800 s）：按 `∫C dV` 审计的收支残差为 **4.4e-14（机器精度）**；按原 `∫C·ρ dV` 写法审计的残差为 **819**（即该写法不成立）。**已按上表更正，编码时务必使用不含 ρ 的形式。**
2. **V1 的生产网格须提高到 `N = 160`**：实测逐档收敛（相对 N=320 基准）——表面含水率的相对误差为 N=40: 1.62%、N=80: 0.77%、N=160: **0.27%**、N=320: 0（一阶收敛）。故 **N=40 不满足 0.5% 判据，生产网格取 N=160**；`N=40/80` 仅用于收敛性展示。
3. **线性求解器须用带主元选取的带状 LU**：`Δt = 1 s` 已超过表面格的对角占优限值 `Δr²/(2α)`（N=40 时为 0.74 s），纯 Thomas 消去在此失去对角占优、误差按 `0.7^N` 反向放大成伪解（实测：有限 Biot 数下纯 Thomas 把中心温度在 60 s 内拉到 T∞，而解析解应仍为 28.00 ℃）。**实现须使用 `scipy.linalg.solve_banded`（或等价的主元选取 LU），不得用无主元的 Thomas。**

### 5.10 风险与回退

| 优先级 | 风险 | 触发条件 | 回退方案 |
|---|---|---|---|
| P1 | `result2` 写入 1089 万单元格失败或超时 | 内存溢出、写入耗时不可接受、xlsx 体积过大 | 改用 `write_only` 模式分块写入；仍失败则回退读法②（3 h = 10801 行）并与建模手确认（对应 XA-7） |
| P1 | 恒温段边界条件无官方依据（A-K3） | 论文被质疑"参数自设" | 以 S1/S2/S3 三方案并列呈现并报告 `t_dry` 差异；在假设表中明确依据（题面未给，属必要假设） |
| P1 | 后期干燥速率极低 | `D` 随 `C` 下降约 2 个数量级（问题1：2.55→0.15 时 4.94e-9→1.85e-11 m²/s），72 h 内中心点可能不达标 | 加密 `C < 0.15` 段空间网格；检查是否需延长时域（但**不得外推附件2 的半径定义域**）；如实报告"72 h 内未达标" |
| P1 | 温标混用导致 `D` 荒谬 | `exp(−3850/T)` 中误用 ℃ | V8 自检门禁；所有经验公式统一以 K 入参 |
| P2 | 中心节点 L'Hôpital 处理写错 | 中心温度出现非物理振荡或数量级异常 | 与"整圆等价 2D 轴对称算例"对照；改用体积平均的有限体积中心格（半径 `Δr/2` 的圆柱） |
| P2 | 移动边界对流项引起振荡 | q4 的 `C(ξ)` 在 ξ 较大处出现负值或振荡 | 改用上风差分 + PCHIP 平滑 `Ṙ`；仍振荡则改用固定域等效方案 c |
| P2 | `result4` 列定义错误 | 官方对表头有明确规定 | 按 XA-4 的待确认结论调整；两种方案都预生成，择一提交 |
| P2 | 输出列位置与模板不符 | 插值位置（0.1 cm 网格）与节点中心不一致 | 用节点中心两点线性插值，并在论文中说明插值方式与误差量级 |

---

## 六、编程依赖

| 项 | 内容 |
|---|---|
| 语言 | **Python 3**（首选；MATLAB 可作为对照实现） |
| 必需库 | `numpy`（向量化/线性代数）、`scipy`（`linalg.solve_banded`、`interpolate.PchipInterpolator`）、`pandas`（读 xlsx / 写定点值 CSV）、`openpyxl`（write_only 写 xlsx）、`matplotlib`（绘图） |
| 可选库 | `scikit-learn`（M-B 的 Page 模型非线性拟合可用 `curve_fit` 替代）、`statsmodels`（残差诊断） |
| 求解器 | 无外部求解器（三对角系统用 `scipy.linalg.solve_banded`；**不得用无主元的 Thomas**，见 §5.9 更正记录③） |
| 环境检查 | 编程手阶段须先运行 Skill 的 `scripts/check_env.py --features data visualization`（按实际功能动态检查，不一次性要求全部包） |
| 目录约定 | 代码/结果/图均写入 `PROJECT_ROOT`；图用 `figures/raw_q*_*`、`process_q*_*`、`result_q*_*` 命名 |

---

## 七、候选图表（每类 ≥3、覆盖 q1~q4）

| 类别 | q1 | q2 | q3 | q4 |
|---|---|---|---|---|
| **raw**（原始数据） | `raw_q1_env_T_C.png`（附件1 烘房温度与含湿量双轴曲线，标注 1800 s 区间） | `raw_q2_radius.png`（附件2 半径随时间，标注 67 h 平台） | `raw_q3_params.png`（附录3 物性随 `C` 变化） | `raw_q4_params.png`（附录4 物性随 `C` 变化） |
| **process**（运行过程） | `process_q1_gridconv.png`（V1 网格无关性收敛） | `process_q1_picard.png`（Picard 迭代残差与迭代次数） | `process_q2_conservation.png`（V3 守恒残差随时间） | `process_q3_event.png`（V4 事件定位 或 `t_dry` 的二分收敛） |
| **result**（最终结果） | `result_q1_profile.png`（若干时刻的径向温度/含水率剖面） | `result_q2_field.png`（`T(r,t)`、`C(r,t)` 时空云图） | `result_q3_center_surface.png`（中心与表面干燥曲线 + `C=0.15` 判据线） | `result_q4_drytime.png`（含收缩与不含收缩的烘干时长对比） |

补充候选（不设上限）：`process_q4_movingboundary.png`（`ξ` 网格与 `R(t)` 演化）、`result_q3_sensitivity.png`（S1/S2/S3 三方案对比）、`result_q4_columns.png`（`result4` 列定义示意图）。

---

## 八、文献依据

> **检索方式**：Skill 的 `tools/paper_search/scripts/hybrid_scholar.py`（OpenAlex + AnySearch 双引擎交叉验证）。
> **可追溯性状态**：题名、作者、年份、DOI 由 OpenAlex 结构化元数据返回并与 AnySearch 交叉命中；**期刊/卷期页部分条目检索源未返回，已标"待补"**。**全部条目尚未逐条打开 DOI 出版页人工核对**，引用前必须完成人工核对，未核对前不得写入正式参考文献表。

1. Tao, Y., Li, D., Chai, W. S., Show, P. L., et al. (2020). *Comparison between airborne ultrasound and contact ultrasound to intensify air drying of blackberry: Heat and mass transfer simulation, energy consumption and quality evaluation.* Ultrasonics Sonochemistry, 72, 105410. DOI: 10.1016/j.ultsonch.2020.105410（交叉验证）——**非稳态热质耦合 + 变扩散系数 + 收缩**的完整建模范例。
2. Defraeye, T., Blocken, B., & Carmeliet, J. (2011). *Analysis of convective heat and mass transfer coefficients for convective drying of a porous flat plate by conjugate modelling.* International Journal of Heat and Mass Transfer, 55(1-3), 112-124. DOI: 10.1016/j.ijheatmasstransfer.2011.08.047（交叉验证）——**对流干燥表面换热/传质系数**的处理依据（支撑 `h`、`h_m` 的用法）。
3. Janjai, S., Mahayothee, B., Lamlert, N., Bala, B. K., et al. (2009). *Diffusivity, shrinkage and simulated drying of litchi fruit.* Journal of Food Engineering.（卷期页**待补**）DOI: 10.1016/j.jfoodeng.2009.07.015（交叉验证）——**收缩 × 有效扩散系数**联合建模（支撑 q4）。
4. Rani, P., & Tripathy, P. P. (2020). *Modelling of moisture migration during convective drying of pineapple slice considering non-isotropic shrinkage and variable transport properties.* Journal of Food Science and Technology.（卷期页**待补**）DOI: 10.1007/s13197-020-04407-4（交叉验证）——**变输运性质 + 收缩**的数值实现参考。
5. Khan, M. I. H., Kumar, C., Joardder, M. U. H., & Karim, A. (2016). *Determination of appropriate effective diffusivity for different food materials.* Drying Technology.（卷期页**待补**）DOI: 10.1080/07373937.2016.1170700（交叉验证）——**有效扩散系数量级合理性判据**，用于 §2.3 的量级自检。

**M-B（经验干燥动力学，对照模型）的经典文献**（来自 Skill 算法资料库，书籍/奠基文献无 DOI）：
- `assets/06-综合类算法说明.md` 所列微分方程建模与蒙特卡洛相关方法，用于对照模型与不确定性分析。

---

## 九、作者自检记录（对照 `references/质检清单.md`）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 已读完题目和全部附件，输入保持只读 | 通过 | A 题 PDF 全文 + 附件1/2 与四个模板逐一读取（结构、表头、规模、单调性、峰值）；输入目录未写入 |
| 先完成题目理解，再写结论目标和模型方案 | 通过 | §四 合同表先写"结论类型"，§五 再给模型与数值方案 |
| 每道子问题最多两个独立模型体系；物理题按模型族计数 | 通过 | §四 计数复核：q1/q3/q4 计 1 个体系，q2 计 2 个（M-A 族 + M-B） |
| 每个变量、参数、单位、目标函数和约束定义完整 | 通过 | §5.1~§5.7 给出方程、边界、参数、离散格式与量纲约定；符号见 `术语表格.md` |
| 数据判定标准有题目、官方、文献或分析依据 | 通过 | §三 每条假设带"依据"列；§5.9 每个阈值注明依据类型 |
| 创新点与题目结构、实现和验证一一对应 | 通过 | 创新点：q4 移动边界的 `ξ` 变换 + `Ṙ` 平滑、q3 的事件定位与中心点判据论证、V3 守恒审计、V6 阶段边界灵敏度 |
| 模型能由 Python 或 MATLAB 实现，依赖明确 | 通过 | §六 依赖清单（无外部求解器） |
| 验证方法和失败条件明确 | 通过 | §5.9 十项检验（含阈值）+ §5.10 风险回退表 |
| 文献可追溯到 DOI 或原始出版页面 | **部分通过** | §八 条目均有 DOI 且双引擎交叉验证；期刊/卷页缺失项已标"待补"；**尚未逐条打开出版页人工核对** |
| 只交付 `题目分析报告.md` 和 `术语表格.md` | 通过 | 本文件 + `术语表格.md` |
| 两个文件写入 `PROJECT_ROOT`，未覆盖 `SKILL_ROOT` | 通过 | 写入 `d:\Code\2026BJCUMCM` |
| 作者自检完成后派发质检 Subagent 执行 `M1` | **待执行** | 见 §十 |

---

## 十、M1 门禁状态与返工记录

### 10.1 A 题版 M1 首轮回执（`FAIL`）

本版（A 题编程级规格）经**未参与编写**的独立质检 Subagent 执行 `M1`，首轮回执为 **`FAIL`**，发现 1 项 `P1` 与 4 项 `P2`。复验方同时独立确认了以下关键项**无误**：题面 4 问的时间点/步长/单位/附录编号全部相符；三组物性公式**逐字符转录正确**；中心节点 L'Hôpital 离散式与外边界半格平衡式推导正确；`α = 1.6886e-7 m²/s` 与 `Δr=5e-4 m` 下的 `Δt ≤ 0.74 s` 复算相符；`D` 的四个工况点量级自洽；假设分级（3 关键 / 6 可放宽）合规；模型族裁定合规；两产物符号与单位一致；5 条文献 DOI 互不重复。

### 10.2 首轮回执的修正清单

| 编号 | 级别 | 问题 | 修正 |
|---|---|---|---|
| **P1-1** | `P1` | §5.7 稳定性论证把 `N = 400`（`Δr = 5e-5 m`）的显式稳定上限误写为 `2.96e-3 s` | 独立复算得 `Δr²/(2α) = 2.5e-9 / 3.3772e-7 = 7.40e-3 s`。已§5.7 改为**两行表格**列出 `N=40`（`0.7402 s`）与 `N=400`（`7.403e-3 s`），并附**更正记录**说明原值错误。因该段将写入论文，必须按更正值引用；结论方向（必须隐式）不变 |
| P2-1 | `P2` | `D₁(0.15) ≈ 1.86e-11`，复算为 `1.855e-11` | §2.3 给出完整算式并取整为 `1.85e-11`；§5.10 风险表同步更正 |
| P2-2 | `P2` | 术语表 §六 把 `ρ` 量级写作"500~800"，与题给公式不符（`C=2.55` 时 `ρ₂=976`、`ρ₄=990`） | 术语表 §六 改为"**650~990 量级**"并列出三组公式的取值范围 |
| P2-3 | `P2` | 烘干判据措辞与题面微差：题面为"**低于** 0.15"（严格 <），报告一度写作 `≤ 0.15` | `(A-3)` 式改为严格不等号 `C(0,t) < 0.15`，并新增"严格不等号的实现说明"（含 1e-4 邻域的判定约定）；术语表 §一 同步（并注明不得写作 `≤ 0.15`） |
| P2-4 | `P2` | §八 自述"卷期页缺失已标待补"，但仅第 3 条标注，第 4、5 条漏标 | 第 4、5 条补标"卷期页**待补**"，使自述与条目一致 |

### 10.3 当前状态

**`FAIL`（首轮）→ 修正完成 → 待第二轮复验。**

- 本轮修正已实质改变两个产物（含一处将写入论文的定量数值），故首轮回执**失效**，须重新冻结快照并再次执行完整 `M1`。
- **按 Skill 规则，`M1` 未返回 `PASS` 前不得进入编程阶段（`P1` 门禁）**，也不得宣称建模交付完成。
- 尚未在离线环境验证的项（不构成阻塞，但引用前必须人工完成）：`附件1/2.xlsx` 与模板的内部数值仅经算术自洽核对（无法解析二进制）；§八 五条 DOI 的出版页未逐条打开核对。
