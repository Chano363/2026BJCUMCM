# 题目分析报告（2026 年高教社杯全国大学生数学建模竞赛 A~E 题）

> 本报告由 math-modeling Skill 的**建模手**角色产出，仅为**建模草稿**，不得原样作为参赛作品提交。
> 所有模型选择、公式与假设在提交前必须由参赛队伍人工核对、改写。

## 〇、任务与核验状态

| 项目 | 内容 |
|---|---|
| 目标竞赛 | 全国大学生数学建模竞赛（CUMCM） |
| 目标届次 | 2026 年（高教社杯） |
| 工作阶段 | 建模手 —— 仅做题目分析与模型选择；**不进入编程、不进入论文** |
| `SKILL_ROOT` | `d:\Code\2026BJCUMCM\.codebuddy\skills\math-modeling-skill`（只读） |
| `PROJECT_ROOT` | `d:\Code\2026BJCUMCM` |
| 输入目录 | `d:\Code\2026BJCUMCM\CUMCM2026Problems`（只读） |
| 计划读取的角色入口 | `建模手/SKILL.md`（已读）；编程手、论文手本阶段不加载 |
| 已读取的工具入口 | `tools/pdf/SKILL.md`、`tools/paper_search/SKILL.md` |
| 已读取的算法资料 | `assets/README.md`、`assets/04-图论与网络分析算法说明.md`；`01/02/03/05/06/07` 按渐进式加载原则**留待对应子问题实现时读取** |
| 文献检索引擎 | `tools/paper_search/scripts/hybrid_scholar.py`（OpenAlex + AnySearch 双引擎，交叉验证） |

**待核验的官方规则（本报告不据此下结论）**

- [ ] CUMCM 2026 当届论文格式规范（`format2026.doc` 已存在但未解析，且当届规则须以官方最新发布为准）
- [ ] 摘要字数、正文页数上限、附录与支撑材料要求
- [ ] 各题结果文件是否必须严格套用附件模板的文件名与工作表名
- [ ] 目标竞赛当届是否允许使用外部求解器（Gurobi/CPLEX）与预训练模型

> 依据 Skill 强制协议第 4 条，上述未核验项不得用往届经验替代；本报告涉及格式的全部表述均标注为"待核验"。

---

## 一、题目与附件清单（输入快照）

附件路径均为相对 `CUMCM2026Problems/` 的路径；SHA-256 取前 16 位十六进制。

### 1.1 赛题原文

| 题号 | 题目 | 页 | SHA-256(16) |
|---|---|---|---|
| A | 药材的烘干问题 | 4 | `052D8014BFF5727C` |
| B | 无线电干扰源的快速自动定位与清除 | 4 | `81C992A9BEE53763` |
| C | 微网与外部电网电力调控策略 | 3 | `2C098F6AE9DD47AE` |
| D | 时频冲突检测与消解 | 2 | `04945C2DB24CD6CB` |
| E | SEM 广告投放策略 | 2 | `3E4E340AE722C5DD` |

> 注：C/D/E 三题 PDF 均小于 A/B，其中 D、E 正文为 2 页、无附录公式表。哈希取自 `Get-FileHash -Algorithm SHA256` 的前 16 位，完整快照见 §1.5。

### 1.2 数据附件

| 题号 | 文件 | 规模（实测） | SHA-256(16) |
|---|---|---|---|
| A | `A题/附件/附件1.xlsx` | 241 行 × 3 列，Δt = 60 s，t ∈ [0, 14400] | `7EF32870ABEEF420` |
| A | `A题/附件/附件2.xlsx` | 145 行 × 2 列，Δt = 1800 s，t ∈ [0, 259200] | `5563ACBFA4B4AFB1` |
| B | `B题/附件/附件1.docx` | 57 段、2 表（模拟器使用说明） | `20A27603EA81EFA8` |
| B | `B题/附件/附件2.docx` | 258 段、13 表（HTTP+JSON 通信协议） | `C882513D5B7E0EC5` |
| C | `C题/附件/附件1.xlsx` | 144 行 × 4 列，Δt = 10 min | `66B87134F5ECCCD6` |
| C | `C题/附件/附件2.xlsx` | 2 表，各 366 行 × 145 列 | `2E95FD446BFAFA0D` |
| C | `C题/附件/附件3.xlsx` | 1461 行 × 26 列 | `8A61B06C52BD0D63` |
| C | `C题/附件/附件4.xlsx` | 366 行 × 145 列 | `20E9C93AEAB5E8E2` |
| D | `D题/附件/附件1.xlsx` | 151 行 × 5 列（150 个用频计划） | `B7F905CD2629F9A8` |
| E | `E题/附件/附件1.xlsx` | Sheet1 2628×10；Sheet2 366×2；Sheet3 2228×9 | `F6AC8E080A967194` |
| — | `format2026.doc` | 69.5 KB（本阶段未解析） | `D77DE49534649B46` |

### 1.3 结果模板（附件中的 `result*.xlsx`）

**A 题** `A题/附件/附件3/`

| 文件 | 工作表 | 模板规模 | SHA-256(16) |
|---|---|---|---|
| `result1.xlsx` | 温度、水分浓度 | 5 行 × 6 列（示意） | `23B261B295C1B787` |
| `result2.xlsx` | 温度、水分浓度 | 5 行 × 6 列（示意） | `23B261B295C1B787` |
| `result3.xlsx` | Sheet1 | 5 行 × 6 列（示意） | `07E4793D620A7F89` |
| `result4.xlsx` | Sheet1 | 5 行 × 6 列（示意） | `86E9300FFA3D30C4` |

**C 题** `C题/附件/附件5/`

| 文件 | 工作表（行 × 列） | SHA-256(16) |
|---|---|---|
| `result1.xlsx` | 计划购电量(145 行 × 2 列，即 144 时段 + 表头)、充放电量(7 行 × 5 列，即 6 个 4 小时段 + 表头) | `28360E0974E7D606` |
| `result2.xlsx` | 计划购电量(335 行 × 147 列)、充放电量(20 行 × 6 列)、紧急购电量(11 行 × 3 列) | `1C26494CFC6D754E` |
| `result3.xlsx` | 计划购电量(335 行 × 147 列)、调整购电量(335 行 × 147 列)、充放电量(26 行 × 6 列)、紧急购电量(11 行 × 3 列) | `C59DA470CABD0BE2` |
| `result4-2.xlsx` | 同 `result2.xlsx` | `1C26494CFC6D754E` |
| `result4-3.xlsx` | 同 `result3.xlsx` | `C59DA470CABD0BE2` |

**D 题** `D题/附件/附件2/`

| 文件 | 表头（列顺序） | SHA-256(16) |
|---|---|---|
| `result1.xlsx` | 序号、冲突装备1、冲突设备2 | `6657970F5EAD42A0` |
| `result2.xlsx` | 用频装备编号、调整后频段区间、调整后时间区间、是否撤销用频计划 | `53F6FEB7BBF0BDFB` |
| `result3.xlsx` | 新增用频装备序号、调整后频段区间、调整后时间区间 | `6E7072651E204FA0` |
| `result4.xlsx` | 用频装备编号、调整后频段范围、调整后时间区间、调整后间隔时长、是否撤销用频计划 | `95E45E755E9FC9E3` |

**E 题** `E题/附件/附件2/`

| 文件 | 表头（列顺序） | SHA-256(16) |
|---|---|---|
| `result2.xlsx` | 方案ID、推广单元、序号、黄金词、重点词、潜力词、问题词、无效词 | `E3EFF773F353D706` |
| `result3.xlsx` | 日期、方案ID、推广单元、关键词、投入金额、预期展位、预期点击量、预期浏览量、预期注册量 | `D7AF4794CB38087B` |
| `result4.xlsx` | 同 `result3.xlsx` | `37E8103BD74E2DE8` |

### 1.4 输入异常与待确认项（数据驱动检查结果）

| 编号 | 现象（实测证据） | 影响 | 处理 |
|---|---|---|---|
| X1 | A 题 `result1.xlsx` 与 `result2.xlsx` 字节完全相同（哈希均为 `23B261B295C1B787`），且均为缩略示意（表头 `0 | 0.1 | 0.2 | … | 2`，5 行 × 6 列） | 两问要求的输出时间跨度不同却共用同一模板，容易误以为模板即为完整网格 | 模板**只是示意**：须自行生成完整网格（距离 0~2.0 cm 每 0.1 cm 共 **21 列**）；时间行数按各问要求另行确定，不得直接填写模板 |
| X12 | A 题问题2 的 xlsx 输出**未给时间上界**（题面"将每隔 1 s、到药材中心距离每隔 0.1 cm 的完整结果保存到 result2.xlsx"），而问题1 明确写"**1800 s 内**"；问题2 的模型对象是"整个烘干过程 2~3 天" | result2 的行数存在两种可能读法：①全过程（2~3 天）@1 s ≈ **17.3 万~25.9 万行** × 21 列；②仅 3 h @1 s = 10801 行 × 21 列 | **列为待确认项**，须以官方规则/与编程手确认后写入模型合同；本报告 §四、§5.1 按读法①（全过程）表述，并同时给出读法②的规模以便回退 |
| X2 | A 题 `result3/result4` 工作表名为 `Sheet1`，而 `result1/result2` 为 `温度`+`水分浓度` | 工作表名影响判分 | result1/2 分温度与水分浓度两表；result3/4 只输出水分浓度 |
| X3 | C 题 `result2` 与 `result4-2`、`result3` 与 `result4-3` 分别字节相同 | 问题4 复用问题2/3 的模板 | 与题目"result4-2 保存波动电价下问题2 的全部结果"一致，**非缺陷** |
| X4 | C 题「计划购电量」列头为 `0:10-0:20 … 23:50-0:00+1`、`0:00-0:10+1`，共 **144 个时段**，另加 `全天购电量`、`全天购电费` | 时段起点为 0:10 而非 0:00，与附件1 时间列（`00:10:00` 起）一致 | 严格按模板列头填写，不可自行改成 0:00 起 |
| X5 | C 题 `result1`「充放电量」为 6 个 4 小时段 + 0:00/24:00 时刻，共 7 行；`result2/3/4` 同表为 20/26 行含`日期`列 | 单日表与多日表结构不同 | 多日表须按 2025-02-01~12-31（334 天）自行扩展 |
| X6 | D 题 `result1` 第 2、3 列为 `冲突装备1`、`冲突设备2`，用词"装备/设备"混用 | 模板疑为笔误 | **以题目正文为准**（"冲突的两个用频装备编号"），填写时统一为"用频装备"并在论文中说明 |
| X7 | D 题 `result4` 第 2 列作 `调整后频段范围`，`result2` 作 `调整后频段区间` | 同义异名 | 视为同一字段，按各自模板列名原样填写 |
| X8 | E 题 `result3/result4` 含 `预期注册量` 列，但**无** `竞价`、`展现量` 列；题面问题4 却要求估计"竞价、展现量" | 题面与模板口径不一致 | 模板列必须填；题面额外要求的竞价与展现量在论文正文以表格/区间形式给出 |
| X9 | E 题 `关键词` 字段被匿名化为纯数字（如 `9`、`283`），且实际唯一关键词 1884 个、记录 2227 条；题面称"6000 多个关键词" | 无法做语义层"关键词管理与运用/创意质量"分析；数据量与题面不符 | 以附件实际为准，问题1 相应部分退化为按方案/推广单元/关键词ID 的代理分析，并在论文中明确声明局限 |
| X10 | A 题附件1 字段名为"温度/水分浓度"，其"水分浓度"取值 0.01963→0.05025；题面另给药材初始水分浓度 2.55 kg/kg | 两者物理含义不同，量级差 2 个数量级 | 附件1 为**烘房空气含湿量**，题面 2.55 为**药材干基含水率**，列为首要防错点 |
| X11 | B 题附件为 docx（协议文档），无任何数据表；成绩依赖在线模拟器实跑（正式测试 3 次，截止 2026-09-13 17:30 前不可再启动） | 无法离线复现、无公开数据 | 记为**不可离线验证项**；策略有效性只能以演练日志与三次正式测试日志为证据 |

### 1.5 输入快照清单（完整哈希）

生成命令：`Get-ChildItem -Recurse -File | Get-FileHash -Algorithm SHA256`（表内为前 16 位）。

| 文件 | SHA-256(16) | 文件 | SHA-256(16) |
|---|---|---|---|
| `A题/A题.pdf` | `052D8014BFF5727C` | `C题/C题.pdf` | `2C098F6AE9DD47AE` |
| `A题/附件/附件1.xlsx` | `7EF32870ABEEF420` | `C题/附件/附件1.xlsx` | `66B87134F5ECCCD6` |
| `A题/附件/附件2.xlsx` | `5563ACBFA4B4AFB1` | `C题/附件/附件2.xlsx` | `2E95FD446BFAFA0D` |
| `A题/附件/附件3/result1.xlsx` | `23B261B295C1B787` | `C题/附件/附件3.xlsx` | `8A61B06C52BD0D63` |
| `A题/附件/附件3/result2.xlsx` | `23B261B295C1B787` | `C题/附件/附件4.xlsx` | `20E9C93AEAB5E8E2` |
| `A题/附件/附件3/result3.xlsx` | `07E4793D620A7F89` | `C题/附件/附件5/result1.xlsx` | `28360E0974E7D606` |
| `A题/附件/附件3/result4.xlsx` | `86E9300FFA3D30C4` | `C题/附件/附件5/result2.xlsx` | `1C26494CFC6D754E` |
| `B题/B题.pdf` | `81C992A9BEE53763` | `C题/附件/附件5/result3.xlsx` | `C59DA470CABD0BE2` |
| `B题/附件/附件1.docx` | `20A27603EA81EFA8` | `C题/附件/附件5/result4-2.xlsx` | `1C26494CFC6D754E` |
| `B题/附件/附件2.docx` | `C882513D5B7E0EC5` | `C题/附件/附件5/result4-3.xlsx` | `C59DA470CABD0BE2` |
| `D题/D题.pdf` | `04945C2DB24CD6CB` | `E题/E题.pdf` | `3E4E340AE722C5DD` |
| `D题/附件/附件1.xlsx` | `B7F905CD2629F9A8` | `E题/附件/附件1.xlsx` | `F6AC8E080A967194` |
| `D题/附件/附件2/result1.xlsx` | `6657970F5EAD42A0` | `E题/附件/附件2/result2.xlsx` | `E3EFF773F353D706` |
| `D题/附件/附件2/result2.xlsx` | `53F6FEB7BBF0BDFB` | `E题/附件/附件2/result3.xlsx` | `D7AF4794CB38087B` |
| `D题/附件/附件2/result3.xlsx` | `6E7072651E204FA0` | `E题/附件/附件2/result4.xlsx` | `37E8103BD74E2DE8` |
| `D题/附件/附件2/result4.xlsx` | `95E45E755E9FC9E3` | `format2026.doc` | `D77DE49534649B46` |
| （文本提取件，非官方输入） | | | |
| `A题/A题.txt` | `8F593FB50826B98A` | `D题/D题.txt` | `F731B3BF1CA74BA3` |
| `B题/B题.txt` | `A3BA3AC81D858667` | `E题/E题.txt` | `3D06F47CCE9F91B1` |
| `C题/C题.txt` | `06F12DB1B353D8C8` | — | — |

> `A题/*.txt` ~ `E题/*.txt` 为本项目此前生成的 PDF 文本提取件，**不是官方输入**，仅用于检索与引用，不参与模型计算。
> **清单条数（已核对）**：本表共 **37 条** = 官方输入 **32 条** + 本项目产出的 **5 份** PDF 文本提取件。
> 其中官方输入 32 条的构成为：**5 份题目 PDF** + **10 个数据附件**（A 2 + B 2 docx + C 4 + D 1 + E 1）+ **16 个结果模板**（A 4 + C 5 + D 4 + E 3）+ `format2026.doc` = 5 + 10 + 16 + 1 = **32**。
> 另：`CUMCM2026Problems/` 目录下实际文件总数为 **38**，比上表多出 1 个 —— `CUMCM2026Problems/五题选题评估报告.md`（本项目自产的选题评估稿，**非官方输入、不参与建模**，故未计入上表；如需完整复算，对该文件亦取 SHA-256 即可）。

---

## 二、全局数据理解与预处理计划

### 2.1 量纲与单位核对（机理/工程题必做）

| 题 | 易错单位 | 正确处理 |
|---|---|---|
| A | 长度 cm 与 m、温标 ℃ 与 K、时间 s 与 h | 控制方程一律 SI（m、K、s）；仅在输出表格时转回 cm/℃/h；`D = 7e-9·e^(-0.89/C)` 中 C 为 kg/kg 无量纲 |
| C | 功率 kW 与能量 kWh、时段 10 min = 1/6 h | 购电量(kWh) = 功率(kW) × (1/6) h；效率 90% 只作用于**充电存入/放电取出**方向 |
| D | 频段与时间均为**离散单位** Δf、Δt | 全程用整数索引，不引入物理频率与秒；`[80,90)` 表示 10 个频段 |
| E | 金额元、时间格式 `00:03:16`、比例 `0.6109` | 跳出率 `0.6109` = 61.09%；`/` 表示无数据，须转 NaN 而非 0 |

### 2.2 缺失、异常与零值处理计划

| 题 | 检查项 | 计划处理 |
|---|---|---|
| A | 附件1/附件2 时间是否等间隔、单调 | 已确认等间隔（60 s / 1800 s）且单调；插值到求解网格用线性插值 |
| A | 附件2 半径单调递减？末值 1.873 cm | 需在编程阶段核验单调性与斜率，非单调则改用单调回归/分段拟合 |
| C | 附件2/附件3/附件4 全年 10 min 数据的缺失与 0 值 | 夜间光伏为 0 属真实值，不得当缺失填充；逐日核对 144 点完整性 |
| C | 附件3 预报表 `日期` 列只在 0:00 行出现（合并单元格） | 须 ffill 展开；预报时刻 0/6/12/18 共 4 组/天 |
| D | 150 个计划频段区间是否恒为 10Δf 宽、时间区间是否恒为 5Δt 宽 | 是（题面：占用频段数量保持不变）；仍须全量核验 |
| E | Sheet3 `跳出率`/`平均访问时长` 含 `/`；消费额存在 0 值 | `/` → NaN；0 消费额样本用于"无效词"识别，不可直接删除 |
| E | Sheet1 日期覆盖 2627 条、方案 5 个、推广单元 12 个 | 已确认；非全量笛卡尔积（365×12=4380），缺失组合视为未投放 |

### 2.3 预处理与特征工程计划

- **A**：统一到无量纲径向坐标 `ξ = r/s(t) ∈ [0,1]`（问题4）；`C` 与 `T` 双场插值；`D(C)`、`k(C)` 逐点更新。
- **C**：把 334 天 × 144 时段整理为张量 `(T=334, N=144)`；`PV_forecast` 拆为 4 个预报时刻各自的 24 h 曲线；构造电价分段标记。
- **D**：把每个计划展开为占用集合 `O_i = {(f_s, f_e, t_s, t_e)_{i,k}}_{k=1..n_i}`，`n_i` = 使用次数；建立"占用层—计划层"两级结构。
- **E**：由 Sheet1 构造日级效益指标（点击率 CTR=点击/展现、上方位占比、单位消费效益）；由 Sheet3 构造关键词级成本-效益指标；由 Sheet2 提供注册量时间序列作为"效益"代理变量（须在问题1 中显式定义"效益"）。

---

## 三、假设分级

> 分级依据：Skill `references/roles/建模手/references/工作流程.md` 规定"**关键假设**通常 ≤ 3 条，**全部可放宽假设合计** ≤ 7 条；假设必须有依据（题目数据、领域文献或官方规则）"。
> **计数口径（本节明确声明，避免歧义）**：该规则按**单道题**计数。本报告是 A~E 五题的**选题阶段**并列分析，因此按**每题**执行 —— 每题关键假设 ≤ 3 条、每题可放宽假设 ≤ 7 条。逐题实点：A 3/5、B 3/4、C 3/5、D 3/5、E 3/6，全部合规；若按"五题合计"读则必然超标，该读法不适用于并列选题分析。
> **关键假设**指放宽会使模型失效；**可放宽假设**指放宽只影响精度，须给出放宽后的复验方式。

### A 题（关键 3 / 可放宽 5）

| 编号 | 假设 | 级别 | 依据 | 放宽后的复验方式 |
|---|---|---|---|---|
| A-K1 | 药材可视为各向同性均质圆柱，传热传质仅沿径向一维进行（轴向与周向可忽略） | 关键 | 题面"形状大致呈圆柱形""到药材中心距离"为一维径向坐标 | 若考虑二维，需 2D 轴对称模型对照；本假设放宽会使一维方程与输出表格列定义同时失效 |
| A-K2 | 水分迁移以 Fick 扩散为主，`D` 仅依赖 `C`（问题1）或 `C`、`T`（问题2~4，附录3/4 给定） | 关键 | 题面明确给出 `D` 的经验公式 | 与文献中"有效扩散系数"框架一致；放宽需引入毛细/对流项（超出题给信息） |
| A-K3 | 表面传质遵循对流边界（传质系数 8e-7 m/s），表面水分浓度与烘房含湿量处于准平衡 | 关键 | 题面附录2 给定对流传质系数 | 边界系数扫描 ±50% 做灵敏度 |
| A-R1 | 烘房环境均匀，温湿度只随时间变化、与空间位置无关 | 可放宽 | 附件1 仅给单一温湿度序列 | 引入空间梯度需额外数据，不可行，改为在论文中声明 |
| A-R2 | 初始温度/含水率在截面上均匀分布 | 可放宽 | 题面只给单一初值 28 ℃ / 2.55 kg/kg | 改用非均匀初值做灵敏度 |
| A-R3 | 相变潜热与热辐射可忽略 | 可放宽 | 温度区间 28~50.25 ℃，无相变 | 加入辐射项对照 |
| A-R4 | 收缩过程中材料质量守恒、密度场按附录3/4 经验式随 `C` 变化 | 可放宽 | 附录3/4 给定 ρ(C) | 改用常密度对照 |
| A-R5 | 烘房含湿量与药材表面含湿量之间的平衡关系为线性 | 可放宽 | 数据无量湿平衡曲线 | 改用非线性等温线（GAB）对照 |

### B 题（关键 3 / 可放宽 4）

| 编号 | 假设 | 级别 | 依据 | 放宽后的复验方式 |
|---|---|---|---|---|
| B-K1 | 示向度误差固定为每点 ±1°（同一地点重复检测不改变误差），不予统计平均 | 关键 | 附录2 明确"同一地点误差固定，重复检测不改变" | 若改为随机误差，定位区域定义与问题1 的四边形几何全部改变，模型失效 |
| B-K2 | 场强仅随距离衰减、与角度无关（覆盖角范围内均匀辐射），即"能收到/收不到"二分而非连续场强建模 | 关键 | 附录1 第(4)条 | 放宽需建立路径损耗模型，题未给参数 |
| B-K3 | 机器狗沿直线移动（5 m/s）且移动中不可检测；时间开销按题给规则线性累加：**频道切换 1 s/次（固定值，与两个频道编号的差值无关；未切换频道则为 0）**、检测 5 s、精测 3 s、清除 2 s | 关键 | 附录2 第(4)(5)(6)(8)条；附件1"如果机器狗未移动或未切换频道，则相应的耗时为0" | 时间为任务目标函数，若计时规则改变则策略排序失效 |
| B-R1 | 机器狗不与障碍物交互，可自由到达区域内任意点 | 可放宽 | 题目未给障碍信息 | 加入障碍需重做路径规划 |
| B-R2 | 干扰源在测试期间静止且频道不变 | 可放宽 | 附录1 第(1)条"频道不随时间变化" | 已由题目保证，仅需在论文中声明 |
| B-R3 | 光学清除的成败仅取决于清除点与源的距离（≤20 m） | 可放宽 | 附录2 第(8)条 | 用蒙特卡洛扰动清除成功率做鲁棒性检验 |
| B-R4 | 交会定位的两条示向线交点唯一（不平行的退化情形可通过选点策略规避） | 可放宽 | 问题2 要求给出选点策略 | 显式构造近平行反例并在策略中加退化检测 |

### C 题（关键 3 / 可放宽 5）

| 编号 | 假设 | 级别 | 依据 | 放宽后的复验方式 |
|---|---|---|---|---|
| C-K1 | 储能充放电效率固定 90%，且 SOC 严格保持 1200~10800 kWh 且 0:00 与 24:00 相等 | 关键 | 附录1 | 效率 85%~95% 参数扫描 |
| C-K2 | 光伏与负载在 10 min 时段内均视为恒定（时段平均功率） | 关键 | 附件均为 10 min 粒度 | 提高时间粒度需额外数据，改为声明 |
| C-K3 | 计划购电量按"时段电量(kWh)"结算，且微网供电不得低于负载（不足部分触发紧急购电） | 关键 | 题面问题1/2 与附录2 | 若允许缺口不罚，问题2 的紧急购电变量消失，模型改用纯 LP |
| C-R1 | 储能无自放电、无循环寿命损耗与爬坡约束 | 可放宽 | 题面未给出 | 加入自放电率与循环成本对照 |
| C-R2 | 微网不可向外部电网售电 | 可放宽 | 题面只提"购电费用" | 允许售电需新增收益项（题未给上网电价） |
| C-R3 | 计划购电量与实际购电量在无紧急购电时一致，不存在偏差考核 | 可放宽 | 问题3 才引入违约/超额罚则 | 问题1/2 补做偏差情景 |
| C-R4 | 问题4 的实时波动电价按附件4 给定序列求解（视为已知或按预报输入） | 可放宽 | 附件4 提供了全年逐 10 min 电价，暗示可用 | 增设"仅知日前预测"的在线滚动变体做对比 |
| C-R5 | 小区负载不可调度（不引入需求响应） | 可放宽 | 题面未给可中断负荷 | 引入 5%~10% 可调负荷做灵敏度 |

### D 题（关键 3 / 可放宽 5）

| 编号 | 假设 | 级别 | 依据 | 放宽后的复验方式 |
|---|---|---|---|---|
| D-K1 | "冲突"严格定义为频段区间有交**且**时间区间有交；同一计划的多频段视为连续区间 | 关键 | 题面"当两个用频计划的时间区间和频段区间都存在交叠时" | 若改为部分频段冲突，则冲突图结构改变 |
| D-K2 | 一个计划只能调整频段、时间或间隔时长中的**一个**参数，或整体撤销；且**仅问题4 允许调整间隔，且仅限"部分 C 类"装备、`|Δm| ≤ 10Δt`** | 关键 | D 题正文"一个用频计划只能调整其中一个参数或撤销"；D 题问题4"允许**部分 C 类**用频装备对用频间隔时长进行调整，但要求与原间隔时长的差异**不超过 10Δt**" | 放宽为多参数调整会改变解空间与最优值；放开"仅 C 类/≤10Δt"限制会使问题4 退化为无约束的间隔优化 |
| D-K3 | 频段/时间平移幅度上限分别为 10Δf、5Δt（问题2/4），问题3 不设上限 | 关键 | 题面 | 幅度上限即约束右端项，须做灵敏度扫描 |
| D-R1 | 各计划调整后仍占用相同数量的频段（宽度不变） | 可放宽 | 题面"占用的频段数量保持不变" | 由题目保证；放宽需重设变量 |
| D-R2 | 不调整使用次数（问题2/3） | 可放宽 | 题面"一般不调整使用次数和使用间隔时长" | 问题4 已放开间隔，可再放开次数作扩展 |
| D-R3 | 三类优先级的权值可用递减常数表示，且"尽量保持"等价于撤销/调整惩罚 | 可放宽 | 题面用"优先级最高/其次/最低"与"尽量"表述，未给数值 | 改变权重做敏感性分析，检查方案的优先级保持性 |
| D-R4 | 问题3 新增设备可自由选择频段与时间区间 | 可放宽 | result3 模板要求填"调整后频段区间/时间区间" | 若限定新增设备类型，需重解 |
| D-R5 | 超出目标时间窗（若有）的占用不予考虑 | 可放宽 | 题面未明确时间轴边界 | 以数据实际最大时间上界为界，并在论文中声明 |

### E 题（关键 3 / 可放宽 6）

| 编号 | 假设 | 级别 | 依据 | 放宽后的复验方式 |
|---|---|---|---|---|
| E-K1 | "效益"以广告带来的**新注册数**及由其派生的单位成本效益为代理指标 | 关键 | 附件 Sheet2 只提供日新注册数，题面未给效益定义 | 替换为"浏览量加权"等替代口径做稳健性检验；口径定义必须在论文中显式声明 |
| E-K2 | 关键词级数据可代表其真实成本-效益结构，不因匿名化而丢失排序信息 | 关键 | 附件 Sheet3 的 序号/关键词 为匿名 ID，但成本、点击、浏览、跳出率、时长均为真实数值 | 按 ID 聚合到方案/推广单元层面复核结论一致性 |
| E-K3 | 2025 年投入结构（5 方案、12 推广单元）在 2026 年保持不变 | 关键 | 题面"拟采用 2025 年同样的方案、推广单元和关键词" | 由题目保证 |
| E-R1 | 假日效应可用日期哑变量/日历特征（工作日、周末、法定假日）刻画 | 可放宽 | 题面要求分析"假日效应" | 改用时间序列的星期/季节分解（STL）交叉验证 |
| E-R2 | 关键词的成本-效益在高消费与零消费两端均可用分位数阈值切分 | 可放宽 | 题面给出五类定义（高/低成本、高/低/无效益） | 改用聚类边界复核分类稳定性（ARI 指标） |
| E-R3 | 关键词之间除同推广单元关联外相互独立 | 可放宽 | 题面提到"关键词的类型和关联关系" | 引入共现/同单元关联约束做对照 |
| E-R4 | 2026 年 9 月各指标的分布与 2025 年同期可比 | 可放宽 | 题面要求预测 2026-09-11~17 | 用 2025 年 9 月同期做留出集回测，报告误差 |
| E-R5 | 竞价、展现量等不确定量可建模为随机变量并用蒙特卡洛给出期望区间 | 可放宽 | 题面明确要求"估计期望范围" | 改用自助法（bootstrap）或分位数回归对照 |
| E-R6 | 预算约束以 2025 年总投入 142 余万元为上界，按日/单元可分 | 可放宽 | 题面"预算资金不超过 2025 年的投入" | 改为分月预算做灵敏度 |

---

## 四、模型合同总表（前置合同）

| 子问题 | 任务定义 | 数据 | 结论类型 | 模型（≤2 独立体系） | 实现 | 验证 | 风险 |
|---|---|---|---|---|---|---|---|
| A1 | 求预热平衡阶段 `T(r,t)`、`C(r,t)` | 附件1 环境序列、附录2 参数 | 场分布表 + 定点值 | **M-A1** 一维圆柱热-湿耦合扩散方程族（隐式差分 + Picard 迭代） | Python/NumPy（可选 MATLAB） | 网格无关性、守恒量、与解析解（常系数）对比 | 非线性 `D` 收敛慢；模板网格需自建 |
| A2 | 全流程 2~3 天 `T`、`C` 场 | 附录3 经验式 | 场分布表 | **M-A2** 同族扩展（变物性 + 分阶段边界）＋**M-A3** Page/Newton 经验干燥动力学（并行对比） | 同上 | 与 M-A3 的干燥曲线对比、能量守恒 | 全过程 @1 s ≈ 17.3 万~25.9 万行 × 21 列（读法①，见 X12），输出体量大 |
| A3 | 求"各处 C < 0.15"所需时间 | 复用 A2 | 单一时间 + 过程表 | 复用 **M-A2**，外层事件定位（二分/插值） | 同上 | 对时间步长做收敛检验 | 判据取中心点还是表面必须明确 |
| A4 | 含收缩的烘干时长 | 附件2 半径序列、附录4 | 单一时间 + 过程表 | **M-A4** 移动边界扩散模型族（`ξ=r/s(t)` 坐标变换）＋固定域等效迭代对照 | 同上 | 两种处理的偏差、半径插值敏感性 | 移动网格易数值不稳定 |
| B1 | 交会定位区域直径算法 + 圆覆盖性 | 无数据（纯几何） | 算法 + 定理/反例 | **M-B1** 计算几何模型（凸包 + 旋转卡壳求直径；覆盖性构造反例） | Python（Shapely/自实现） | 随机算例与暴力枚举对拍 | 覆盖性结论需严格证明，不能只给数值 |
| B2 | 第二检测点选择策略与候选区域 | 纯几何 | 策略 + 区域描述 | **M-B2** 以定位区域面积/直径为目标的选址优化（网格搜索 + 解析判据） | Python | 网格加密收敛性、边界退化算例 | 退化（示向线近平行）需显式处理 |
| B3 | 全向源自动定位清除策略 | 模拟器在线 | 策略 + 统计指标 + 日志 | **M-B3** 概率栅格 + 贝叶斯更新的信息增益搜索（近似 MDP）＋分区覆盖启发式对比 | Python + HTTP/JSON 客户端 | 演练测试统计（清除比例、平均时间）、三次正式测试 | 无离线数据集；时间窗与网络强约束 |
| B4 | 含定向源的策略 | 模拟器在线 | 同上 | **M-B4** M-B3 的部分可观测扩展（无信号扇区建模）＋同一启发式的定向版 | 同上 | 同上（分组对比全向/定向案例） | 不可见扇区导致漏检，需专用回访机制 |
| C1 | 单日计划购电策略 | 附件1、附录1 | 策略表 + 费用 | **M-C1** 日前线性规划（LP，144 时段） | Python + scipy/PuLP | 约束可行性、对偶/影子价格、与贪心策略对比 | SOC 首末相等易写错 |
| C2 | 含紧急购电的单日策略（全年） | 附件1/2、附录1 | 全年策略 + 紧急购电表 | **M-C2** 含紧急购电惩罚的 LP（新增紧急购电变量）＋场景法随机规划对照 | 同上 | 紧急购电触发次数与费用分解、与确定性解对比 | 计费口径（计划 vs 紧急）易混 |
| C3 | 多时点预报下滚动调整策略 | 附件1/2/3 | 计划+调整+紧急购电 | **M-C3** 滚动时域优化（MPC，4 个预报时刻、分段线性罚则） | 同上 | 与 M-C1 的费用对比、预报时刻数敏感性 | 分段线性成本易线性化出错 |
| C4 | 波动电价下重算 C2/C3 | 附件2/3/4 | 两套完整结果 | **M-C4** 波动电价下的 MPC/鲁棒优化（对问题4-2）/ 滚动优化（对问题4-3） | 同上 | 与已知电价的离线最优解间隙、情景鲁棒性 | "已知波动"与"在线决策"的假设选择 |
| D1 | 冲突检测 | 附件1 | 冲突对清单 + 统计 | **M-D1** 区间重叠判定 + 冲突图构建（`O(n²)` 全枚举 + 扫描线加速） | Python（图用 NetworkX） | 与暴力两两判定对拍、边界情形（端点相接） | 占用周期展开的端点约定（`[a,b)` 半开） |
| D2 | 冲突消解方案 | 附件1 | 调整/撤销清单 + 统计 | **M-D2** 多目标分层 MILP（字典序加权）＋**M-D3** 贪心+局部搜索/禁忌启发式对比 | Python + PuLP/Gurobi | 可行解校验（无剩余冲突）、与启发式解对比、权重敏感性 | 多目标量化依据不足会被质疑 |
| D3 | 最多可新增 C 类设备数 | 复用 D2 解 | 数值 + 新增计划表 | **M-D4** 最大可行集模型（在无冲突解上最大化新增数）＋团数/容量**上界证明** | 同上 | MILP 最优性与上界一致性、上界构造的可验证性 | 上界论证是得分关键，必须严谨 |
| D4 | 允许调间隔的消解（**仅部分 C 类**，`|Δm| ≤ 10Δt`） | 附件1 | 调整/撤销清单 | **M-D5** 四参数互斥 MILP（在原 M-D2 上增加 `Δm_i` 与开放集 `S ⊆ C 类`）＋启发式对比 | 同上 | 与 D2 结果对比（撤销数是否下降）、`|Δm|≤10` 与"仅 C 类"硬校验 | 间隔调整使占用整体平移、非局部改变冲突关系，需全量重校验 |
| E1 | 投放策略合理性与时间/假日规律 | 附件1 三表 | 分析结论 + 指标 + 检验 | **M-E1** 描述统计 + 回归/时间序列（假日哑变量、STL 分解）＋**M-E2** 综合评价（熵权/TOPSIS） | Python（pandas/statsmodels） | 回归残差诊断、留出年份回测、权重重排稳健性 | "效益"口径自行定义；创意字段缺失 |
| E2 | 关键词五类划分 | 附件1 Sheet3 | 分类清单 | **M-E3** 成本-效益分位数阈值矩阵（主）＋K-means/GMM 聚类（对比） | Python（sklearn） | 两种方法一致率（ARI）、阈值敏感性 | "无成本无效益"需单独规则 |
| E3 | 预算约束下的投放模型与两段日期策略 | 附件1 + E2 结果 | 每日策略表 | **M-E4** 0-1 整数规划/背包（含单元关联约束）＋**M-E5** 多目标（NSGA-II）对比 | Python + PuLP | 预算与单元约束可行性、与随机分配基线对比 | 关联约束规模；"优越性"需量化 |
| E4 | 2026-09-11~17 策略 + 期望区间 | 附件1 | 策略 + 区间估计 | **M-E6** 预测（SARIMA / 回归 / 树模型，双模型对比）→**M-E7** 蒙特卡洛 + 随机/鲁棒优化（串联） | Python | 用 2025-09 同期做留出集回测（MAPE）；覆盖率检验 | 时间跨度外推风险；模板缺竞价/展现量列 |

> 模型计数复核：每题每个子问题均 ≤ 2 个独立模型体系；A 题 M-A1/M-A2/M-A4 属**同一物理机制（热-湿耦合扩散）在不同边界/域条件下的精度扩展**，按 Skill 规则计为**一个模型族**（基础版、变物性版、移动边界版），仅 M-A3（经验干燥动力学）为独立第二体系，符合"≤2"。

---

## 五、逐题模型与数学定义

### 5.1 A 题　药材的烘干问题

**子问题结论类型**：A1/A2 给双场时空分布（表 1~4 与 `result1/2.xlsx`）；A3/A4 给"烘干所需时间"标量 + 含水率分布表（表 5/6 与 `result3/4.xlsx`）。

#### 模型族 M-A（热-湿耦合扩散，一维圆柱）

控制方程（SI，`r ∈ [0, R(t)]`）：

```
ρ(C)·c_p(C)·∂T/∂t = (1/r)·∂/∂r( k(C)·r·∂T/∂r )          (A-1)
∂C/∂t            = (1/r)·∂/∂r( D(C,T)·r·∂C/∂r )          (A-2)
```

边界条件（第三类）：

```
r = R:  -k·∂T/∂r = h·(T - T_∞(t)),  -D·∂C/∂r = h_m·(C - C_∞(t))
r = 0:  ∂T/∂r = 0,  ∂C/∂r = 0   （对称条件）
```

初始条件：`T(r,0) = T_0 = 28 ℃`，`C(r,0) = C_0 = 2.55 kg/kg`。

> **符号约定（与 `术语表格.md` 对齐）**：本节公式按**题目原文**记号书写（水分扩散系数记作 `D`，药材半径记作 `R(t)`，含水率记作 `C`）。`术语表格.md` 为消除跨题冲突，在符号表中把 A 题的扩散系数统一记为 **`D_m`**（并注明"题目原文记作 `D`"）。论文中二者任选其一，但须全文一致、不得混用。

各问参数取值：

| 问 | 时域 | `T_∞, C_∞` | `ρ, c_p, k, D` |
|---|---|---|---|
| A1（问题1） | `t ∈ [0,1800] s` | 取附件1 插值（241 点，Δt=60 s） | 常数：ρ=820、c_p=2600、k=0.36、`h=25`、`h_m=8e-7`、`D=7e-9·exp(-0.89/C)` |
| A2/A3（问题2、3） | 预热 + 恒温，共 2~3 天 | 前 14400 s 用附件1，之后为恒温段 | 附录3：`ρ=650+128C`；`c_p=1450+2736C/(C+1)`；`k=0.21+0.38C/(C+1)`；`D=2.4e-3·exp(-0.45/C)·exp(-3850/T)` |
| A4（问题4） | 同 A2，域收缩 | 同 A2 | 附录4：`ρ=760+90C`；`c_p=1850+2150C/(C+1)`；`k=0.12+0.20C/(C+1)`；`D=4.2e-4·exp(-0.30/C)·exp(-3850/T)`，`R(t)` 由附件2 插值（2 → 1.873 cm） |

移动边界处理（A4）——无量纲坐标变换：

```
ξ = r / R(t) ∈ [0, 1]
∂C/∂t |_r = ∂C/∂t |_ξ - (ξ · Ṙ(t)/R(t)) · ∂C/∂ξ
(1/r)·∂/∂r( D·r·∂C/∂r ) = (1/R²)·(1/ξ)·∂/∂ξ( D·ξ·∂C/∂ξ )
```

得到固定域 `[0,1]` 上的抛物型方程 + 对流项 `-(ξ Ṙ/R) ∂C/∂ξ`，可用同一隐式差分求解。

**算法步骤（M-A）**

1. 空间离散：N 个节点（建议 N = 201，`Δr = 0.01 cm`），`r=0` 用 L'Hôpital 极限形式 `2·∂²·/∂r²`；
2. 时间离散：隐式 Euler（问题1 步长可选 1 s，与输出粒度一致）或 Crank–Nicolson；
3. 非线性处理：外循环 Picard 迭代，收敛判据 `max|C^(m+1)-C^(m)| < 1e-10`，上限 50 次；每次用上一轮 `C` 更新 `D, ρ, c_p, k`；
4. 线性系统：三对角，用 Thomas 算法（O(N)）；
5. A3：在 `[0, 3 天]` 上对 `max_r C(r,t) < 0.15` 做事件定位（先粗网格扫描 + 线性插值细化）；
6. A4：按 `ξ` 网格求解并映射回物理坐标 `r = ξ·R(t)`。

**输入输出**：输入 = 附件1/2 表格 + 附录参数；输出 = `result1~4.xlsx`（列：0, 0.1, …, 2.0 cm 共 21 列；行：按各问步长）+ 论文表 1~6 的定点值（保留 4 位小数）。

**验证方式与阈值**

| 检验 | 方法 | 通过标准（阈值依据见括注） |
|---|---|---|
| 网格无关性 | N = 101/201/401 三点对比 | 选定量（如 A2 末时刻中心含水率）相对变化 < 0.5%（工程精度经验值） |
| 时间步长收敛 | Δt 减半对比 | 同上 < 0.5% |
| 守恒性 | 对 `∫ρc_pT dV` 与水分总质量做收支审计 | 残差 < 1%（离散格式理论量级） |
| 常系数特例 | 令 `D, k` 为常数，与 Bessel 级数解析解对比 | 最大相对误差 < 1%（差分格式截断误差量级） |
| 与经验模型对比 | A2 的干燥曲线 vs Page 模型拟合 | `R² > 0.98`（文献常用门槛） |

**风险与回退**（已按质检回执 P1-4 修正量级估算）

> 上一版曾据 `D = 2.4e-3·exp(-0.45/C)` 的**单因子**估算断言 `D` 在低含水率段"数值爆炸"（`1.2e-4 m²/s`），该结论**不成立**——它漏掉了附录3 的 Arrhenius 温度因子 `exp(-3850/T)`。补全后复核：
>
> | 工况 | `exp(-0.45/C)` | `exp(-3850/T)` | `D`（m²/s） |
> |---|---|---|---|
> | `C = 2.55`，`T ≈ 301 K`（28 ℃，初始） | 0.838 | 2.79e-6 | **5.6e-9** |
> | `C = 0.15`，`T ≈ 350 K`（77 ℃，干燥后期） | 0.0498 | 1.67e-5 | **2.0e-9** |
>
> 故问题2~4 的 `D` 落在 `~1.5e-9 ~ 6e-9 m²/s`，与文献报告的有效水分扩散系数典型区间 `1e-10 ~ 1e-8 m²/s` 一致（见 §八 A 题文献 5），**属于正常量级，不构成 P0 风险**。

**真正的风险（下调为 P1）与回退**：

1. 问题1 的 `D = 7e-9·exp(-0.89/C)` 随 `C` 下降急剧减小：`C` 从 2.55 降到 0.15 时，`D` 从约 `4.9e-9` 降到约 `1.9e-11 m²/s`（下降约 2 个数量级）→ **后期干燥速率极低**，外层先干、内层水分输运成为限速环节。回退/应对：隐式格式 + Picard 迭代；对 `C < 0.15` 段加密空间网格并适当放大时间步长，同时监控迭代收敛次数。
2. 温度敏感性主要来自 Arrhenius 因子：`T` 由 301 K 升到 350 K 时 `exp(-3850/T)` 由 `2.79e-6` 升到 `1.67e-5`（约 6 倍），须对 `T` 的取值按 K（不是 ℃）代入做单位自检，否则会得到荒诞的 `D` 值。
3. 若移动边界（A4）数值不稳定，回退为"固定域 + 逐时段等效半径"的准稳态迭代，并报告两种处理的偏差。

**候选图表**

- raw：`raw_q1_env_T_C.png`（附件1 环境温湿度曲线）、`raw_q1_radius.png`（附件2 半径随时间）、`raw_q2_params.png`（附录3/4 物性随 C 变化）
- process：`process_q1_gridconv.png`（网格无关性收敛）、`process_q1_picard.png`（Picard 迭代残差）、`process_q2_field3d.png`（T/C 时空演化三维面）、`process_q3_event.png`（终止时刻事件定位）
- result：`result_q1_profile.png`（不同时刻径向温度/含水率剖面）、`result_q2_surface_center.png`（表面与中心干燥曲线）、`result_q3_drytime.png`（达标时间与判据位置）、`result_q4_shrink.png`（收缩前后分布对比）

---

### 5.2 B 题　无线电干扰源的快速自动定位与清除

**子问题结论类型**：B1 给算法 + 直径数值 + 覆盖性结论（能/不能，需证明）；B2 给选点策略与候选区域描述；B3/B4 给策略 + 算法 + 三次正式测试统计表与日志。

#### M-B1 计算几何模型（问题1）

设检测点 `S_i = (x_i, y_i)`，示向度 `θ_i`，误差 `±δ`（`δ = 1°`）。指向 `G` 的两条边界射线方向为 `θ_i ± δ`。四边形 `Q` 由 `S_1` 出发的两条射线与 `S_2` 出发的两条射线的 4 个交点构成（`Q = conv{4 交点}`，命名"定位区域"）。

多边形直径：

```
Diam(Q) = max_{p,q ∈ Q} ||p - q|| = max over vertices of Q  （凸多边形直径由顶点取得）
```

算法（旋转卡壳，O(h)，h 为凸包顶点数）：对凸包按极角排序 → 双指针推进同步旋转 → 记录最大距离。需注意 `Q` 可能退化为无界区域（射线近平行），此时须报告"定位区域无界"，不能输出有限直径。

覆盖性判据：以 `Diam(Q)` 为直径作圆 `Ω`，判别 `Q ⊆ Ω`。一般情况下**不成立**，需构造反例或给出充分条件（例如 `Q` 为矩形时 `Ω` 无法覆盖四个角，宽度/长度比越接近 1 越易失败），并给出"何时成立"的充要/充分条件证明。

**验证**：与 `O(h²)` 顶点对暴力枚举对拍；随机 10⁴ 组算例检验一致性；退化算例（`θ_1 - θ_2 < 2δ`）单独测试。

#### M-B2 检测点选择优化（问题2）

决策变量：`S_2 = (x_2, y_2)`。目标：最小化定位区域面积 `Area(Q(S_2))`（或直径）。约束：`S_2` 在可行域内、与 `S_1` 的示向线夹角足够大（避免退化）。

解析判据：给定 `S_1`、`θ_1`、`δ`，`G` 位于以 `S_1` 为顶点、张角 `2δ`、中轴 `θ_1` 的楔形 `W` 内。对候选 `S_2`，`Area(Q)` 关于 `S_2` 的表达式可由 4 个交点坐标闭式给出，从而构建 `min_{S_2} Area(Q(S_2))`，并用网格搜索 + 局部细化求数值最优，再给出等值线形式的候选区域。

**验证**：网格加密收敛性（最优值随网格细化稳定）；与文献的最优传感器布置判据（AoA 几何精度因子）对照；用多个随机 `G` 检验策略的稳健性（定位区域面积分位数）。

#### M-B3/M-B4 搜索与清除策略（问题3、4）

状态：目标区域离散为概率栅格 `{p_{ij}}`（源存在的后验概率），初始均匀；机器人位置、当前频道、剩余时间。

信息增益（观测效率）的标准做法：对候选动作 `a = (移动至 x, 检测频道 c)`，期望后验熵下降

```
IG(a) = H(P) - E_o[ H(P | o) ]
```

其中观测 `o ∈ {收到示向度, 无信号}`。因观测离散且模型已知，可用"两分支"近似计算，取 `argmax IG(a) / time(a)`（单位时间增益）。获得示向度后，用两条方位线做交会定位，得到定位区域后移动到该区域中心附近并在 ≤20 m 处执行精测 + 清除（含"距离 ≤5 m 且覆盖范围内可直接精测"的特例）。

时间模型（必须与题面附录2 与附件1 计时规则一致）：

```
time(a) = 直线距离 / 5                        # 移动：5 m/s
        + 1 · 1[本次检测发生了频道切换]        # 切换：固定 1 s/次，与 |Δ频道| 无关
        + 5                                   # 检测：调整天线朝向并获得稳定读数
        [+ 3 精测 + 2 清除]                    # 仅对目标频道执行清除动作时计入
```

> **易错点（已按质检回执 P0-1 修正）**：题面附录2 第(4)条为"**任意两个频道之间的切换时间为 1 秒**"，即**每次切换固定 1 s**，而非 `1 s × |Δ频道|`。若误按编号差线性计价，会系统性放大跨频道跳变的代价并改变动作序，直接歪曲 B3/B4 的最优策略。

**对比模型 M-B3'（分区覆盖启发式）**：把圆域按有效接收半径（1000~1500 m）划分子区，采用螺旋/割草机路径逐区扫描；一旦获得示向度即插入交会定位与清除。

**M-B4 扩展**：定向源存在 ±90° 无信号扇区 → 观测模型变为三值（示向度 / 无信号（超距）/ 无信号（角度外）），需引入"角度外"假设分支并在无信号时降低该位置对若干方向的概率，策略上增加"沿疑似定向轴反向排查"的动作。

**验证方式与阈值（B3/B4 —— 已按质检回执补量化阈值）**

| 检验 | 方法 | 通过标准（阈值依据） |
|---|---|---|
| 清除完整性 | 演练测试结束后模拟器给出源总数，核对已清除数 | 清除比例 = 100%（问题3 明确要求"确保所有干扰源被清除"，故为硬指标，不允许缺口） |
| 时间预算 | 记录 `/enter` 到 `/exit` 的程序运行时间 | ≤ 20 min 且 ≤ 25 min 窗口（题面硬约束）；目标 ≤ 15 min 留出安全裕度 |
| 稳定性（可提交判据） | 连续演练 N = 20 次统计清除比例与平均定位清除时间 | 清除比例 20/20 且平均时间的变异系数 `CV < 20%` 才提交正式测试（经验值，用于判断策略是否收敛） |
| 策略对比 | M-B3（信息增益）与 M-B3'（分区覆盖）在各 20 次演练中的平均值 | 主模型平均定位清除时间需优于对比模型；若劣于，须分析原因或改用对比模型 |
| 泛化性 | 演练案例按干扰源个数（10~16）分组统计 | 各组清除比例均为 100%，不出现小规模好、大规模崩的断层 |
| 退化/边界 | 人为构造源位于区域边缘、两源距离小于有效接收半径、频道相邻等情形 | 不得出现死循环；虚拟时间不得触及 100 h 上限 |

**风险与回退**：无离线数据，所有验证只能依赖演练测试；若信息增益计算耗时导致 20 min 程序预算不足，回退为"离线预计算分区路径 + 在线贪心插入清除"的轻量策略。若正式测试机会耗尽仍未取得 3 次有效结果，按 §七 P0 行走受限交付。

**候选图表**：`raw_q3_region.png`（目标区域与有效接收半径示意）、`raw_q3_case.png`（某案例源分布，演练模块可得）、`process_q3_probgrid.mp4/png`（概率栅格演化）、`process_q3_igmap.png`（信息增益热图）、`process_q4_blind.png`（定向源无信号扇区）、`result_q3_trace.png`（机器狗轨迹）、`result_q3_success.png`（清除比例/平均时间统计）、`result_q4_compare.png`（全向 vs 定向对比）。

---

### 5.3 C 题　微网与外部电网电力调控策略

**子问题结论类型**：均给"策略表"（购电量、充放电量、紧急购电量）+ 费用指标。

#### M-C1 日前线性规划（问题1）

集合与索引：时段 `t = 1..144`（对应模板 `0:10-0:20 … 0:00-0:10+1`），时长 `Δt = 1/6 h`。

变量：`g_t ≥ 0`（该时段**计划购电量**，kWh）、`ch_t ≥ 0`（充电量，kWh）、`dis_t ≥ 0`（放电量，kWh）、`E_t`（时段末储电量，kWh，`E_0 = 6000`）。时段时长 `Δt = 1/6 h`。

> **量纲约定（C 题全文唯一口径，与 `术语表格.md` §三 一致 —— 已按质检回执 P1-1 修正）**：所有购电/充放电变量一律取**电量（kWh）**口径，不用功率口径。依据：附件列名与结果模板列名（`购电量`、`充电量`、`放电量`、`储电量`）全部是 kWh，采用电量口径可直接落在模板上且避免同一符号两套量纲。题给"最大充放电功率 5000 kW"折算为**单时段电量上界** `5000 × Δt = 833.333 kWh`；负载与光伏给的是功率（kW），故需乘 `Δt` 折算为电量后再进入平衡式。

目标：

```
min  Σ_t  p_t · g_t          # p_t[元/kWh] × g_t[kWh] = 元
```

约束：

```
供电量不低于负载（功率平衡折算为电量）:
  g_t + dis_t - ch_t  ≥  (L_t - PV_t)·Δt                       (C-1)
充放电互斥与单时段电量上界（5000 kW × Δt = 833.333 kWh）:
  0 ≤ ch_t ≤ 833.333,  0 ≤ dis_t ≤ 833.333
  ch_t · dis_t = 0  ⟺  ch_t ≤ 833.333·u_t, dis_t ≤ 833.333·(1-u_t), u_t ∈ {0,1}   (C-2)
SOC 递推与效率（kWh）:
  E_t = E_{t-1} + η·ch_t - dis_t/η,  η = 0.90                  (C-3)
SOC 界限与首末相等:
  1200 ≤ E_t ≤ 10800,  E_144 = E_0 = 6000
```

> 说明：若 `ch_t·dis_t = 0` 在最优解中自动成立（因同时充放电严格劣），可用连续 LP 求解并事后校验，避免引入 0-1 变量；这是**需要数值验证的建模判断**，不能默认成立。输出需按模板给出 144 时段购电量与 6 个 4 h 段的充/放电量与 0:00、24:00 储电量。

#### M-C2 含紧急购电的 LP（问题2）

新增变量 `e_t ≥ 0`（紧急购电量，kWh），结算规则：

```
计划购电费 = Σ_t p_t · g_t
紧急购电费 = Σ_t 5·p_t · e_t
缺口关系（电量口径）:  e_t ≥ (L_t - PV_t)·Δt + ch_t - dis_t - g_t
```

其余同 M-C1；逐日独立求解（334 天，2025-02-01~12-31）。

#### M-C3 滚动时域优化（问题3）

4 个预报时刻 `τ ∈ {0, 6, 12, 18}`，各给出未来 24 h 整点预报。策略：

- `0:00`：按预报制定**计划购电量** `g^plan_t`；
- 其后时刻：按新预报制定**调整购电量** `g^adj_t`，费用规则

```
超出部分（g^adj > g^plan）: 1.5 · p_t · (g^adj - g^plan)     # 电价 ×1.5
低于部分（g^adj < g^plan）: 0.5 · p_t · (g^plan - g^adj)     # 违约电价 ×50%
紧急购电:                 5   · p_t · e_t                    # 电价 ×5
```

以"计划 + 调整 + 紧急"总费用最小为目标，分阶段顺序求解（计划阶段用期望预报，调整阶段用更新预报）；并需回答"是否需要引入其他时刻的预报"——通过**增设预报时刻的消融实验**（8 次/12 次预报 vs 4 次）比较总费用下降幅度来回答，而非定性断言。

#### M-C4 波动电价的扩展（问题4）

`附件4` 给出全年逐 10 min 电价序列。两种建模路径须**显式择一并声明**：

- 路径一（确定性）：把附件4 电价视为当天已知 → 直接复用 M-C2/M-C3 的 LP/MPC；
- 路径二（在线决策）：`0:00` 时电价未知 → 用历史电价做预测 + 场景法/鲁棒优化。

建议：**主模型取路径一**（与附件数据一致、可复现），并以路径二做对照与灵敏度分析，从而把假设 C-R4 的放宽方式落实为可验证的实验。

**验证方式与阈值**

| 检验 | 方法 | 通过标准 |
|---|---|---|
| 约束可行性 | 逐时段检查 C-1~C-3 与 SOC 界限 | 违反数 = 0（硬约束，任一违反即不合格） |
| 充放电互斥 | 统计 `min(ch_t, dis_t) > 0` 的时段数 | 0 个（若用连续 LP 则必须为 0，否则改用 MILP） |
| 求解器一致性 | `scipy.linprog`(HiGHS) 与 PuLP/Gurobi 对同一天求解 | 目标值相对差 < 1e-6 |
| 与朴素策略对比 | 无储能 / 简单阈值充放电策略 | 费用下降幅度需报告（无固定阈值，作为效益证据） |
| 预报时刻消融 | 4 次 vs 8 次预报 | 报告费用差与边际收益拐点 |

**风险与回退**：334 天逐日求解的计算量（334×144 变量）需向量化实现，若单日 LP 过慢则改用解析最优的阈值结构（电价低充、高放）并仅在边界处求解 LP。

**候选图表**：`raw_q1_price_load_pv.png`（电价/负载/光伏三线图）、`raw_q2_year_heatmap.png`（全年日-时段热力图）、`process_q1_soc.png`（SOC 轨迹）、`process_q2_tradeoff.png`（购电与紧急购电的时段分布）、`process_q3_forecast_update.png`（4 个预报时刻的预报更新）、`result_q1_schedule.png`（144 时段策略柱状图）、`result_q2_cost_split.png`（费用分解堆叠图）、`result_q3_adjust.png`（计划 vs 调整差异）、`result_q4_compare.png`（固定 vs 波动电价费用对比）。

---

### 5.4 D 题　时频冲突检测与消解

**子问题结论类型**：D1 给冲突对清单 + 统计；D2/D4 给调整/撤销清单 + 统计表；D3 给最大新增数量 + 新增计划表（并需给出上界论证）。

统一记号：计划 `i` 参数 `(f_i^s, f_i^e)`、`(t_i^s, t_i^e)`、间隔 `m_i`、次数 `n_i`。取半开区间约定（与题面 `[80,90)` 一致）。

#### M-D1 冲突检测与冲突图

占用展开：

```
O_i = { [t_i^s + k·(m_i + (t_i^e - t_i^s)), t_i^e + k·(m_i + (t_i^e - t_i^s))) }_{k=0..n_i-1}
```

冲突判定（半开区间，端点相接不算冲突）：

```
conflict(i, j) ⟺ [f_i^s, f_i^e) ∩ [f_j^s, f_j^e) ≠ ∅  AND  ∃ a∈O_i, b∈O_j : a ∩ b ≠ ∅
```

冲突图 `G = (V, E)`，`|V| = 150`，`E = {(i,j) : conflict(i,j)}`。实现用 O(n²) 双循环即可（`150² = 22500`），无须高级算法；扫描线仅作为加速对照。

**理论结构（用于消解与上界）**：把二维交叠拆成两个一维交叠条件——`G = G_time ∩ G_freq`，其中 `G_freq` 是频率区间的交叠图（**区间图**，每个计划一个区间），`G_time` 是各计划时间占用**集合**的交叠图（每计划至多 `n_i` 个区间，即"多区间图"）。因此 `G` 是两个区间图的交，其 boxicity ≤ 2。这一结构可用于：① 说明 `G_freq` 上的团–着色对偶（区间图是完美图）；② 为问题3 的容量上界提供理论工具。**注意**：该框架的精确定理表述须在编程/论文阶段打开下列文献核对后再引用（见 §八 D 题文献 3、4）。

#### M-D2 多目标分层 MILP（问题2）

变量：`u_i ∈ {0,1}`（撤销）、`Δf_i ∈ [-10,10]`（频段平移）、`Δt_i ∈ [-5,5]`（时间平移），互斥约束：

```
u_i + 1[Δf_i ≠ 0] + 1[Δt_i ≠ 0] ≤ 1
```

目标（按**题面给定次序**分层，已按质检回执 P2-5 修正）：

```
min  ( W1·Σ_i u_i ,                                   # ① 尽可能减少撤销
       W2·Σ_i 1[u_i = 0 且 (α_i + β_i) = 1] ,          # ② 尽量减少被调整的计划数量
       W3·Σ_{i ∈ 高优先级} 1[u_i + α_i + β_i ≥ 1] ,    # ③ 高优先级计划应尽量保持（A>B>C）
       W4·Σ_i (|Δf_i| + |Δt_i|) )                      # ④ 尽量减少调整幅度
```

权重取严格递减 `W1 ≫ W2 ≫ W3 ≫ W4`，即字典序加权；题面次序为"少撤销 > 少调整数量 > 保优先级 > 减小幅度"，**不可把③与④互换**。为线性化"仅一个参数可调"，引入 0-1 指示变量 `α_i`（调频段）、`β_i`（调时间）与 `α+β+u ≤ 1`，并用 `-10α ≤ Δf ≤ 10α`、`-5β ≤ Δt ≤ 5β` 把不调参的计划固定。

冲突约束的对偶化处理：对每条冲突边 `(i,j)`，需构造"新参数下不再冲突"的析取约束 `¬conflict(i,j)`；因 `Δf, Δt` 为整数有界，可**枚举每对计划的所有平移组合**（`21×11 = 231` 种/计划）并预计算"该组合是否仍冲突"的 0-1 表，再用大 M 或指示约束表达。若枚举规模过大，改用"禁止组合"割平面的启发式迭代。

#### M-D3 启发式（对比模型）

贪心：按"冲突度/优先级"排序，逐个将冲突最多的计划做最小幅度平移，失败则撤销；局部搜索/禁忌搜索改进。用于与 MILP 对比，证明 MILP 解的质量。

#### M-D4 最大新增（问题3）与上界

在 D2 所得无冲突解 `P*` 上，为新增 C 类计划设变量 `(f^s, f^e, t^s)`，最大化 `Σ y_k`。**上界论证**（得分关键，至少给出一种）：

1. **资源容量上界**：把 `[0, F)` 频段 × `[0, T)` 时间的占用面积求和，`Σ_i (宽度_i × 总占用时长_i) ≤ F × T`，由此得新增数量的松弛上界；
2. **团数上界**：在任一时频"单元"（某频段 × 某时间格）上同时可容纳的计划数有限，用最大团给出局部上界；
3. **区间图/boxicity 工具**：借助 `G_freq` 为区间图（完美图）的性质给出更紧的界。

MILP 的最优值应与上述上界做一致性校验（`MILP ≤ 上界`，差距报告为"界间隙"）。

**验证方式**：冲突零残留（硬校验，违反数必须为 0）；`result*.xlsx` 逐行回代入冲突判定函数；与启发式解的目标值对比；权重敏感性（`W` 变动后方案是否稳定）；上界与最优值的间隙。

**风险与回退**：多目标量化的主观性会被质疑 → 必须给出权重依据（如"撤销一次等价于 5 次最小幅度调整"的比值论证 + 敏感性分析）；若 MILP 规模不可解，回退为"分块/分层求解 + 启发式补全"并在论文中报告间隙。

#### M-D5 允许调整间隔时长的消解（问题4）—— 已按质检回执 P1-2 补齐

题面原文："若允许**部分 C 类**用频装备对用频**间隔时长**进行调整，但要求与原间隔时长的**差异不超过 10Δt**"。因此相对于 M-D2 的增量约束为：

```
仅对 C 类、且仅对其中被选中的"部分"计划开放间隔调整：
  开放集 S ⊆ C 类；i ∉ S 时 Δm_i = 0
间隔调整幅度上限：
  |Δm_i| ≤ 10,  i ∈ S                        (M-D5-1)
参数互斥（题面"只能调整其中一个参数或撤销"）扩展为四选一：
  u_i + α_i + β_i + γ_i ≤ 1,  γ_i = 1[Δm_i ≠ 0]   (M-D5-2)
  -10α_i ≤ Δf_i ≤ 10α_i,  -5β_i ≤ Δt_i ≤ 5β_i,  -10γ_i ≤ Δm_i ≤ 10γ_i
新占用展开（间隔改变后占用位置整体平移，须重新生成 O_i）：
  O_i(Δm_i) = { [t_i^s + k·(m_i+Δm_i+ L_i), t_i^e + k·(m_i+Δm_i+ L_i)) },  L_i = t_i^e - t_i^s
```

> **注意**：`Δm_i ≠ 0` 会使该计划**全部**后续占用整体平移，冲突关系发生非局部变化，不能只检查相邻两次占用；须对每条冲突边重新判定。同时题面要求"对问题1 中检测出的时频冲突进行消解"，即问题4 的冲突集仍以问题1 的检测结果为准。

目标函数沿用 M-D2 的四层次序；`|Δm_i|` 计入第④层"调整幅度"。**输出**：`result4.xlsx` 的 5 列（用频装备编号、调整后频段范围、调整后时间区间、调整后间隔时长、是否撤销用频计划）。

**验证方式**：与 M-D2 的结果对比（撤销数是否下降、调整数是否上升）；`|Δm_i| ≤ 10` 与"仅 C 类、仅部分"两项硬校验（违反数必须为 0）；间隔调整后的冲突零残留。

**风险与回退**：开放集 `S` 的选取本身是决策的一部分，若把 `S` 也作为 0-1 变量会显著增大规模 → 回退为"先按冲突度排序贪心选择 `S`，再在给定 `S` 下解 MILP"，并报告该启发式对最优值的影响。

**候选图表**：`raw_q1_plan_hist.png`（频段/时间/次数分布直方图）、`raw_q1_occupancy.png`（时频占用矩阵热力图）、`process_q2_conflictgraph.png`（冲突图与度分布）、`process_q2_ilp_bound.png`（求解界收敛曲线）、`process_q3_upperbound.png`（上界构造示意）、`process_q4_dm.png`（间隔调整幅度分布）、`result_q2_gantt.png`（消解前后甘特图对比）、`result_q2_stats.png`（A/B/C 保留/调整/撤销统计）、`result_q3_added.png`（新增计划布置图）、`result_q4_compare.png`（调间隔前后撤销数对比）。

---

### 5.5 E 题　SEM 广告投放策略

**子问题结论类型**：E1 给分析结论与规律（含假日效应检验）；E2 给分类清单；E3/E4 给逐日投放策略表（含预期指标区间）。

#### M-E1/M-E2 效益规律与策略合理性（问题1）

- 指标构造：`CTR = 点击量/展现量`、上方位占比 `= 上方位展现量/展现量`、上方位点击占比、单位成本 `= 消费额/点击量`、日效益 `= 新注册数`（Sheet2）、`ROI = 新注册数/消费额`。
- 规律分析：对日序列做 STL 分解（趋势/周季节/残差）+ 回归 `注册数 ~ 假日哑变量 + 星期 + 方案 + 推广单元 + 消费额`，检验假日效应显著性与方向；对"效益与投放时间"用日-小时？——注意 Sheet1 只有日粒度，故"投放时间"只能落到**日期/星期/假日**层面，须在论文中说明粒度限制。
- 综合评价（M-E2）：以熵权法或 CRITIC 客观赋权，再用 TOPSIS 对"方案 × 推广单元"排序，评价投放结构合理性。

> "设计质量与创意"无数据字段支撑 → 退化为按方案/推广单元层次的代理分析，**必须在论文中明确声明局限**（对应 X9）。

#### M-E3 五类关键词划分（问题2）

先构造成本指标 `c_k = 消费额_k`、效益指标 `b_k = f(点击量, 浏览量, 跳出率, 平均访问时长)`（如 `b_k = 点击量 × (1 - 跳出率) × 归一化平均访问时长`，须声明构造依据）。主模型用分位数阈值矩阵：

```
低成本: c_k ≤ q_50(消费额>0)     高成本: 其余
高效益: b_k ≥ q_50(b)            低效益: 其余
无成本且无效益: c_k = 0 且 b_k = 0
```

阈值取中位数（依据：题面"高/低"为二元相对判断且未给绝对标准，故取稳健分位数；须做 `q_30/q_50/q_70` 敏感性）。对比模型用 K-means（或 GMM）在 `(log c, b)` 平面聚类，报告两法一致率（ARI）。

#### M-E4 预算约束下的选词与投放（问题3）

```
max  Σ_k  v_k · y_k                     (v_k = 预测单位投入效益)
s.t. Σ_k  c_k · y_k · x_{k,d} ≤ B_d    （日预算）
     Σ_d  ... ≤ B_total ≤ 2025 年总投入
     y_k ∈ {0,1}, 关联约束: 同推广单元内关键词选择满足协同/互相依赖规则
     每个推广单元满足"低成本、高效益"约束
```

输出 2025-02-01~08 与 2025-08-01~08 每日策略（`result3.xlsx` 列：日期、方案ID、推广单元、关键词、投入金额、预期展位、预期点击量、预期浏览量、预期注册量）。

"优越性"必须量化：与 `2025 年同期实际`、`均匀分配预算`、`仅投黄金词` 三种基线对比 ROI/注册量。

#### M-E5 多目标对比模型（问题3 的第二体系 —— 已补齐定义）

M-E4 把"低成本、高效益、不超预算"压缩为单目标（加权效益）并施以约束。为验证该压缩不掩盖权衡结构，**并行**建立多目标版本：

```
目标向量：  min  ( 总投入成本  Σ_k c_k·y_k ,
                  max 预期注册量 Σ_k reg_k·y_k ,
                  最小化未达预算的下偏差 )
              （效益系数用 `reg_k` 而非 `r_k`，以免与 A 题径向坐标 `r` 冲突，见 `术语表格.md` §五/§七）
求解：      NSGA-II（种群 100、代数 200、交叉 0.9、变异 0.05）
输出：      Pareto 前沿 + 用 TOPSIS 选出折衷解
```

**用途与边界**：M-E5 只用于**对照与解释**（说明 M-E4 的单目标加权解在 Pareto 前沿上的位置），不作为权威结果；`result3.xlsx` 以 M-E4 的解为准。两模型的解应相互印证，若差异显著，须检查权重设定 `v_k` 的合理性。

#### M-E6/M-E7 2026 年预测与最优策略（问题4）

串联：先用 2025 数据预测 2026-09-11~17 的竞价、展现量、展现位、点击量、浏览量、注册量（双模型对比：季节回归/指数平滑 vs 树模型；用 2025-09 同期做留出回测，报告 MAPE），再用蒙特卡洛抽样 + 场景优化给出策略与各指标的期望区间（分位数区间）。

**注意 X8**：`result4.xlsx` 无"竞价""展现量"列，故竞价与展现量的期望范围须在论文正文以表格给出，模板只填其要求的 9 列。

**验证方式与阈值**

| 检验 | 方法 | 通过标准 |
|---|---|---|
| 分类稳健性 | 分位数法 vs 聚类的一致率 | ARI > 0.6（中等以上一致，经验值） |
| 预算可行性 | 逐日/总计核算 | 超预算 = 0 |
| 预测精度 | 2025-09 留出集 | MAPE < 20%（月尺度业务预测常用阈值） |
| 区间覆盖率 | 蒙特卡洛区间对留出期的覆盖率 | 90% 名义区间实测覆盖 85%~95% |
| 策略优越性 | 三种基线对比 | 报告 ROI 提升幅度；无提升则该结论不成立，须改写 |

**风险与回退**："效益"定义是本唯一的主观核心 → 必须做**多口径稳健性检验**（至少两种效益口径下分类与策略结论一致）；若结论对口径敏感，则在论文中如实报告敏感性而非强行给单一结论。

**候选图表**：`raw_q1_daily.png`（日消费与注册量双轴）、`raw_q2_keyword_scatter.png`（成本-效益散点）、`process_q1_stl.png`（STL 分解）、`process_q2_cluster.png`（聚类与阈值边界）、`process_q3_budget.png`（预算分配过程）、`process_q4_mc.png`（蒙特卡洛抽样分布）、`result_q1_weights.png`（熵权/TOPSIS 权重与排名）、`result_q2_quadrant.png`（五类关键词四象限图）、`result_q3_schedule.png`（两段日期每日投放策略）、`result_q4_interval.png`（2026 各指标期望区间）。

---

## 六、编程依赖

| 题 | 语言 | 关键库/工具 | 求解器 | 阻塞风险 |
|---|---|---|---|---|
| A | Python（首选）或 MATLAB | `numpy`, `scipy`, `pandas`, `openpyxl` | 自实现 Thomas 算法 | 无（纯数值） |
| B | Python | `requests`（HTTP+JSON）、`numpy`、`shapely`(可选)、`matplotlib` | 无 | **模拟器在线可用性、25 min 窗口、3 次正式测试机会**；截止 2026-09-13 17:30 |
| C | Python | `pandas`, `numpy`, `pulp` 或 `scipy.optimize.linprog` | HiGHS（scipy 内置）/ CBC（PuLP）/ Gurobi（可选） | 334 天逐日求解的计算耗时 |
| D | Python | `pandas`, `networkx`, `pulp` | CBC/PuLP 或 Gurobi | MILP 规模（150 计划 × 平移枚举）需控制 |
| E | Python | `pandas`, `scikit-learn`, `statsmodels`, `pulp`, `matplotlib` | CBC/PuLP | 无 |

> Skill 强制协议第 3 条：已有脚本与模板必须优先调用。本阶段已使用 `tools/paper_search/scripts/hybrid_scholar.py`；后续 PDF/Excel/绘图/文档生成须分别读取 `tools/pdf|figure|xlsx|docx/SKILL.md` 后再执行。

---

## 七、风险与回退条件（总体）

| 优先级 | 风险 | 触发条件 | 回退方案 |
|---|---|---|---|
| P1 | A 题后期干燥速率极低（问题1 的 `D` 随 `C` 由 2.55 降到 0.15 而下降约 2 个数量级，见 §5.1 风险栏） | 时间步内含水率变化趋零、Picard 迭代收敛变慢，或算得总时长显著偏离题面 2~3 天 | 隐式格式 + 加密 `C < 0.15` 段空间网格；若总时长异常，优先复核 `D` 表达式中的温标（必须用 K）与单位 |
| P0 | B 题无法及时完成三次正式测试 | 距 2026-09-13 17:30 不足，或模拟器/网络异常 | 立即按 Skill 交付协议走"受限交付"，用演练日志 + 已有正式测试结果作答，并在论文中如实说明测试次数 |
| P0 | C 题 SOC 首末相等 / 紧急购电口径写错 | 约束校验出现违反或费用结构不符 | 以模板列定义为准重建约束；逐项对照题面表格 |
| P1 | D 题 MILP 求解超时 | 枚举组合或割平面规模失控 | 分层求解（先撤销决策再平移）+ 启发式补全，报告界间隙 |
| P1 | D 题问题3 上界论证不严谨 | 只有数值解、无证明 | 至少给出资源容量上界（可严格推导）并明确标注为松弛界 |
| P1 | E 题结论对"效益"口径敏感 | 多口径下分类/策略结论不一致 | 论文中如实报告敏感性，给出主口径并说明选择依据 |
| P1 | A 题输出体量巨大：问题2 按**读法①（全过程 @1 s）≈17.3 万~25.9 万行 × 21 列**，按**读法②（仅 3 h @1 s）为 10801 行 × 21 列**（读法辨析见 X12） | 生成 xlsx 超时、内存溢出或文件过大 | 分块写入 + 边界批量刷新；先写 CSV 再按需转换；若 xlsx 写入失败则回退为读法②并在论文中说明依据（触发条件见 X12 的待确认结论） |
| P2 | C 题 `result2/3` 多日模板需自行扩展 334 天 | 模板仅 20/26 行示例 | 按模板列头与日期范围程序化生成表体 |

---

## 八、文献依据

> **检索方式**：`tools/paper_search/scripts/hybrid_scholar.py`（OpenAlex + AnySearch 双引擎）。
> **可追溯性状态**：下列条目的题名、作者、年份、DOI 均由 OpenAlex 结构化元数据返回，并与 AnySearch 交叉命中（标注"交叉验证"）；**期刊/会议、卷期页信息部分条目检索源未返回**（如 A 题第 3 条缺卷页、B 题第 1/2 条缺期刊名），已在条目内注明为"待补"。**全部条目尚未逐条打开 DOI 出版页面做人工核对**，依据 Skill 核验规则第 2 条，须在论文引用前完成人工核对；未核对前不得写入正式论文参考文献表。
> **混杂项说明**：检索中出现的若干高被引但与主题无关的条目（如 LeNet、quickhull、MAX-CUT）已按"相关性优先于引用量"原则剔除，未纳入下表；`interval graph` 专项检索相关性不足，未强行采用其返回结果。

### A 题

1. Tao, Y., Li, D., Chai, W. S., Show, P. L., et al. (2020). *Comparison between airborne ultrasound and contact ultrasound to intensify air drying of blackberry: Heat and mass transfer simulation, energy consumption and quality evaluation.* Ultrasonics Sonochemistry, 72, 105410. DOI: 10.1016/j.ultsonch.2020.105410（交叉验证）——**非稳态热质耦合 + 变扩散系数 + 收缩**的完整建模范例。
2. Defraeye, T., Blocken, B., & Carmeliet, J. (2011). *Analysis of convective heat and mass transfer coefficients for convective drying of a porous flat plate by conjugate modelling.* International Journal of Heat and Mass Transfer, 55(1-3), 112-124. DOI: 10.1016/j.ijheatmasstransfer.2011.08.047（交叉验证）——**对流干燥表面换热/传质系数**的处理依据。
3. Janjai, S., Mahayothee, B., Lamlert, N., Bala, B. K., et al. (2009). *Diffusivity, shrinkage and simulated drying of litchi fruit.* Journal of Food Engineering.（卷期页 **待补**）DOI: 10.1016/j.jfoodeng.2009.07.015（交叉验证）——**收缩 × 有效扩散系数**联合建模。
4. Rani, P., & Tripathy, P. P. (2020). *Modelling of moisture migration during convective drying of pineapple slice considering non-isotropic shrinkage and variable transport properties.* Journal of Food Science and Technology. DOI: 10.1007/s13197-020-04407-4（交叉验证）——**变输运性质 + 收缩**的数值实现参考。
5. Khan, M. I. H., Kumar, C., Joardder, M. U. H., & Karim, A. (2016). *Determination of appropriate effective diffusivity for different food materials.* Drying Technology. DOI: 10.1080/07373937.2016.1170700（交叉验证）——**有效扩散系数量级合理性判据**（用于校验题给 `D` 是否异常）。

### B 题

1. Zhang, Y. D., Amin, M. G., & Kaushik, S. (2007). *Localization and Tracking of Passive RFID Tags Based on Direction Estimation.*（期刊名检索源未返回，**待补**）DOI: 10.1155/2007/17426（交叉验证）——方位估计定位的经典处理。
2. Moses, R. L., Krishnamurthy, D., & Patterson, R. (2003). *A Self-Localization Method for Wireless Sensor Networks.*（期刊名检索源未返回，**待补**）DOI: 10.1155/s1110865703212063（交叉验证）——几何定位与可观测性分析。
3. Herath, S. C. K., & Pathirana, P. N. (2013). *Optimal Sensor Arrangements in Angle of Arrival (AoA) and Range Based Localization with Linear Sensor Arrays.* Sensors, 13(9), 12277. DOI: 10.3390/s130912277（交叉验证）——**最优检测点布置**（直接支撑问题2）。
4. Moreno-Salinas, D., Pascoal, A., & Aranda, J. (2013). *Sensor Networks for Optimal Target Localization with Bearings-Only Measurements in Constrained Three-Dimensional Scenarios.* Sensors, 13(8), 10386. DOI: 10.3390/s130810386（交叉验证）——仅测向（bearings-only）最优布站。
5. Almadhoun, R., Taha, T., Seneviratne, L., & Zweiri, Y. (2019). *A survey on multi-robot coverage path planning for model reconstruction and mapping.* SN Applied Sciences. DOI: 10.1007/s42452-019-0872-y（交叉验证）——覆盖路径规划综述。
6. Tan, C. S., Mohd-Mokhtar, R., & Arshad, M. R. (2021). *A Comprehensive Review of Coverage Path Planning in Robotics Using Classical and Heuristic Algorithms.* IEEE Access, 9. DOI: 10.1109/access.2021.3108177（交叉验证）——覆盖搜索算法全景。
7. Peña Queralta, J., Taipalmaa, J., Pullinen, B. C., Sarker, V. K., et al. (2020). *Collaborative Multi-Robot Search and Rescue: Planning, Coordination, Perception, and Active Vision.* IEEE Access, 8. DOI: 10.1109/access.2020.3030190（交叉验证）——**搜索—规划—感知**协同框架，用于把"策略"上升为有理论支撑的决策模型。

### C 题

1. Fernández García, F. R., & Bordons, C. (2013). *Optimal economic dispatch for renewable energy microgrids with hybrid storage using Model Predictive Control.* IECON 2013. DOI: 10.1109/iecon.2013.6700458（交叉验证）——微网 MPC 经济调度奠基性工作。
2. Sachs, J., & Sawodny, O. (2016). *A Two-Stage Model Predictive Control Strategy for Economic Diesel-PV-Battery Island Microgrid Operation in Rural Areas.* IEEE Transactions on Sustainable Energy. DOI: 10.1109/tste.2015.2509031（交叉验证）——两阶段 MPC 结构，可对应"计划 + 调整"两阶段。
3. Zheng, Y., Li, S., & Tan, R. (2017). *Distributed Model Predictive Control for On-Connected Microgrid Power Management.* IEEE Transactions on Control Systems Technology. DOI: 10.1109/tcst.2017.2692739（交叉验证）——并网微网功率管理。
4. Han, Y., Zhang, K., Li, H., Coelho, E. A. A., et al. (2017). *MAS-Based Distributed Coordinated Control and Optimization in Microgrid and Microgrid Clusters: A Comprehensive Overview.* IEEE Transactions on Power Electronics. DOI: 10.1109/tpel.2017.2761438（交叉验证）——微网优化调度综述。

### D 题

1. Aardal, K., van Hoesel, S. P. M., Koster, A. M. C. A., Mannino, C., et al. (2007). *Models and solution techniques for frequency assignment problems.* Annals of Operations Research. DOI: 10.1007/s10479-007-0178-0（交叉验证）——**频率指配问题（FAP）建模与求解的权威综述**，本题消解模型的主要理论依据。
2. Park, T., & Lee, C. Y. (1996). *Application of the graph coloring algorithm to the frequency assignment problem.* Journal of the Operations Research Society of Japan. DOI: 10.15807/jorsj.39.258（交叉验证）——**图着色 → 频率指配**的直接映射。
3. Gyárfás, A. (1985). *On the chromatic number of multiple interval graphs and overlap graphs.* Discrete Mathematics. DOI: 10.1016/0012-365x(85)90044-5（交叉验证）——**多区间图（多次占用）色数**性质，支撑问题3 的上界分析。
4. Bellantoni, S. J., Hartman, I. B.-A., Przytycka, T. M., & Whitesides, S. (1993). *Grid intersection graphs and boxicity.* Discrete Mathematics. DOI: 10.1016/0012-365x(93)90354-v（交叉验证）——**boxicity / 网格交图**，支撑"时频二维交叠 = 两个区间图之交"的结构刻画。
5. Titiloye, O., & Crispin, A. (2011). *Quantum annealing of the graph coloring problem.* Discrete Optimization. DOI: 10.1016/j.disopt.2010.12.001（交叉验证）——图着色的现代求解思路（作为启发式对照）。
6. Lueker, G. S., & Booth, K. S. (1979). *A Linear Time Algorithm for Deciding Interval Graph Isomorphism.* Journal of the ACM. DOI: 10.1145/322123.322125（OpenAlex 独有）——区间图识别的线性算法（用于确认 `G_freq` 的区间图性质）。

### E 题

1. Symitsi, E., Markellos, R. N., & Mantrala, M. K. (2022). *Keyword portfolio optimization in paid search advertising.* European Journal of Operational Research. DOI: 10.1016/j.ejor.2022.03.006（交叉验证）——**投资组合理论做关键词预算分配**，直接对应问题3。
2. Li, H. A., Kannan, P. K., Viswanathan, S., & Pani, A. (2016). *Attribution Strategies and Return on Keyword Investment in Paid Search Advertising.* Marketing Science. DOI: 10.1287/mksc.2016.0987（交叉验证）——**关键词投入回报的归因**，支撑"效益"指标构造。
3. Lu, X., & Zhao, X. (2014). *Differential Effects of Keyword Selection in Search Engine Advertising on Direct and Indirect Sales.* Journal of Management Information Systems. DOI: 10.2753/mis0742-1222300411（交叉验证）——关键词选择对销售的差异化效应。
4. Bayer, E., Srinivasan, S., Riedl, E. J., & Skiera, B. (2020). *The impact of online display advertising and paid search advertising relative to offline advertising on firm performance and firm value.* International Journal of Research in Marketing. DOI: 10.1016/j.ijresmar.2020.02.002（交叉验证）——付费搜索的效益度量。

### 来自 Skill 算法资料库的经典文献（书籍/奠基文献，无 DOI，可追溯至 `assets/*.md`）

- 线性规划：Dantzig, G. (1963). *Linear Programming and Extensions.* Princeton University Press.（`assets/01`）
- 整数规划：Wolsey, L. (1998). *Integer Programming.* Wiley.（`assets/01`）
- 动态规划：Bellman, R. (1957). *Dynamic Programming.* Princeton University Press.（`assets/01`）
- 模拟退火：Kirkpatrick, S., Gelatt, C. D., & Vecchi, M. P. (1983). *Optimization by Simulated Annealing.* Science.（`assets/01`）
- 禁忌搜索：Glover, F. (1989/1990). *Tabu Search — Part I / Part II.* ORSA Journal on Computing.（`assets/01`）
- 时间序列：Box, G. E. P., & Jenkins, G. M. (1970). *Time Series Analysis.* Holden-Day.（`assets/02`）
- 指数平滑/预测：Hyndman, R. J., & Athanasopoulos, G. (2018). *Forecasting: Principles and Practice.* OTexts.（`assets/02`）
- 梯度提升：Chen, T., & Guestrin, C. (2016). *XGBoost: A Scalable Tree Boosting System.* KDD.（`assets/02`）
- 支持向量机：Cortes, C., & Vapnik, V. (1995). *Support-Vector Networks.* Machine Learning.（`assets/02`）
- 网络流：Ford, L. R., & Fulkerson, D. R. (1956). *Maximal Flow Through a Network.* Canadian Journal of Mathematics.（`assets/04`）
- 匹配/指派：Kuhn, H. W. (1955). *The Hungarian Method for the Assignment Problem.* Naval Research Logistics Quarterly.（`assets/04`）

---

## 九、选题建议

> 本节全部结论只依据本报告已记录的证据：§四 模型合同、§五 逐题模型与验证方案、§六 编程依赖、§七 风险回退表、§八 文献，以及 §1.4 的输入异常 X1~X12。不引入任何未经核验的外部信息。
> **前置限制**：本节未考虑队伍实际专业背景（数学/编程/写作的人员配比），也未纳入 2026 当届官方规则（`format2026.doc` 未解析，见 §〇 待核验项）。正式选题前须补齐这两项。

### 9.1 定性对照

| 题 | 模型可实现性 | 数据/证据完备度 | 验证方案强度 | 论文可写性 | 综合风险 |
|---|---|---|---|---|---|
| A | 高（纯数值，无外部依赖） | 高（参数全给） | 强（网格/守恒/解析解三重验证） | 高 | 低 |
| B | 中（须在线实跑） | **低（无离线数据，X11）** | 弱（仅演练/正式测试统计，无离线复现） | 中 | **高** |
| C | 高（LP/MPC 成熟） | 高（附件最全） | 强（可行性 + 求解器交叉 + 基线对比 + 预报消融） | 高 | 低 |
| D | 高（规模小，可求最优） | 中（150 行，模板有 X6/X7 异名） | 强（硬校验 + 上界一致性） | 中高 | 中 |
| E | 高（库齐全） | 中（**效益口径缺定义 E-K1、关键词匿名化 X9、模板缺列 X8**） | 中（依赖口径稳健性） | 中高 | 中 |

### 9.2 量化评分（7 维，满分 5 分）

**权重设定依据**：CUMCM 为 72 小时三人赛制，**能否在时限内跑通全流程并写出完整论文**决定成绩的中位数，故"工作量与时间风险"权重最高（0.20）；模型、数据、验证、写作四项是基础保障，各 0.15；"得分上限"与"失分暴露面"影响的是分布的上尾与下尾而非中位数，各 0.10。

| 维度（权重） | A | B | C | D | E | 评分证据来源 |
|---|---|---|---|---|---|---|
| D1 模型可实现性（0.15） | 5 | 3 | 5 | 5 | 5 | §六：A/C/D/E 无外部依赖；B 需在线模拟器 + HTTP 客户端 + 时间预算 |
| D2 数据/证据完备度（0.15） | 5 | 2 | 5 | 3 | 3 | A 参数全给（附录2/3/4）；C 附件最全；B 无数据表（X11）；D 模板异名（X6/X7）；E 口径缺失 + 匿名化（E-K1/X9） |
| D3 验证方案强度（0.15） | 5 | 2 | 5 | 5 | 3 | §五 各题验证表：A 三重验证；C 四类检验；D 硬校验 + 上界一致性；B 仅演练统计；E 依赖口径稳健性 |
| D4 论文可写性（0.15） | 4 | 4 | 5 | 3 | 4 | A 推导深但问题2 输出体量大；C 公式/图表最丰富；D 正文成果形式少、可视化需自建；E 易流于描述；B 易写成工程日志 |
| D5 工作量与时间风险（0.20） | 3 | 1 | 4 | 4 | 3 | A 需自建数值格式 + 17~26 万行输出（X12）；B 有 25 min 窗口 / 3 次机会 / 09-13 17:30 截止；C 334 天逐日求解需向量化；D 规模最小；E 数据清洗与指标构建琐碎 |
| D6 得分上限（0.10） | 4 | 5 | 3 | 4 | 4 | 同题域越缺可比文献、结构越独特，创新空间越大。B 的**同题域**直引文献最薄（§八 B 题列 7 条**全部为跨域可迁移文献**，无同题域直引，故此处"薄"指同题域可比性而非条数——其条数实为五题最多）；A 移动边界 + 变物性；D 上界证明；E 口径定义；C 框架成熟，需靠 MPC/对偶/随机优化争分 |
| D7 失分暴露面（0.10，分越高越安全） | 4 | 1 | 5 | 3 | 2 | B 测试失败即失去全部数据支撑；E 主观口径使结论易被质疑；D 量化与上界论证做虚会被扣分；A 失分集中在细节 |
| **加权总分** | **4.25** | **2.45** | **4.60** | **3.90** | **3.45** | — |
| **排名** | 2 | 5 | **1** | 3 | 4 | — |

**分项最优**：D6 得分上限 B 最高（5），但被 D2/D3/D5/D7 拖到末位 —— 这正是"高风险高上限"的典型结构。C 在 D3/D4/D7 三项满分，是**下尾最厚**的一题。

### 9.3 收益—风险定位

**两个轴的定义（务必区分，避免与 §9.2 的 D6 混淆）**

- **纵轴 = 期望成绩水平**，用 §9.2 的**加权总分**度量（它综合了可实现性、证据、验证、写作与时间风险）。
- **横轴 = 失分暴露面**，用 §9.2 的 **D7** 度量（**D7 分越高 = 暴露越低 = 越安全**；此方向为唯一约定，下表档名一律照此读）。
- **D6（得分上限）只衡量"创新空间"**，是"在期望水平之上再多拿分"的能力，**不等于**期望成绩水平。B 的 D6 = 5（全表最高）却加权总分最低（2.45），正是因为它的 D2/D3/D5/D7 四项均为全表最低 —— 高创新空间被基础项拖垮，属"高风险、低期望"。

| 象限（总分 × 暴露） | 定位 | 题目 | 说明 |
|---|---|---|---|
| 高总分 · 低暴露 | **理想区** | **C**（4.60 · D7=5）、**A**（4.25 · D7=4） | C 是期望成绩最稳的一题（创新空间 D6=3 为全表最低，故不靠新奇取胜）；A 紧随其后，失分集中但可控 |
| 中总分 · 中暴露 | **中位区** | **D**（3.90 · D7=3） | 期望成绩居中；若把上界证明做实，可上探；做虚则下探 |
| 中总分 · 高暴露 | **谨慎区** | **E**（3.45 · D7=2） | 总分居中偏下、暴露偏高，短板是主观口径（E-K1）与问题1 的分析降级（X9） |
| 低总分 · 极高暴露 | **高风险区** | **B**（2.45 · D7=1） | 暴露为全表最高（D7 最低）；D6 全表最高（创新空间最大）但 D2/D3/D5/D7 四项全表最低，属"要么出彩要么失分"的极端形态 |

### 9.4 团队画像匹配

| 队伍的约束条件（按"最短板"判断） | 推荐 | 直接依据 |
|---|---|---|
| 编程一般，但数学与写作扎实 | **C** | 只需调用成熟求解器，无算法实现风险（§六） |
| 无人写过数值 PDE / 差分格式 | **不选 A** | A 的核心工作量是自建离散格式与非线性迭代（§5.1） |
| 编程强、有网络环境、现在就能开跑模拟器 | B 可考虑 | 但必须先满足 §9.5 的 B 题 Go 判据 |
| 想要"最优解 + 理论界"的硬结论 | **D** | 规模最小（150 变量），可给最优值与团数/容量上界（§5.4 M-D4） |
| 走数据挖掘路线、指标定义能力强 | **E** | 但须先固定"效益"口径并接受 X9 局限 |
| 三人都在同一专业（如纯数学） | **A 或 C** | A 偏机理推导、C 偏优化建模，都不依赖跨专业知识 |
| 时间已被压缩（不足 48 h） | **C 或 D** | 二者代码量最小、无外部依赖 |

### 9.5 Go / No-Go 判据（硬门槛，超时即转题）

选题不是一次性决定，须设定**可验证的失败判据**与**决策时点**（以竞赛开始时刻为 T0）。以下判据均取自各题验证方案中最基础、最不可绕过的一条：

| 题 | 决策时点 | Go 判据（全满足才继续） | No-Go 后的动作 |
|---|---|---|---|
| **B** | **T0 + 4 h** | ① `/enter` → **检测接口** → `/exit` 的完整往返已跑通（检测接口的具体端点名以 `附件2.docx` 协议为准，本报告无法解析该 docx，故不写死端点名）；② 程序侧自算的耗时与模拟器返回的计时一致（含"切换固定 1 s"，见 §5.2 时间模型）；③ 至少 5 次演练测试的清除比例达 100% | **立即转题**。正式测试机会不可再生，且 09-13 17:30 后不能启动新测试 |
| **A** | **T0 + 8 h** | 问题1 的数值解与常系数解析解比对最大相对误差 < 1%（§5.1 验证表） | 先排查单位与温标（K/℃）、边界实现；若仍不通过，降级为显式格式或转 C/D |
| **C** | **T0 + 6 h** | 单日 LP 跑通且约束违反数为 0（含 SOC 首末相等、供电量 ≥ 负载） | 检查量纲（kWh vs kW 折算，§5.3 量纲约定），**不要换题**——此类失败必为自身实现问题 |
| **D** | **T0 + 12 h** | 冲突检测结果与暴力两两判定完全一致（该检验项的显式表述见 §四 D1 行的"验证"列；§5.4 M-D1 为检测模型定义处，其 O(n²) 全枚举即暴力基准）；MILP 在 30 min 内给出可行解 | 改以启发式 M-D3 为主模型，MILP 仅作小规模对照 |
| **E** | **T0 + 6 h** | "效益"口径已书面固定，且两种阈值口径（`q_30/q_50/q_70`）下分类一致率 ARI > 0.6 | 先固定口径再推进；若始终不一致，须在论文中报告敏感性而非强行给单一结论 |

> 上述时点按"三个半天"的常规节奏给出，若队伍作息不同可按比例缩放，但 **B 题的绝对截止（2026-09-13 17:30）不可缩放**。

### 9.6 与本项目《五题选题评估报告》的口径差异核对

本项目另有一份面向团队分享的 `CUMCM2026Problems/五题选题评估报告.md`（信息整理视角）。两份结论**方向一致**（C 最优、B 风险最高），但有两处**实质性口径差异**，使用时须注意：

| 维度 | 《五题选题评估报告》 | 本报告（建模手） | 差异原因与建议 |
|---|---|---|---|
| "资料/证据丰富度" | **把学术文献量计入**：A=C=5.0，D=E=4.3，B=3.5 | **只计"能否离线复现与验证"**：A=5、C=5、D=3、E=3、B=2 | **文献丰富 ≠ 可验证**。B 的文献支撑虽薄（3.5），真正致命的是无离线数据（X11）；E 文献充足（4.5）但口径缺失（E-K1）使证据链不闭合。**建议以本报告为准**，因为它建立在模型合同与验证方案之上 |
| "论文易写度" | C=5.0 > A=4.5 > D/E=3.5 > B=3.0（分题打分，含可视化素材） | D4 列：C=5 > A=4 = B=4 = E=4 > D=3 | 本报告把"正文成果形式少、可视化需自建"单列为 D 题的主要扣分项；A 的扣分来自问题2 的 17~26 万行输出呈现压力（X12） |
| 结论表述 | "时间充裕选 A，求稳选 C" | 同结论，但补入 §9.2 加权排序与 §9.5 Go/No-Go 判据 | 两份互补：前者用于团队快速对齐，后者用于决策与止损 |

### 9.7 推荐方案与 72 小时关键路径

**主推：C 题**（加权 4.60，下尾最厚）；**次推：A 题**（4.25，上限更高但需数值能力）；**D 题**（3.90）作为"想给硬结论"时的选择。

| 阶段 | C 题（主推） | A 题（次推） | 共同要点 |
|---|---|---|---|
| 0~6 h | 读题 + 量纲自检（kWh/kW）+ 问题1 LP 跑通并过约束校验 | 读题 + 量纲自检（cm/m、℃/K）+ 问题1 隐式差分并与解析解比对 | 先跑通**最小可运行结果**（Skill `P1` 门禁），不追求全量 |
| 6~20 h | 问题2 全年 334 天向量化求解 + 问题3 MPC 分阶段求解 | 问题2 变物性求解 + 问题3 事件定位求烘干时间 | 中期必须有一次全流程贯通，验证输出模板可写入 |
| 20~36 h | 问题4 波动电价重算（问题4-2/4-3）+ 预报时刻消融实验 | 问题4 移动边界 + 固定域对照 | 此阶段完成**参数扫描/灵敏度**与**三类图**（raw/process/result） |
| 36~54 h | 论文正文：模型推导 + KKT/对偶解释 + 结果分析 | 论文正文：方程离散推导 + 守恒/收敛验证 + 灵敏度 | 论文手并行启动，不等全部结果（按 `W1 证据大纲` 先搭骨架） |
| 54~66 h | 图表定稿 + 结果表核对（逐项对照模板列名与单位） | 同 | 逐项核对 X1~X12 中与结果文件相关的要求（列头、工作表名、时段起点） |
| 66~72 h | 摘要 + 全文一致性检查 + 附录与支撑材料打包 | 同 | 留出至少 4 h 用于"零改动"复核，不做任何新增实验 |

### 9.8 结论

1. **综合最优：C 题**（加权 4.60）。它在模型可实现性、数据完备度、验证强度、论文可写性、失分暴露五项上均为最高或并列最高，唯一短板是创新空间偏中等 —— 需靠 MPC 的两阶段结构、对偶经济解释、波动电价下的随机/鲁棒优化拉开差距。
2. **上限最高且可行：A 题**（4.25）。它是五题中唯一必须自建数值格式的题，机理清晰、验证链条最完整；但必须接受问题2 的 17~26 万行输出（X12 待确认）与后期干燥极慢（§5.1 风险栏）两项工程压力。
3. **硬结论路线：D 题**（3.90）。规模最小、可给最优值与上界，但"多目标量化依据"和"问题3 上界证明"必须做实，否则反成失分点；正文可视化需自行设计。
4. **E 题**（3.45）适合统计/数据方向，但选题前必须完成两件事：锁定"效益"口径（E-K1）、接受关键词匿名化带来的问题1 分析降级（X9）。
5. **B 题**（2.45）不建议作为首选。其得分上限是五题最高（D6 = 5，同题域文献最薄），但**证据完备度、验证强度、时间风险、失分暴露四项均为最低**：无离线数据（X11）、正式测试仅 3 次、且 2026-09-13 17:30 后不能启动新测试。只有在 §9.5 的 T0+4h 判据全部满足时才继续。
6. **不要用"文献多不多"选 B/E**：B 的文献薄但风险真因是"无数据"，E 的文献足但风险真因是"口径未定义"。选题应看**证据链能否闭合**，而非资料是否好看。

---

## 十、作者自检记录（对照 `references/质检清单.md`）

| 检查项 | 结果 | 证据 |
|---|---|---|
| 已读完题目和全部附件，输入保持只读 | 通过 | 5 份 PDF 全文提取；11 个数据附件与 16 个结果模板逐一读取表头与规模；输入目录未被写入，临时脚本已删除 |
| 先完成题目理解，再写结论目标和模型方案 | 通过 | §五 每子问题先写"结论类型"，再给模型 |
| 每子问题最多两个独立模型体系；物理题按模型族计数 | 通过 | §四 表格已逐行核对；A 题 M-A1/A2/A4 按模型族合并计 1，M-A3 为第二体系 |
| 每个变量、参数、单位、目标函数和约束定义完整 | 通过 | §五 各题给出集合、变量、目标、约束与量纲约定；完整符号见 `术语表格.md` |
| 数据判定标准有题目、官方、文献或分析依据 | 通过 | §三 每条假设标"依据"列；§五 阈值均注明依据类型 |
| 创新点与题目结构、实现和验证一一对应 | 通过 | 创新点集中在：A4 移动边界坐标变换、B3 信息增益策略、D3 上界论证、E 效益口径稳健性 |
| 模型能由 Python 或 MATLAB 实现，依赖明确 | 通过 | §六 依赖与求解器清单 |
| 验证方法和失败条件明确 | 通过 | §五 各题验证表（含阈值）+ §七 风险回退表 |
| 文献可追溯到 DOI 或原始出版页面 | **部分通过** | §八 全部条目有 DOI/出版信息且双引擎交叉验证；期刊/卷页缺失条目已标"待补"；**尚未逐条打开出版页人工核对，列为待办** |
| 只交付 `题目分析报告.md` 和 `术语表格.md` | 通过 | 本文件 + `术语表格.md`；无第三份产物 |
| 两个文件写入 `PROJECT_ROOT`，未覆盖 `SKILL_ROOT` | 通过 | 写入 `d:\Code\2026BJCUMCM`；`SKILL_ROOT` 仅只读访问 |
| 作者自检完成后派发质检 Subagent 执行 `M1` | 通过 | 已于本轮派发**未参与编写**的独立质检 Subagent；首轮回执为 `FAIL`，返工后见 §十一 |
| `M1` 通过后两个产物未发生未经复验的实质变化 | **待复验** | 第四轮 `PASS` → 扩充 §九 失效 → 第五轮 `PASS` → 修 5 条 P2 失效 → 第六轮 `PASS` → 修 §9.3 暴露档名（第七轮）失效 → **须重新冻结快照并执行第七轮 `M1`**。`术语表格.md` 自第四轮起未再改动（哈希保持 `80EA4806…3582FA5C`） |

---

## 十一、M1 返工记录与修正清单

**首轮 `M1` 回执状态：`FAIL`**（独立质检 Subagent，未参与本报告编写）。回执按 `P0/P1/P2` 列出问题；下表逐条记录**主 Agent 的核实结论与已实施的修正**。依据 Skill `references/Subagent调度.md` 第 19 条，本轮修正属"实质变化"，原回执全部失效，须重新执行 `M1`。

### 11.1 已修正项

| 回执编号 | 回执指出的问题 | 核实结论 | 修正内容 |
|---|---|---|---|
| **P0-1** | B 题时间模型把"频道切换 1 s"误作 `1 s × \|Δ频道\|` | **成立**。题面附录2 第(4)条为"任意两个频道之间的切换时间为 1 秒"（固定值），附件1 亦写明"未切换频道则耗时 0" | §5.2 时间模型改为 `1 · 1[发生频道切换]`，并加"易错点"说明；§三 B-K3 同步改为"1 s/次（固定值）"；`术语表格.md` §二 新增"频道切换时间"条目并标注"不得写作 1 s × 频道差" |
| **P1-1** | C 题 `g_t/ch_t/dis_t` 在报告（kW）与术语表格（kWh）间量纲冲突 | **成立**。同一符号两套单位，破坏可复现性 | 统一为 **kWh 电量口径**（与附件列名、结果模板列名一致）：目标改为 `min Σ p_t·g_t`；C-1 改为 `≥ (L_t-PV_t)·Δt`；功率上限 5000 kW 折算为 `833.333 kWh/时段`；C-3 改为 `E_t = E_{t-1} + η·ch_t - dis_t/η`；§5.3 增加"量纲约定"说明块；`术语表格.md` §三 同步（新增"最大充放电功率"条目并注明折算） |
| **P1-2** | D 题问题4 的"仅部分 C 类 + `\|Δm\| ≤ 10Δt`"约束遗漏 | **成立**。题面问题4 明写该约束，原报告未转录 | 新增 §5.4 `#### M-D5`，给出 `\|Δm_i\| ≤ 10`、`u_i+α_i+β_i+γ_i ≤ 1` 四参数互斥、间隔改变后占用整体平移的展开式与重校验要求；§四 合同表 D4 行改为 M-D5 并补约束；§三 D-K2 补"仅问题4、仅部分 C 类、≤10Δt"；`术语表格.md` §四 新增"间隔调整量 `Δm_i`""间隔开放集 `S`" |
| **P1-3** | A 题 result2 输出规模表述错误（写成 3 h / 10⁴ 行） | **成立**。问题1 明写"1800 s 内"，问题2 未给时间上界而其模型对象是"整个烘干过程 2~3 天" | 新增待确认项 **X12**，明确两种读法（全过程 ≈17.3 万~25.9 万行 vs 仅 3 h = 10801 行）并声明本报告按读法①表述；§四 A2 风险列改为"≈17.3 万~25.9 万行 × 21 列（读法①，见 X12）"；§1.4 X1 同步修正 |
| **P1-4** | A 题 `D(C)` 量级风险评估漏掉 Arrhenius 温度因子，结论不成立 | **成立**。补入 `exp(-3850/T)` 后 `D ≈ 1.5e-9~6e-9 m²/s`，属正常量级 | §5.1"风险与回退"整段重写：给出两种工况的完整数值复核表，明确"**不构成 P0 风险**"；真正的风险改为"后期干燥速率极低（问题1 的 `D` 随 `C` 下降约 2 个数量级）"与"温度必须用 K 代入"；§七 原 P0 行相应下调为 P1 并改写 |
| **P2-1** | §1.1 中 E 题哈希疑为笔误，且交叉引用指向不存在的快照清单 | **成立**。D 题哈希应为 `04945C2DB24CD6CB`（原误写为 E 题哈希改首字符） | 修正 D 题哈希；注释中的错误引用改为 §1.5；**新增 §1.5 完整输入快照清单**（**37 条**文件的 SHA-256 前 16 位：官方输入 32 + 文本提取件 5） |
| **P2-2** | §八 表头自述"逐条含期刊/会议"与条目不符 | **成立** | §八 说明改为"期刊/卷期页信息部分条目检索源未返回"；A 题第 3 条标"卷期页待补"，B 题第 1、2 条标"期刊名待补" |
| **P2-3** | §1.3 C 题 `result1` 记法（145×2）与约定不符 | **不成立**。`145×2` 本身即"145 行 × 2 列"，与同表 `(335×147)` 约定一致 | 为消除歧义，已将全部 `(行×列)` 显式写全（如 `(145 行 × 2 列，即 144 时段 + 表头)`） |
| **P2-4** | 假设分级"≤7"是指每题还是全文，口径未声明 | **成立**（表述歧义） | §三 增加"计数口径"说明：按**每题**计数，并列出逐题实点 A 3/5、B 3/4、C 3/5、D 3/5、E 3/6 |
| **P2-5** | D 题多目标次序与题面不一致（幅度被提到优先级之前） | **成立**。题面次序为"少撤销 > 少调整数量 > 保优先级 > 减小幅度" | §5.4 目标函数改为四层 `W1≫W2≫W3≫W4`，分别对应四项，并注明"不可把③与④互换" |
| **P2-6** | 术语表格缺 B 题参与计时的主要量 | **成立** | `术语表格.md` §二 新增 9 条：移动速度、频道切换时间、检测时间、精确定位时间、清除时间、强信号直清距离、精测/清除距离上限、虚拟时间上限、程序运行时间上限 |
| **条款7 补充** | B 题验证方案无量化阈值 | **成立**（受 B 无离线数据所限） | §5.2 新增"验证方式与阈值（B3/B4）"表：清除完整性（100%，题面硬指标）、时间预算（≤20 min / ≤25 min）、可提交判据（20/20 且 `CV<20%`）、策略对比、泛化性、退化边界 |

### 11.2 未采纳 / 无法验证项

| 回执内容 | 处理 |
|---|---|
| 回执自述"无法重算 SHA-256、无法解析 xlsx/docx 内部、无网络" | 属工具能力限制，**不作为缺陷**。本报告 §1.2/§1.3/§1.5 的哈希与规模由主 Agent 以 `Get-FileHash` 与 `openpyxl` 实测得到，已在 §1.5 注明生成命令；回执亦通过文件大小层面印证了"模板字节相同"的判断 |
| §八 各 DOI 的真实性 | **无法在离线环境验证**，已在 §八 显式标注"须在论文引用前打开出版页人工核对"；未核验前不得写入正式参考文献表 |

### 11.3 复验要求

1. 重新冻结快照：对修正后的 `题目分析报告.md` 与 `术语表格.md` 重算 SHA-256；
2. 由**未参与本轮修正**的质检 Subagent 重新执行完整 `M1`（不得以本回执作为"部分修正即通过"的依据）；
3. 复验重点关注：P0-1 的 B 题时间模型、P1-1 的 C 题量纲一致性、P1-2 的 D 题问题4 约束、P1-4 的 A 题 `D` 量级结论是否已按上述表格改正；
4. 复验 `PASS` 后方可进入编程阶段（`P1` 门禁）或宣称建模交付完成。

### 11.4 第二轮复验（复验回执：`FAIL`）的修正

第二轮独立复验确认 §11.1 中的 **P0-1、P1-1、P1-2、P1-4 与全部 P2 项"已确认修正"**（复验方独立手算的 `D = 5.607e-9 / 1.9957e-9 m²/s` 与本报告 5.6e-9 / 2.0e-9 相符，`5000 × 1/6 = 833.333 kWh` 相符），同时指出以下未被一次修完的项。本轮逐条处理：

| 第二轮编号 | 复验指出的问题 | 核实结论 | 修正内容 |
|---|---|---|---|
| **P1-1**（上一轮 P1-3 的残留） | §七 风险表仍保留 `问题2 需 10⁴ 行 × 21 列` 的过期规模，与 §1.4 X12、§四 A2 的"读法① 17.3 万~25.9 万行"矛盾 | **成立**。§11.1 只改了 X1 与 §四 A2，漏改 §七 | §七 该行改写为"问题2 按**读法① ≈17.3 万~25.9 万行 × 21 列**，按**读法② 则为 10801 行 × 21 列**（读法辨析见 X12）"，并在回退方案中写明触发条件。全文复检：`10⁴` 仅剩 §5.2 "随机 10⁴ 组算例"（B 题对拍用，与输出规模无关）与本记录中的历史描述；`10800` 仅剩 C 题 SOC 上界 |
| **P2-1** | §1.5 未列入 `CUMCM2026Problems/五题选题评估报告.md`；且 §11.1 自述"35 个文件"与清单实际条数（37）及目录文件数（38）三处不一致 | **成立** | §1.5 增加"清单条数（已核对）"说明：本表 **37 条** = 官方输入 32 + 文本提取件 5；目录实际 **38** 个，多出的 `五题选题评估报告.md` 为非官方输入、不参与建模，已显式声明；§11.1 的"35 个文件"同步改为"**37 条**" |
| **P2-2** | `术语表格.md` §二 存在重复条目："清除时间"出现两次、"程序运行时间"与"程序运行时间上限"语义重叠 | **成立** | 删除无符号的"清除时间"重复行与"程序运行时间"重复行，保留带符号/带上限的版本；"光学精确定位"改为动作定义并指向"精确定位时间"；"平均定位清除时间"的对照项改为"程序运行时间上限" |
| **P2-3** | `术语表格.md` §七 的符号处理约定（`D_m`、`n_{click}`、`R_{reg}`）与 §一/§五 实际符号（`D`、`C`、`R_d`）互相矛盾，且未落实到报告正文 | **成立** | 落实为统一符号：§一 扩散系数改为 **`D_m`**（注明原文记作 `D`），§五 点击量改为 **`n_click`**、新注册数保持 **`R_d`**；§七 表头改为"处理约定（已在 §一/§二/§五 各表中落实）"并把 `R_{reg}` 更正为 `R_d`、新增 `S` 的行；报告 §5.1 增加"符号约定"说明块，声明正文按题目原文记号书写、与术语表 `D_m` 的对应关系 |
| **P2-4** | §四 E3 行引用的 `M-E5` 在 §5.5 无定义小节 | **成立** | §5.5 新增 `#### M-E5 多目标对比模型（问题3 的第二体系）`：给出三目标向量、NSGA-II 参数、Pareto 前沿 + TOPSIS 折衷，并明确其**仅作对照**、`result3.xlsx` 以 M-E4 的解为准 |

### 11.5 第三轮复验（复验回执：`FAIL`）的修正

第三轮独立复验逐点确认：**点①（§七 A 题体量口径）、点②（§1.5 条数/目录计数）、点③（术语表 §二 去重）、点⑤（M-E5 定义）"已确认修正"**，点④ 的 §七↔§一/§五 与报告 §5.1 符号块"已确认修正"。仅遗留 1 项 `P1` 与 3 项 `P2`，本轮全部处理：

| 第三轮编号 | 复验指出的问题 | 核实结论 | 修正内容 |
|---|---|---|---|
| **P1**（阻塞项） | `术语表格.md` §六 跨题清单的 E 题列仍写"点击量符号（**本报告用 `C`**，须与 A 题区分）"，与同文件 §五/§七 已统一的 `n_click` 直接矛盾 | **成立**。系上一轮符号统一漏改 §六 | §六 该格改为"点击量（本报告统一用 **`n_click`** 表示，**不得用 `C`**，见 §五/§七）"；并全文复查 §六 其余各行是否存在同类"改名后未同步"的残留（C 题列的储能容量已同步为 `E_max`，其余无冲突） |
| **P2-a** | §1.5 括号内分解"5 PDF + 11 数据附件 + E题附件1 计 1 + 16 模板 + format2026.doc"按字面相加 = 34 ≠ 32，措辞有歧义 | **成立**（实测数据附件为 10 个） | §1.5 说明改为"官方输入 32 条 = **5 份题目 PDF + 10 个数据附件（A2+B2+C4+D1+E1）+ 16 个结果模板（A4+C5+D4+E3）+ `format2026.doc`**"，即 5+10+16+1=32 |
| **P2-b** | `术语表格.md` §五 中 `B` 同时表示"跳出率"与"预算"，且 §七 未提示该重载 | **成立** | §五 跳出率符号改为 **`BR`**、预算符号改为 **`B_total`／`B_d`**（并注明"不得简写为 `B`"）；§七 新增 `B` 行说明该重载已消解 |
| **P2-c** | 报告 §5.5 用 `r_k` 表示关键词级预期注册量，与 A 题径向坐标 `r` 同名；术语表仅有日级 `R_d` | **成立**（跨题符号重载未提示） | 报告 §5.5 M-E5 的效益系数改为 **`reg_k`** 并加注；`术语表格.md` §五 新增"预期注册量 `reg_k`"条目（与日级"新注册数 `R_d`"分行区分颗粒度）；§七 新增 `r`、`I` 两行提示 |

### 11.6 第五轮复验（`PASS`）与第六轮修正

**第五轮复验结论：`PASS`。** 复验方独立复算了 §9.2 的加权评分（5 个总分、5 个排名、权重合计 1.00 全部相符）、抽查了 8 格评分证据来源、逐条核对了 §9.5 判据的溯源、**打开 `五题选题评估报告.md` 原文比对了 §9.6 的三处转述（全部属实）**、验证了 §9.7 赛程区间连续无缺口且末值为 72、核对了 §9.8 结论数字与 §9.2/§9.3 一致，并完成回归检查（A 题体量口径三处一致、B 题切换仍为固定 1 s、章节编号连续、交叉引用有效、术语表无重复与符号冲突）。

复验同时提出 5 条不阻塞的 `P2`。其中 2 条属**实质缺陷**（非文字瑕疵），已在本轮修正；另 3 条为提示性。修正如下：

| 第五轮编号 | 复验指出的问题 | 核实结论 | 修正内容 |
|---|---|---|---|
| **P2-a**（实质） | §三 的假设编号为 `E-K1`，而 §九 四处引用写作 `K-E1`，标签不匹配 | **成立**。属交叉引用错误，出现 4 次 | 全文 4 处 `K-E1` 统一改为 **`E-K1`**（§9.1、§9.2 D2 行、§9.6、§9.8 第 4 条），与 §三 一致 |
| **P2-b**（实质） | §9.3 把 C 放入"**高上限**·低暴露"并称"不封顶"，但 §9.2 中 C 的 D6（得分上限）为 3，是**全表最低**；同时 §9.3 又用"D6 最高"解释 B 的"高上限"，口径自相矛盾 | **成立**。是本节新写的**论证缺陷**：把"创新空间 D6"与"期望成绩水平"混为一谈 | §9.3 重写：**显式定义两轴** —— 纵轴 = 加权总分（期望成绩水平），横轴 = D7（失分暴露面）；并明确标注"D6 只衡量创新空间，不等于期望成绩水平，B 的 D6 最高但总分最低正是被 D2/D3/D5/D7 四项全表最低拖垮"。四象限标签按总分×暴露重排：理想区 C+A、中位区 D、谨慎区 E、高风险区 B |
| **P2-c** | §9.2 的 D6 行以"（§八）"标注"B 文献最薄"，但 §八 B 题实列 7 条（为五题最多），易被误读 | **成立**（属表述歧义） | D6 行补注："B 的**同题域**直引文献最薄（§八 B 题 7 条**全部为跨域可迁移文献**，无同题域直引；其条数实为五题最多）" |
| **P2-d** | §9.5 的 B 题判据写死 `/measure` 端点，但该端点名只存在于无法解析的 `附件2.docx`，不可验证 | **成立** | 判据改为"`/enter` → **检测接口** → `/exit` 的完整往返（检测接口的具体端点名以 `附件2.docx` 协议为准，本报告无法解析该 docx，故不写死端点名）" |
| **P2-e** | §9.5 的 D 题判据"与暴力两两判定一致"实际落在 §四 D1 验证列，§5.4 M-D1 无独立验证段 | **成立**（引用落点不精确） | 判据内补注检验项所在位置："（该检验项见 §四 D1 行的'验证'列与 §5.4 M-D1）" |

### 11.7 第六轮复验（`PASS`）与第七轮修正

**第六轮复验结论：`PASS`。** 复验方逐条确认 5 条 P2 的修正到位（并对 `K-E1`→`E-K1`、`/measure` 写死、D 判据落点做了全文检索定位，正确区分了"现行引用"与"§11.6 历史记录"），独立复算 §9.2 的加权总分与排名全部相符，并确认回归项（A 题体量口径三处一致、B 题固定 1 s、章节编号连续、§11.6 与 §10 状态一致）未受影响。

复验同时指出：我在 §9.3 重写时**引入了一处新的自相矛盾** —— 该节自定横轴为"D7 分越高越安全"，但 E 行档名写作"低暴露"（其实 D7=2 属高暴露）、B 行写作"极低暴露"（其实 D7=1 属极高暴露），即档名与自定方向相反（`P2`，不阻塞）。本轮修正：

| 第六轮编号 | 问题 | 修正内容 |
|---|---|---|
| **P2-1**（本轮新引入） | §9.3 四象限的暴露档名与自定横轴方向相反：E 标"低暴露"、B 标"极低暴露" | ① 横轴定义处补写"**此方向为唯一约定，下表档名一律照此读**"；② E 行档名改为"**高暴露**"、行内说明改为"总分居中偏下、暴露偏高"；③ B 行档名改为"**极高暴露**"并补"暴露为全表最高（D7 最低）"。**D7 分值（C5/A4/D3/E2/B1）与分区归属均未改动** |
| **P2-2** | §9.5 D 判据的落点提示指向"§5.4 M-D1"，而该节无独立"与暴力对拍"验证句 | 改为"该检验项的**显式表述**见 §四 D1 行的'验证'列；§5.4 M-D1 为检测模型定义处，其 O(n²) 全枚举即暴力基准" |

**当前 M1 状态：`FAIL`（一轮）→ `FAIL`（二轮）→ `FAIL`（三轮）→ `PASS`（四轮）→ 因 §九 扩充失效 → `PASS`（五轮）→ 因 P2 实质修正失效 → `PASS`（六轮）→ 因本轮 §9.3 档名修正失效 → 待第七轮复验**（仅 `题目分析报告.md` 变化；`术语表格.md` 自第四轮起未再改动，哈希保持 `80EA4806…3582FA5C`）。按 Skill 规则，**在复验 `PASS` 之前不得进入编程阶段（`P1` 门禁）**，也不得宣称建模交付完成。

> 收敛轨迹：P0 项 1→0（第四轮起为 0）；P1 项 5→1→1→0（第四轮起为 0）；P2 项 6→5→3→2→5→2→2。**第四轮起，问题性质已全部收敛为"文件内部表述一致性"，未再出现模型、公式、单位或约束层面的错误。** 这也说明：每新增一节内容都可能引入新的自相矛盾，**新增内容后必须复验，事后修改必须先冻结快照再验证**。
